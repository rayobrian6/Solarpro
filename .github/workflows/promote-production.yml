# ============================================================
# promote-production.yml — DISABLED
#
# This workflow has been consolidated into main.yml.
# Kept as placeholder to avoid broken references.
# All deployment logic is in main.yml.
# ============================================================

name: Promote Production (disabled)

on:
  workflow_dispatch:
    # Manual trigger only — not automatic
    # Use main.yml for all production deployments

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: |
          echo "Deployment is handled by main.yml on push to master."
          echo "To deploy manually, use the Vercel dashboard or trigger main.yml."