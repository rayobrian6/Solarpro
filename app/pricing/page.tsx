'use client';
/**
 * Pricing Configuration — accessible to ALL authenticated users.
 * Re-exports the same component that was previously admin-only.
 */
export { default } from '@/app/admin/pricing/page';