# SAM 2 Segmentation Service — Python dependencies
# Install: pip install -r requirements.txt
#
# NOTE: PyTorch, SAM 2, and their core deps are installed separately
# in the Dockerfile (steps 1-6) because they require special handling.
# This file contains only the web/app-specific deps.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Image processing
Pillow==11.1.0
opencv-python-headless==4.11.0.86
# numpy is installed in Dockerfile step 1 (2.1.2) — don't override it here
# numpy==1.26.4

# Utilities
python-multipart==0.0.20
httpx==0.28.1
pydantic==2.10.4
