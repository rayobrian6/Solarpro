# SAM 2 Segmentation Service — Python dependencies
# Install: pip install -r requirements.txt
#
# NOTE: PyTorch and SAM 2 are installed separately in the Dockerfile
# (before this file) because they require special index URLs or
# building from source. This file contains only the additional deps.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Image processing
Pillow==11.1.0
opencv-python-headless==4.11.0.86
numpy==1.26.4

# Utilities
python-multipart==0.0.20
httpx==0.28.1
pydantic==2.10.4
