# services/vision/requirements.txt
# Pin to ranges so minor patches are pulled automatically, major versions are locked.

# ── core inference ──────────────────────────────────────────────────────────
ultralytics>=8.2.0,<9.0.0      # YOLOv8 + training + validation
torch>=2.2.0,<3.0.0            # PyTorch CPU/GPU backend
torchvision>=0.17.0,<1.0.0     # image transforms used by ultralytics
opencv-python-headless>=4.9.0  # image decode/encode (headless = no GUI deps)
Pillow>=10.2.0                  # image loading fallback used by ultralytics
numpy>=1.26.0,<2.0.0           # array ops

# ── API server ───────────────────────────────────────────────────────────────
fastapi>=0.110.0,<1.0.0        # web framework
uvicorn[standard]>=0.29.0      # ASGI server (includes websockets + httptools)
pydantic>=2.6.0,<3.0.0         # request/response validation

# ── utilities ────────────────────────────────────────────────────────────────
python-multipart>=0.0.9        # form/file upload support (FastAPI optional)
httpx>=0.27.0                  # async HTTP client for health-check tests