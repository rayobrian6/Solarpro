# Phase 1: Google Solar API as Pipeline C — Final Steps

## Context
Pipeline C implementation is complete (types, client, adapter, API route, UI wiring).
Need to run checks, fix errors, commit, and push.

## Tasks
- [ ] Run `npx tsc --noEmit` — fix any TypeScript errors
- [ ] Run `npx eslint . --ext .ts,.tsx` — fix any ESLint errors
- [ ] Run `npx vitest run` — fix any test failures
- [ ] Commit all Pipeline C changes to dev
- [ ] Push to dev remote
