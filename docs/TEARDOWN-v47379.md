# TEARDOWN — Permit Package v47379 (BRAIDON M PILLA) vs Wyssling PE-sealed reference

Date: 2026-07-03 · Method: 116-agent workflow (31 finders × personas, dedup, adversarial verify + code root-cause) · 498 raw → 85 unique → 61 CONFIRMED (23 verifiers lost to session rate-limit — see end).

Evidence: sheet PNGs + reference renders in scratchpad (regenerate via shoot.mjs/ref_render.py recipe). Source package: Downloads/PermitPackage-BRAIDON M PILLA — Solar (9).html (engine v47379).


## P0 — Permit-killers / wrong data / code violations (25)

### P0-1 [SET-WIDE (PV-0, PV-4A/B, PV-5, E-1, SCHED)] Package contradicts itself on interconnection method: supply-side tap vs 100A load-side backfeed breaker

**Description:** The set tells mutually exclusive interconnection stories. PV-0's SYSTEM SUMMARY lists '(N) 100A BACKFEED BREAKER (NEC 705.12(B))' and Scope of Work says 'per NEC 705.12', while SYSTEM INFORMATION on the same sheet, PV-4A, PV-5's L-5 placard, and E-1 all declare 'Supply Side Tap'. PV-4A simultaneously states the 120% rule 'does not apply (supply-side)' and includes a load-side 'Backfeed Breaker 120% Rule' methodology row; PV-4B's (clipped) load calc concludes the load-side option FAILS ('120% Busbar Rule — FAIL — UPGRADE PANEL': 200A bus x 1.2 - 200A main = 40A max vs 100A backfeed), and SCHED still orders a 'Backfeed Breaker Label per NEC 705.12'. A plan checker cannot determine which design is being permitted, and one of the package's own analyses says the shown load-side design is non-compliant — automatic correction/rejection.

**Evidence:** HTML line 1019 ('100A BACKFEED BREAKER (NEC 705.12(B))'), line 1240 ('Supply Side Tap — NEC 705.12(A)'), lines 1919-1920 (PV-4A '705.12 Busbar Rule — N/A (Supply-Side Tap)'), line 1963 (PV-4A 'Load-side connection method'), line 2238 (PV-4B 'FAIL — UPGRADE PANEL'), line 3963 (sld_metadata 'SUPPLY_SIDE_TAP'); SCHED BOM item 21 LABEL-BF vs E-1 'Backfed Breaker: N/A — Tap Connection'.

**Root cause:** lib/permit/sections/electricalPages.ts:342-399 (PV-4B load calc unconditionally runs the load-side 120% analysis — prints "requires a ${bfAmps}A backfeed breaker installed at the load end" and "FAIL — UPGRADE PANEL" without ever checking project.interconnectionMethod); also lib/permit/sections/coverSheet.ts:61-62,206-207 (PV-0 always emits "(N) ${backfeedA}A BACKFEED BREAKER (NEC 705.12(B))" whenever backfeedA derives, even though line 70 of the same file reads interconnectionMethod = SUPPLY_SIDE_TAP for the adjacent SYSTEM INFORMATION row); plus static leaks: electricalPages.ts:78 ("Backfeed Breaker … Load-side connection method" methodology row on PV-4A) and the unconditional LABEL-BF BOM item in lib/permit/utils/bomForPermit.ts.

**Fix sketch:** Single-source the method: generatePermit.ts:490-512 already resolves interconnMethod / elecResult.busbar.busbarRule ('supply-side'), so thread that flag into every sheet generator. In electricalPages.ts wrap the load-calc block in a branch — supply-side: replace steps 8-9 + interpretation with an NEC 705.11 supply-side analysis (tap conductor/OCPD sizing, "120% rule N/A"); load-side: keep current table. In coverSheet.ts gate the summary row: supply-side → "(N) 125A FUSED SUPPLY-SIDE TAP DISCONNECT (NEC 705.11)" instead of the backfeed-breaker line; likewise gate electricalPages.ts:78's methodology row and the LABEL-BF BOM item.

**Verifier notes:** Every cited contradiction verified in the raw HTML. Load-side story: line 995 Scope of Work "INTERCONNECT PV SYSTEM TO EXISTING 200A SERVICE PANEL PER NEC 705.12"; line 1019 "(N) 100A BACKFEED BREAKER (NEC 705.12(B))"; line 1963 PV-4A methodology "Backfeed Breaker | 120% Rule | Load-side connection method"; lines 2237-2254 PV-4B "100A breaker required" → "FAIL — UPGRADE PANEL" with prose "The PV system requires a 100A backfeed breaker installed at the load end of the existing 200A busbar"; BOM item 21 "Backfeed Breaker Label per NEC 705.12". Supply-side story: line 1240 PV-0 SYSTEM INFORMATION "Supply Side Tap — NEC 705.12(A)"; lines 1919-1920 PV-4A "Busbar Rule — N/A (Supply-Side Tap) ✓ PASS"; line 2629 PV-5 L-5 placard "Interconnection: Supply Side Tap — NEC 705.12(A)"; E-1 SLD lines 3321/3418/3423 "SUPPLY SIDE TAP — NEC 705.11" / "Backfed Breaker: N/A — Tap Connection"; line 3963 sld_metadata "selected: STRING / SUPPLY_SIDE_TAP" (also note STRING vs the 53 IQ8A micro BOM — a separate inconsistency). Math independently recomputed and correct: 18.5 kW AC / 240V = 77.1A; ×1.25 = 96.4 → next standard OCPD 100A; 200A bus × 1.2 = 240A; 240 − 200 = 40A max backfeed; 100A > 40A → load-side genuinely fails, so supply-side tap is the only viable design — yet PV-0's summary and PV-4B present the failing load-side design as the installed scope. P0 stands: the package's own analysis condemns half of its own declared design; a plan checker cannot tell which interconnection is being permitted. Minor secondary nit: the set cites supply-side as both 705.12(A) (helpers.ts:101, NEC 2017 numbering) and 705.11 (E-1/PV-4A, NEC 2020+ numbering) — worth normalizing to the AHJ's adopted NEC cycle while fixing.

_Reported by 9 agent(s)._

### P0-2 [PV-0] Literal 'NEC 705.12(B): REQUIRES REVIEW' — an unresolved compliance flag printed on the permit cover

**Description:** SYSTEM INFORMATION contains the row 'NEC 705.12(B) | REQUIRES REVIEW'. This is internal QA telemetry leaking into the AHJ deliverable: it announces in writing that the busbar compliance analysis was never resolved. The generator emits it whenever backfeedA exceeds (1.2 x bus - main), even when the interconnection is a supply-side tap where 705.12(B) does not apply. Grounds for intake rejection on page 1.

**Evidence:** PV-0.png System Information last row; HTML lines 1242-1243: '<td class="il">NEC 705.12(B)</td><td class="iv">REQUIRES REVIEW</td>'.

**Root cause:** lib/permit/sections/coverSheet.ts:59-67 (rulePass computed via naive 120% math with no supply-side branch) feeding the ternary at :302-303 that prints 'REQUIRES REVIEW'

**Fix sketch:** In coverSheet.ts, stop recomputing the busbar rule locally: read the already-resolved result from input.compliance.busbar (generatePermit.ts:540-553 correctly distinguishes supply-side via busbarRule === 'supply-side'). If method is SUPPLY_SIDE_TAP, print 'N/A — SUPPLY SIDE TAP (NEC 705.11)'; for load-side print 'PASS — 120% RULE' or the actual remedy label; never emit 'REQUIRES REVIEW' into the deliverable.

**Verifier notes:** Verified on PV-0.png and HTML lines 1240-1243: cover prints 'NEC 705.12(B) | REQUIRES REVIEW' one row under 'INTERCONNECTION | Supply Side Tap — NEC 705.12(A)'. Math recomputed: 200A bus x 1.2 - 200A main = 40A allowed vs 100A backfeed, so rulePass=false fires as described. The 120% rule genuinely does not govern a supply-side tap (NEC 705.11/705.12(A)), and the package contradicts itself: PV-4A (HTML line 1919) says 'NEC 705.12 Busbar Rule — N/A (Supply-Side Tap)' while another sheet (HTML line 2238) says 'FAIL — UPGRADE PANEL' — same root-cause family, worth fixing together. Reference set never prints unresolved QA flags, only affirmative compliance citations. P0 stands: an explicit unresolved-compliance statement on page 1 of an AHJ submittal is plausible grounds for intake rejection and directly contradicts the stated interconnection.

_Reported by 3 agent(s)._

### P0-3 [E-1] Drawn interconnection is non-compliant and contradicts the BOM: 'supply-side tap' landed on the load-side main bus through a 100A NON-FUSED disconnect with no OCPD anywhere

**Description:** E-1 labels the connection 'SUPPLY SIDE TAP — NEC 705.11' but draws the tap on the MAIN BUS inside the MSP, downstream of the 200A main breaker — a load-side connection, and exactly the configuration the package's own calc computes as failing the 120% rule. Worse, the only device shown is a '100A NON-FUSED' disconnect and 'Backfed Breaker: N/A', so the PV feeder has no overcurrent protection at all (NEC 705.11(C)/240 violation as drawn). Meanwhile SCHED's BOM specifies entirely different hardware for the same function: a 200A FUSIBLE Eaton disconnect with 2x 125A Class RK5 fuses. Two sheets specify different primary disconnects and the drawn set has zero PV OCPD.

**Evidence:** E-1.png MSP block: 'TAP' tied to MAIN BUS below the 200A MAIN (HTML 3321); '100A NON-FUSED / NEC 690.14' (HTML 3318); 'AC OCPD (125%) 100 A' with no device containing it (HTML 3402-3403, 3422-3423); SCHED BOM items 9-10 '200A Fusible AC Disconnect (fuse: 125A)' Eaton DPF224RB + LLNRK125SP (HTML 2813).

**Root cause:** lib/sld-professional-renderer.ts:1433-1442 (renderMSPSupply draws the 'supply-side tap' onto MAIN BUS at line 1439, with the 200A MAIN breaker at lines 1455-1458 sitting between that bus and the utility exit — so the tap is topologically load-side); lib/sld-professional-renderer.ts:1310 (renderDisconnect hardcodes '${ocpd}A NON-FUSED' for all interconnection types), diverging from lib/bom-engine-v4.ts:828/857-871 which correctly selects a FUSED Eaton DPF224RB + 2x LLNRK125SP for supply-side.

**Fix sketch:** In renderMSPSupply, route the PV feeder to the service-entrance conductors between the meter and the MAIN breaker (draw the tap point on the service conductor segment outside/upstream of the main breaker, not on MAIN BUS). In renderDisconnect, accept the interconnection type and, when supply-side, render 'FUSED (2x {fuseA}A RK5)' with fuse/enclosure amps computed by the same sizing function bom-engine-v4 uses (extract its acRequiredAmps→nextFuse→nextEnclosure logic into a shared helper, and unify the DC-kW vs AC-kW basis) so E-1, the calc table, and SCHED cannot diverge.

**Verifier notes:** Every element of the finding verified against the raw HTML SVG. (1) Tap geometry: MSP rect x=1180 w=160; bus y=427 spans x=1188-1332; TAP at x=1208 connects down to the bus; 200A MAIN breaker at x=1310-1330 on the same bus with the service exit to its right — the labeled 'SUPPLY SIDE TAP — NEC 705.11' is drawn load-side of the main OCPD. (2) Recomputed 120% rule: 200*1.2-200 = 40A max load-side backfeed vs required 100A PV OCPD (77A*1.25=96.3→100A per the sheet's own calc table) — the drawn configuration fails NEC 705.12; only a genuine supply-side landing works. (3) No PV OCPD as drawn: '100A NON-FUSED' disconnect + 'Backfed Breaker: N/A — Tap Connection'; only 20A branch breakers exist, which do not protect the 77A combined feeder; NEC 705.11 requires the supply-side OCPD (normally the fused disconnect). (4) BOM contradiction confirmed at HTML line 2813: Eaton DPF224RB '200A Fusible AC Disconnect — NEC 705.11 supply-side (fuse: 125A)' + 2x Littelfuse LLNRK125SP, vs the drawn 100A non-fused. Bonus divergence found: BOM sizes from DC kW (21.2kW→110A→125A fuse/200A enclosure) while E-1's calc table sizes from AC kW (18.497kW→100A) — independent engines with different bases. The generated sheet provably comes from sld-professional-renderer.ts (MSP symbol 160x180, busY=by2+38, tapX=bx+28 all match exactly; the near-duplicate in lib/permit/sections/electricalPages.ts:740 uses 96x120 and has no supply-side variant). Severity re-assessed and kept at P0: self-contradictory, code-violating interconnection on the most-reviewed sheet, guaranteed plan-check rejection.

_Reported by 5 agent(s)._

### P0-4 [SET-WIDE (PV-0, PV-2, PV-2B, PV-3, SCHED, E-1, APP-A)] Equipment pairing electrically impossible: SunPower Maxeon 3 400W (Voc 75.6V) on Enphase IQ8A (max DC input 58-60V)

**Description:** Every sheet pairs the Maxeon 3 400W — a 104-cell IBC module with Voc 75.6V / Vmp ~65.8V — with the Enphase IQ8A, whose max DC input is ~58V (the package itself prints 60V) and whose compatibility list covers only 54/60/66/72-cell modules. SCHED prints the contradiction on one sheet (Voc 75.6V two inches above 'Max DC V 60V'), and APP-A's own NEC 690.7 table computes 94.5V corrected max — 157% of the printed limit. Every microinverter would be over-voltage on day one: a UL-listing/NEC 110.3(B) violation, equipment damage in the field, and an instant plan-check rejection. The AC math (53 x 349VA = 18.497 kW) confirms IQ8A was genuinely used for sizing, so this is the design of record. VAL-1 nevertheless stamps 'Module Data Verified — PASS'.

**Evidence:** SCHED.png Solar Modules 'Voc 75.6V' vs Inverters 'Max DC V 60V' (HTML 2748, 2771); APP-A 'Voc x1.25 = 94.5 V max' (HTML 2907); PV-0.png header/System Summary; PV-2/PV-2B/PV-3 title blocks 'MODULE SunPower Maxeon 3 400W / INVERTER Enphase IQ8A'; manufacturer datasheets SPR-MAX3-400 Voc 75.6V vs IQ8A 58V.

**Root cause:** lib/system/panelCompatibilityGate.ts:318 (micro topology returns 'compatible' for any panel, no Voc check) + lib/computed-system.ts:951/1022 (NEC 690.7 voltagePass only runs for isString; micro path has no module-Voc vs inverterMaxDcV check) + lib/permit/sections/validationPage.ts:87 (VAL-1 'Module Data Verified' PASS = wattage > 0 only). Maxeon 3 (voc:75.6) added at lib/equipment-db.ts:321-338 as the gate's default low-Isc swap target; IQ8A maxDcVoltage:60 at lib/equipment-db.ts:1914-1920.

**Fix sketch:** Add a per-module voltage gate for micro topology, mirroring optimizer-binding.ts:141: in panelCompatibilityGate.ts's micro short-circuit and in computed-system.ts's micro path, require vocCorrected × modulesPerDevice ≤ inverterMaxDcV (and Vmp within MPPT window), emitting a NEC_690_7_VOLTAGE error / 'incompatible' status so auto-heal never suggests a >60V-Voc panel for micro brands. Have VAL-1's 'Module Data Verified' row surface that check's result instead of wattage > 0. Also correct IQ8A maxDcVoltage from 60 to the datasheet 58V.

**Verifier notes:** Verified in HTML and on SCHED.png: SCHED prints module Voc 75.6V (line 2748) two rows above IQ8A 'Max DC V 60V' (2771), BOM shows 53 × IQ8A-72-M-US; APP-A prints Voc 75.6V, '×1.25 = 94.5 V max' (2907), and 'Max DC Voltage 60 V' (2926) on the same sheet — 157.5% of the printed limit, internally contradictory regardless of external datasheets. Math recomputed: 75.6×1.25=94.5; 53×349VA=18,497; 53×400W=21.20kW — IQ8A genuinely drove sizing, so it is the design of record. Real-world specs agree (SPR-MAX3-400 Voc 75.6V high-voltage IBC; IQ8A datasheet max DC input 58V; only IQ7X-class micros supported these modules). VAL-1 'Module Data Verified — PASS' is wattage>0 only (validationPage.ts:85-88). Every one of the 53 micros over-voltage at STC, worse cold — NEC 110.3(B)/UL listing violation and instant plan-check rejection. P0 stands.

_Reported by 9 agent(s)._

### P0-5 [SET-WIDE (PV-0, PV-4B, PV-4C, SCHED, VAL-1)] Content overflows the fixed page height and is silently clipped on at least five sheets — losing the sheet index, construction notes, the failing 120% busbar analysis, BOM rows, and audit tables

**Description:** Rendered sheets end mid-content at the page border. PV-0: 7 sheet-index rows, the ENTIRE Construction Notes section (4 blocks, 19 notes), and the vicinity map's pin label, address strip and north arrow never print. PV-4B: the 'Load Calculations — NEC 220.82' table ending in the red '120% Busbar Rule — FAIL — UPGRADE PANEL' badge, the Grounding & Bonding Detail, and the page conclusion are all clipped — the set literally hides its own failing interconnection analysis. PV-4C: the page conclusion is cut mid-sentence and the Structural Rules Check + Auto-Resolutions tables never render. SCHED: BOM rows 20-22, totals and summary are cut. VAL-1: ~55 rows of decision/state records fall off the page. Printed/PDF output silently loses exactly the content reviewers need.

**Evidence:** PV-0.png index ends at PV-4C row on the border (HTML 1087-1172, 1328-1358 prove clipped content); PV-4B HTML 2225-2258 ('FAIL — UPGRADE PANEL' at 2238) absent from PV-4B.png; PV-4C.png truncated '...revision may be' vs HTML 2529-2570; sched_bottom.png row 20 sliced; VAL-1 HTML 3938-4160 not rendered.

**Root cause:** repo/lib/permit/generatePermit.ts:878-887 — .page has fixed height:11in (line 880) with overflow:hidden (line 887), plus .page-body overflow:hidden (line 905); section generators (lib/permit/sections/coverSheet.ts, compliancePages.ts, structuralPages.ts, validationPage.ts) emit unbounded content with no height budgeting or pagination.

**Fix sketch:** Add a pagination pass: give each sheet a content-height budget (11in minus margins/title strip) and have long-table sections (sheet index + construction notes on PV-0, load-calc/grounding on PV-4B, rules/auto-resolution tables on PV-4C, BOM rows, VAL-1 decision records) estimate per-row heights and split overflow onto auto-generated continuation sheets (e.g. VAL-1.2 'SHEET 15A OF 16', repeating the table header and title block). As a stopgap, at minimum reorder critical content (120% busbar FAIL verdict, sheet index) above the fold and emit a generation-time warning when a sheet's estimated content exceeds the page budget instead of clipping silently.

**Verifier notes:** All 5 sheets verified against PNG + raw HTML. PV-0: PNG index stops at PV-4C row; HTML 1087-1135 has 16 rows (7 lost: PV-5, SCHED, APP-A, CERT, PE-1, E-1, APP-CAD) and the entire Construction Notes section (HTML 1140-1172, 4 blocks) never renders. PV-4B: PNG ends mid NEC-references table; HTML 2220-2257 (Load Calculations NEC 220.82 with row 9 '120% Busbar Rule — FAIL — UPGRADE PANEL' at line 2238, red FAIL badge at 2256) and Grounding & Bonding Detail (2259+) are absent from the PNG — the printed set hides its own failing interconnection analysis while E-1/PV-0 claim a 100A backfeed supply-side tap. PV-4C: PNG cut mid-sentence in page conclusion; Structural Rules Check + Auto-Resolutions tables (HTML 2528-2570, same .page div ending 2571) never render. SCHED: BOM (HTML line 2813) has 22 rows + TOTAL LINE ITEMS + SUMMARY; sched_bottom.png shows row 20 (Rapid Shutdown Label) sliced at the border, rows 20-22 and totals lost. VAL-1: PNG ends at decision:inverter_selection (HTML 3934); HTML 3937-4160 continues with ~13 decision rows plus Engineering State Invalidation (state registry, generation hash) unrendered. Finding's suspected files partially wrong on names (no equipment-schedule.ts; sections live under lib/permit/sections/) but the mechanism is exactly right. P0 stands: silent print-time loss of a failing code analysis in a permit deliverable.

_Reported by 7 agent(s)._

### P0-6 [SET-WIDE (SCHED, VAL-1, PV-2B, E-1, PV-5)] String-inverter boilerplate applied to a microinverter job: wire sizing justifies a fictional DC string circuit while the real AC circuits get no justification

**Description:** SCHED's 'Wire Sizing Justification — NEC 690.8 & 310.15' contains exactly one circuit: 'DC 1-1, #10 AWG USE-2, 50 ft' — a DC source circuit that does not exist in an Enphase micro design (the modules table likewise shows all 53 modules as 'String 1-1', which in series would be ~4,000 VDC). The circuits that DO exist — six/four 20A AC branches and the ~77A feeder — get zero conductor justification anywhere, yet the section carries a 'VERIFIED' badge. The justification's own ampacity cells are wrong too: '30A (#10), 40A (#8)' are 60°C values printed under a 90°C header (Table 310.16 90°C: 40A/55A). The same template rot appears set-wide: VAL-1 reports '53 panels | 1 string(s)' with topology 'STRING', PV-2B's title block says 'ARRAY GEOMETRY & STRING LAYOUT' for an AC branch color map, E-1's legend defines an unused DC-conductor linetype and SCHED buys DC warning labels for nonexistent DC circuits, and PV-5's inspection hold point demands 'Voc/Isc verification' that cannot be performed on a micro system.

**Evidence:** SCHED.png wire-sizing table single 'DC 1-1' row + '30A (#10), 40A (#8)' under 'Ampacity (90°C)' (HTML 2780-2792); modules table '1-1 ... 53 ... #10 AWG ... 50' (HTML 2744-2752); VAL-1 HTML 3766, 3967; PV-2B title block vs header (HTML 1676, 1688); E-1 legend (HTML 3359-3360); SCHED item 18 'DC Conductor Label Set, stringCount x 2'; PV-5 hold point 4 (HTML 2671).

**Root cause:** lib/permit/sections/structuralPages.ts:1183-1199 (DC-only wire-sizing table iterating inv.strings with no inv.type==='micro' branch; line 1194 hardcodes "30A (#10), 40A (#8)" — 60°C values — under the 90°C header; line 1217 hardcodes the VERIFIED badge; lines 1139-1154 label micro modules as "String N-N"). Same template rot: validationPage.ts:127, arrayPages.ts:462, compliancePages.ts:306, bom-engine-v4.ts:1123-1127, sld-professional-renderer.ts:2305.

**Fix sketch:** In pageEquipmentSchedule, branch on inverter topology: for micro systems replace the DC source-circuit table with an AC branch-circuit justification (per-branch micro count × max continuous output current × 1.25 vs Q-cable/#10 THWN-2 ampacity with rooftop derates, plus the aggregate feeder vs its OCPD per 705.12) and retitle the modules table column "Branch". Fix the ampacity constants to Table 310.16 90°C (#10=40A, #8=55A) with the derated value computed, not asserted. Thread the same topology flag into validationPage ("N branch(es)"), the PV-2B sheet title, the PV-5 hold point (swap Voc/Isc for per-module rapid-shutdown/production verification), the BOM DC-label item, and drop the VERIFIED badge unless the computation actually ran.

**Verifier notes:** All cited evidence verified in the generated HTML: single "DC 1-1 / #10 AWG USE-2 / 50 ft" row in a micro job (inverter table itself says "Microinverter Enphase IQ8A"; BOM has 53 micros and an AC-only wiring stage), "30A (#10), 40A (#8)" printed under an "Ampacity (90°C)" header (correct 90°C values are 40A/55A), modules table "String 1-1" ×53 (75.6V Voc × 53 ≈ 4,007 VDC if series), VAL-1 "53 panels | 1 string(s)" PASS + sld_metadata "selected: STRING", PV-2B title "ARRAY GEOMETRY & STRING LAYOUT", E-1 unused DC-conductor legend entry, BOM item 18 "DC Conductor Label Set, stringCount × 2", PV-5 hold point 4 "Voc/Isc verification". Real AC branch/feeder circuits receive no conductor justification anywhere in the SCHED wire-sizing section despite the hardcoded VERIFIED badge. Finder's suspected files (electricalPages.ts/equipment-schedule.ts) were wrong — the schedule lives in structuralPages.ts — but the defect is exactly as described. E-1 legend sub-claim is the weakest (cosmetic unused linetype) but real. P0 stands: the package is internally contradictory on topology, which is a plan-check rejection risk.

_Reported by 13 agent(s)._

### P0-7 [PV-1] Equipment legend is wrong-project boilerplate: phantom ESS equipment (System Controller, Backup Load Panel, 'ESS') plus factually wrong data (controller model = the microinverter; 'UTILITY NUMBER: Bidirectional Net Meter')

**Description:** The legend lists '(N) ENPHASE SYSTEM CONTROLLER' and '(N) BACKUP LOAD PANEL — CRITICAL LOADS SUB-PANEL', and the AC-disconnect box says 'EXTERIOR WALL (ESS)' — on a project with NO battery (grep of the full package finds zero battery equipment; the label schedule prints 'Battery Storage: NONE' and greys out L-7/L-8). These items appear nowhere else in the set — not on E-1, not in any schedule — a classic site-plan-vs-SLD mismatch carried over from the battery-equipped reference template. Compounding it, the controller's model line reads 'ENPHASE IQ8A' (the microinverter, not a controller — a real controller is IQ System Controller 2/3), and the 'UTILITY NUMBER' field contains a meter TYPE ('Bidirectional Net Meter') where the reference prints an actual meter number (330840107). Wrong equipment data on a permit drawing; a crew would rough-in a subpanel that was never sold.

**Evidence:** HTML 1452 ('(N) ENPHASE SYSTEM CONTROLLER' + desc 'ENPHASE IQ8A'), 1463 ('BACKUP LOAD PANEL'), 1492-1493 ('EXTERIOR WALL (ESS)', 'UTILITY NUMBER: Bidirectional Net Meter'); grep: 'SYSTEM CONTROLLER' and 'BACKUP LOAD' occur only here; label-schedule 'Battery Storage: NONE'; ref-p4-PV1-site.png shows these items paired with an actual IQBATTERY-5P and real utility number.

**Root cause:** lib/permit/sections/sitePlan.ts:47-58 (equipItems: SYSTEM CONTROLLER and BACKUP LOAD PANEL at lines 50-51 are unconditional despite hasBatt being computed at line 43; controller desc = `${invMfr} ${invModel}` → prints the microinverter 'ENPHASE IQ8A' as the controller model); sitePlan.ts:538 (hardcoded 'EXTERIOR WALL (ESS)'); sitePlan.ts:539 + lib/system-state.ts:280 (utilityMeter defaults to the type string 'Bidirectional Net Meter' and is printed into the 'UTILITY NUMBER:' field)

**Fix sketch:** Gate the '(N) SYSTEM CONTROLLER' and '(N) BACKUP LOAD PANEL' entries behind hasBatt (spread them into the same `...(hasBatt ? [...] : [])` block as the battery items at lines 54-57), and when shown, describe the controller as an actual controller (e.g. 'IQ SYSTEM CONTROLLER 3'), not invModel. Make line 538 conditional: `EXTERIOR WALL${hasBatt ? ' (ESS)' : ''}`. For line 539, print a real meter number field (add e.g. project.utilityMeterNumber, falling back to '—') instead of project.utilityMeter, whose system-state default is a meter TYPE.

**Verifier notes:** Every claim checks out against the raw HTML and the repo. Generated package: line 1452 '(N) ENPHASE SYSTEM CONTROLLER' with desc 'ENPHASE IQ8A' (IQ8A is the microinverter, not a controller), line 1463 '(N) BACKUP LOAD PANEL / CRITICAL LOADS SUB-PANEL', lines 1492-1493 'EXTERIOR WALL (ESS)' + 'UTILITY NUMBER: Bidirectional Net Meter'. The project has no battery: the label schedule prints 'Battery Storage: NONE' (HTML lines 3465-3466) and battery text appears nowhere else except the greyed label rows (line 2689). The reference set (ref-text.txt) is a genuine battery project — line 188 '(N) 2 - ENPHASE IQBATTERY-5P-1P-NA BATTERY' — so its System Controller / Backup Load Panel / '(ESS)' / real 'UTILITY NUMBER: 330840107' (line 404) are legitimate there; the generator carried them over unconditionally. Internally the set is self-contradictory (PV-1 shows ESS equipment; the label schedule says NONE), which is exactly the kind of sheet-to-sheet mismatch plan checkers reject, and a crew following PV-1 would rough in a critical-loads subpanel that was never sold. P0 stands: factually wrong equipment and wrong model data on a permit drawing, plus a type string in a number field.

_Reported by 9 agent(s)._

### P0-8 [PV-2 / PV-2B / PV-5] Fire-access non-compliance: 18-inch setbacks drawn where the 36-inch default governs (48% coverage), claimed 3-foot pathways never drawn anywhere, notes hedge 'confirm exception w/ AHJ', and PV-5 inverts the code requirement

**Description:** PV-2 itself states ROOF COVERED BY ARRAY = 48.0%, above the 33% threshold for the reduced ridge setback, yet every ridge/hip band on PV-2 and PV-2B is drawn and labeled 1'-6". Notes and callouts claim 'MAINTAIN 3'-0" ACCESS PATHWAY — IFC §1204.2' and even assert 'HIP CLEARANCES PROVIDE EAVE-TO-RIDGE ROUTES' — false on its face, since the hip clearances are 18", half the required pathway width; no 36" pathway is drawn, hatched, dimensioned, or legended on any sheet (a known unshipped placementEngine item). The code basis is self-contradictory and unresolved on an issued-for-permit set ('18" per AHJ amendment — 36" default; confirm exception w/ AHJ'), PV-2B contradicts PV-2 on eave setbacks ('modules may extend to eave' vs 'FIRE SETBACK 1.5' EDGES'), one PV-2B module encroaches its own 18" hip setback (~12.5" from the hip), the setback hatches on PV-2B carry no dimension labels, and PV-5's general note states the requirement BACKWARDS ('3 ft from ridge, 18" from edges' — code requires 18-36" at the ridge and 36" pathways at edges). This is a life-safety layout failure that forces a re-layout, not a label fix.

**Evidence:** PV-2.png 'ROOF COVERED BY ARRAY 48.0%' + 11x "1'-6\" FIRE SETBACK" labels, zero 3-ft hatches (SVG text audit, HTML 1619); General Note 2 + Callout 2 text; HTML 1756-1758 ('36" default; confirm exception w/ AHJ', 'Modules may extend to eave') vs HTML 1629 ('1.5' EDGES'); PV-2B corner-to-hip distance 12.77px ≈ 12.5"; HTML 2655 (PV-5 note 5 '3 ft from ridge, 18" from edges and valleys'); PV-1 also shows no pathway/ridge-setback annotation.

**Root cause:** lib/drafting/templates/roof.ts:140-143 (18" default band drawn on every ridge/hip; pathwayFt computed but used ONLY in note text at line 662, never rendered as geometry) + lib/jurisdictions/necVersions.ts:48,59,72,... (every state record hardcodes ridgeSetbackInches:18 with no 33%-coverage condition, piped in via app/api/engineering/permit/route.ts:533) + lib/permit/sections/arrayPages.ts:428-430 (static hedged/contradictory note strings) + lib/permit/sections/compliancePages.ts:291 (static wrong PV-5 note with stale §605.11 cite). calcFireSetbacks in lib/engineering/fire-setbacks.ts is only called by the legacy app/api/engineering/plan-set/route.ts:390, not the active generatePermit pipeline.

**Fix sketch:** Add a single fire-access resolver: ridge/hip setback = 36" unless (array coverage <= 33% per IRC R324.6.2.2 / IFC 2021 1204.2.1.3 exception) OR a documented AHJ amendment flag in the AHJ record — roof.ts already computes coverage % for its table, so feed that in. Render the 36" access pathway as actual hatched/dimensioned geometry in roof.ts using the already-computed pathwayFt (and make placement respect it), then drive the PV-2B fire-setback bullets (arrayPages.ts:428-430) and PV-5 note 5 (compliancePages.ts:291) from the same resolved values with a consistent IFC 2021 §1204.2 cite. Remove the "confirm exception w/ AHJ" hedge and the "modules may extend to eave" vs "1.5' EDGES" contradiction by emitting one canonical setback statement set-wide.

**Verifier notes:** Verified against the sheet PNGs and raw HTML, recomputing where possible. (1) PV-2 array table states ROOF COVERED BY ARRAY 48.0% while every ridge/hip band is drawn and labeled 1'-6" and the legend defines only a 1'-6" FIRE SETBACK — 48% > 33%, so the IRC/IFC 2021 reduced-ridge-setback exception cannot apply and the 36" default governs; no documented amendment exists (the set itself says "confirm exception w/ AHJ", HTML 1756). (2) No 36" access pathway is drawn, hatched, dimensioned, or legended on PV-1, PV-2, or PV-2B; pathways exist only as text ("MAINTAIN 3'-0\" ACCESS PATHWAY"), and PV-2 General Note 2's claim that "HIP CLEARANCES PROVIDE EAVE-TO-RIDGE ROUTES" is false on its face since hip clearances are 18", half the required width. (3) PV-2B fire-setback notes are self-contradictory for an issued-for-permit set (18" per AHJ amendment vs 36" default, confirm exception) and contradict PV-2's "FIRE SETBACK 1.5' EDGES" with "Modules may extend to eave". (4) PV-5 note 5 verified at HTML 2655: "Maintain IFC §605.11 fire code setbacks: 3 ft from ridge, 18\" from edges and valleys" — a garbled/invented requirement (no model code requires 18" from edges; edges require 36" pathways) that also contradicts the drawn 18" ridge bands and cites stale 2015-era §605.11 while the title block and other sheets cite IFC 2021 §1204.2. One sub-claim I could not independently verify: the ~12.5" module-to-hip encroachment on PV-2B (pixel-level measurement beyond PNG resolution) — plausible but not load-bearing for the verdict. Severity stays P0: this is a fire-access life-safety layout defect a plan reviewer will reject, and fixing it at 48% coverage forces a re-layout (module count/coverage must drop or pathways must be carved), not a label edit.

_Reported by 12 agent(s)._

### P0-9 [PV-2B] Branch-circuit wiring unbuildable as drawn: branches cross roof planes over hips through setback zones, no homeruns reach the JB, JB sits in the hip setback, and the noted single 3/4" EMT for 6 branches fails NEC either way

**Description:** Branch 5 daisy-chains Plane 1 (N) modules straight across the NW hip into Plane 3 (W); Branch 6 jumps Plane 4 (E) across the SE hip into Plane 2 (S) — Enphase trunk cable cannot span a hip in free air, no transition JB/conduit is shown, and the wires are painted through the hatched fire-setback keep-outs (this is the exact planMicroBranches defect fixed 07-03; v47379 predates it). None of the six chains connects to the junction box — the only routing is a 19px dashed stub annotated 'ROUTE FIELD-VERIFIED' — and the JB itself is drawn straddling the hip line inside the hatched setback corridor. The chains also double back over already-wired modules on both E/W planes. Finally, the callout '(N) JB — 6 AC BRANCH CIRCUITS / 3/4" EMT' is a code violation on its face: 12 CCC triggers a 50% derate (NEC 310.15(C)(1)) — #12 fails ampacity for the 20A OCPD, #10 passes ampacity but fails 3/4" EMT fill (12x0.0211 = 0.253 in2 > 0.213 in2); 1" EMT or split runs are required. The branch legend also shows per-module 'Wp 400W' with no branch VA/amps/OCPD to check.

**Evidence:** SVG: teal line (320.9,278.8)->(229.8,454.2) crosses hip (122,244)-(315.5,457.6); magenta (924.1,505.9)->(798.6,612.4) crosses hip (958,654)-(757.5,457.6); branch endpoints never reach JB at (946,620); JB rect on hip within hatch (pv2b-jb.png); doubled segments at x~160 and x~922; HTML 1690 ('(N) JB — 6 AC BRANCH CIRCUITS / ¾" EMT — ROUTE FIELD-VERIFIED'); NEC Ch.9 Tables 1/4/5 + 310.15(C)(1) recomputation; reference runs 3 circuits in its 3/4" EMT.

**Root cause:** repo/lib/drafting/templates/roof.ts:428-471 (branch chain = greedy nearest-neighbor over all modules of a branch, no plane grouping or setback awareness; v47379 render is solid diagonals, predating even the dashed mitigation) + roof.ts:477-485 (JB anchored at panel bbox max + fixed offset with no hip/setback check; '¾" EMT' hardcoded at :484 regardless of branchGroups.length; only routing drawn is a 16px dashed stub at :480 — no homeruns); cross-plane branch membership from lib/permit/utils/branching.ts planMicroBranches (plane-grouping fix on dev postdates the package).

**Fix sketch:** In roof.ts, build each branch chain per planeId (never chain across planes) and route inter-plane/homerun segments as dashed Manhattan runs snapped to eave corridors that avoid the setback hatch polygons; place the JB by projecting the panel-bbox corner to the nearest eave point outside all setback corridors and draw dashed homeruns from each chain head to it. Replace the hardcoded ¾" EMT string with a computed callout: derive CCC = 2 x branch count, apply 310.15(C)(1) adjustment + Ch.9 Table 4 fill to pick conduit size (<=4 circuits -> ¾", 5-6 -> 1" or two ¾" runs), and reconcile with the sheet's own 'IQ Combiner accepts 4 + AC subpanel' note (i.e., show two runs matching E-1).

**Verifier notes:** All claims independently verified. Visual: PV-2B.png + pv2b-plane3.png show solid teal B5 line crossing the NW hip through the red-hatched setback from Plane 1 (N) into Plane 3 (W); pv2b-jb.png shows magenta B6 crossing the SE hip and the JB square drawn inside the hatched setback corridor; no branch chain connects to the JB (code proves only a dashed stub is drawn). Text: HTML line 1690 exactly matches '(N) JB — 6 AC BRANCH CIRCUITS / ¾" EMT — ROUTE FIELD-VERIFIED', and it contradicts the sheet's own note 'IQ Combiner accepts 4; remaining branches land on AC subpanel, see E-1'. NEC recomputation holds: 6 circuits = 12 CCC -> 50% adjustment (310.15(C)(1)); #12 THHN 30x0.5=15A < 18.1A (14.5A x 1.25) and < 20A OCPD -> fails; #10 passes ampacity (20A) but fill 12x0.0211=0.2532 in2 > 0.213 in2 (¾" EMT 40%) -> fails; 1" EMT (0.346 in2) needed. Reference set demonstrably does it right: ref-text.txt lines 554-563/772-774 show 3 circuits per ¾" EMT. P0 stands: a facially NEC-violating conduit callout plus branch wiring drawn over hips through fire setbacks on the flagship circuit sheet is plan-check rejection material.

_Reported by 12 agent(s)._

### P0-10 [PV-3 / APP-A] Racking system contradicts itself across the set: Roof Tech RT-MINI on PV-3 vs IronRidge XR100/FlashFoot2 on APP-A (and the cover title), with conflicting lag diameters and a fictitious RT-MINI 'rail' + L-foot assembly

**Description:** PV-3 specifies 'MOUNTING SYS: ROOF TECH RT-MINI' with a 3/8" x 3" lag; APP-A's Racking System table in the same package specifies 'IronRidge XR100' with 'FlashFoot2 with L-Foot' and a 5/16" x 3" lag; the cover even titles the whole set 'PHOTOVOLTAIC IRONRIDGE XR100 SYSTEM'; PE-1 hedges with generic 'Lag bolt w/ flashing'. Two manufacturers' systems and two lag diameters in one permit set — the installer cannot order material and neither ICC-ES report covers the hybrid drawn. Compounding it, RT-MINI is a rail-less butyl-flashed attachment, yet PV-3 labels it a 'MOUNTING RAIL' and pairs it with a separate 'STANDOFF / L-FOOT — 3/8" SS LAG' and metal flashing (an assembly that does not exist in Roof Tech's line), and PV-2's callout 5 hardcodes 'L-foot @ 48" O.C. into rafters' regardless of racking selection while the roof table says TRUSS 2x6.

**Evidence:** HTML 1815 (RT-MINI as system and rail) vs HTML 2939-2947 (APP-A: IronRidge XR100 / FlashFoot2 / 5/16" x 3") and 3163 (PE-1); lib/drafting/sheetComposition.ts:590 hardcodes the L-foot string; PV-2 table 'TRUSS 2x6 @ 24" O.C.'; reference set is internally coherent (Unirac NXT Umount + Stronghold + 2X12-14 screws).

**Root cause:** lib/permit/utils/canonical.ts:19-23 (MOUNT_SYSTEM_MAP hardcodes roof→'IronRidge XR100', overriding the selected racking) + lib/permit/sections/compliancePages.ts:446-452 (APP-A table rows — FlashFoot2, 5/16"×3", rail profile — are hardcoded IronRidge literals) + lib/drafting/sheetComposition.ts:391,576,590,595 (PV-2/PV-3 use raw project.mountingSystem 'Roof Tech RT-MINI' with hardcoded 3/8" lag, 'MOUNTING RAIL' label, and 'L-foot @ 48" O.C. into rafters' string)

**Fix sketch:** Make racking single-source: resolve the selected racking record once from lib/mounting-hardware-db.ts (which already carries real per-system data: name, rail model, flashing kit, fastenerDiameterIn=0.3125, embedment) and thread it into the canonical model instead of MOUNT_SYSTEM_MAP's hardcoded 'IronRidge XR100'. Then render APP-A's Racking System table and PV-3's spec/callout strings (lag diameter, attachment name, rail vs pad labels) from that record, deleting the hardcoded '3/8" DIA × 3" MIN SS' / 'FlashFoot2 with L-Foot' / '5/16" × 3"' literals in sheetComposition.ts:576,595 and compliancePages.ts:450-451.

**Verifier notes:** Every cited HTML instance verified: cover 974/982/1022 = IRONRIDGE XR100; PV-2/PV-3 1616-1840 = ROOF TECH RT-MINI + 3/8" lag; APP-A 2938-2947 = IronRidge XR100 / FlashFoot2 / 5/16"×3". Two manufacturers and two lag diameters in one sealed set — plan-check rejection grade, and PV-4C/PE structural certification is for a system not drawn on PV-3, hence P0 stands. One finder overstatement corrected: RT-MINI is NOT rail-less (that's Roof Tech E Mount AIR); per the repo's own equipment-db.ts:2251-2266 RT-MINI is a flashed pad + L-foot + compatible rail, so the drawn assembly exists — the real defects there are labeling RT-MINI as the 'RAIL' and the 3/8" lag, which is wrong for BOTH candidate systems (repo DBs specify 5/16" for RT-MINI and FlashFoot2). Reference set is internally coherent (single Unirac system set-wide), confirming the professional standard.

_Reported by 5 agent(s)._

### P0-11 [PV-3] Attachment engineering self-contradictory: specified 3" lag cannot achieve the specified 2-1/2" embedment, detail bubbles 5/6/7 contradict both callout schedules, and PV-3 still mandates the 48"/2x6 configuration the package's own analysis auto-resolved to 36"/2x8

**Description:** Three permit-stopping contradictions on one sheet. (1) ATTACHMENT SPECS require 'LAG BOLT 3/8" x 3" MIN' AND 'EMBEDMENT 2-1/2" MIN INTO RAFTER' — but the sheet's own stack (L-foot + flashing + shingle + 5/8" OSB ≈ 1.1-1.3") leaves a 3" lag only ~1.8" of embedment; a crew buying the stated minimum hardware fails the stated minimum embedment (a 4" lag is required; the reference's 2X12-14 screw @ 1.75" is achievable). (2) In DETAIL 1/PV-3 bubbles 5/6/7 tag SHINGLE/SHEATHING/RAFTER, while both callout schedules define 5=RAFTER, 6=EMT CONDUIT, 7=BONDING JUMPER (NEC 690.43) — same tag, different component, and the promised conduit/bonding items are never drawn. (3) PV-3 directs 48" O.C. on 2x6 rafters everywhere, but PV-4C computes 145% utilization, says the framing 'REQUIRES REINFORCEMENT', and records auto-resolutions 'REDUCE_ATTACHMENT_SPACING 48->36' and '2x6->2x8 — RESOLVED' — in a table clipped off the page — so the printed drawings never incorporated the engine's own fix (and 36" isn't even a multiple of the 24" rafter spacing). Spec nits pile on: '316 S.S. THROUGHOUT' vs notes' 'OR HOT-DIP GALVANIZED', module '66" x 40" @ 45 LBS' vs the real 41.2"-wide 42-lb Maxeon 3, 'DESIGN CODE: ASCE 7-22 / NEC 690.43' (a bonding subsection cited as a design code), and the same 7-item schedule printed twice with diverging text.

**Evidence:** PV-3.png specs rows LAG BOLT/EMBEDMENT (red) + callout 3 '@ 2.5" embed' + detail 'SHEATHING (5/8" OSB)'; SVG bubble leaders 5->SHINGLE, 6->SHEATHING, 7->RAFTER vs schedule rows (HTML ~1819); HTML 1815 ('ATTACH: 48" O.C. MAX'), 2517 ('REQUIRES REINFORCEMENT'), 2556-2566 (auto-resolutions on clipped PV-4C region); hardware/weight/design-code strings HTML ~1834.

**Root cause:** repo/lib/drafting/sheetComposition.ts:576-577 (hardcoded 3" lag + 2-1/2" embedment as independent constants), sheetComposition.ts:394 (attachSpacing = raw input || 48, never the resolver output), lib/drafting/templates/roof.ts:1033-1051 vs roof.ts:1111-1119 and sheetComposition.ts:592-600 (three independent callout arrays: detail bubbles numbered i+1 over 7 physical layers vs two schedules defining 5/6/7 as RAFTER/EMT/BONDING), lib/structural-resolver.ts:128,300 (produces 48→36 + 2x6→2x8 resolutions that only render in the PV-4C tables at HTML 2550-2570, which are clipped off the printed page)

**Fix sketch:** Compute lag length from embedment + roof-stack thickness (embedment 2.5" + OSB 0.625 + shingle 0.25 + flashing/L-foot ≈ 0.4 → spec 3/8" x 4" min), and make one shared callout definition array that both the detail-bubble renderer (roof.ts zLayers loop) and both schedule renderers consume — draw the EMT/bonding items or drop them from the schedule. Pipe structural-resolver resolved values (attachmentSpacing, rafterSize) into getRoofData so PV-3/PV-1/PV-4A print 36"/2x8 (snapping resolved spacing to a multiple of rafter O.C., i.e. 24" here), and fix PV-4C page overflow so the Rules Check / Auto-Resolutions tables print.

**Verifier notes:** All three contradictions verified. (1) PV-3.png ATTACHMENT SPECS shows LAG BOLT 3/8" DIA x 3" MIN SS and EMBEDMENT 2-1/2" MIN INTO RAFTER (highlighted red), with the detail drawing SHEATHING (5/8" OSB) + SHINGLE + FLASHING + L-FOOT in the load path; math is impossible even through sheathing alone (2.375" < 2.5"). Note the reference (ref-p6-PV3) and PV-4C's own requirements text avoid this trap. (2) In DETAIL 1/PV-3 the bubbles are numbered by stack position (5=SHINGLE ROOF, 6=SHEATHING, 7=RAFTER) while both printed schedules define 5=RAFTER, 6=EMT CONDUIT, 7=BONDING JUMPER; conduit/bonding are never drawn; the two schedules also diverge (row 3: '3/8" STAINLESS LAG' vs '3/8" SS LAG @ 2.5" embed'). (3) HTML 1815/1834/3760/4113 all mandate 48" O.C. on 2x6 while PV-4C's narrative (line 2517, '145% REQUIRES REINFORCEMENT') and the Auto-Resolutions table (2556-2568: 48→36, 2x6→2x8 'RESOLVED') say otherwise — and the rendered PV-4C.png confirms those tables are clipped off the page (sheet ends mid-sentence at PAGE CONCLUSION), so the permit set as printed never shows or incorporates the fix. Bonus inconsistency observed: PV-4C rules-check table claims '2x6 utilization 73% PASS' while the same page says 145% — the sheet argues with itself. P0 stands: a plan checker sees a structural analysis demanding reinforcement while every drawing sheet specifies the unreinforced configuration, plus an unachievable embedment spec.

_Reported by 18 agent(s)._

### P0-12 [PV-4A / PV-4C] Structural results contradict themselves across (and within) sheets: rafter 73% PASS vs 89%/145% FAIL, attachment uplift 210/500 lbs vs 370/984 lbs, 'Snow per Attachment: 0 lbs' beside a 13.2 psf snow load, dead load 17.5 psf outside its own stated basis

**Description:** Two independent engines compute the same members and disagree everywhere. PV-4C's rafter card shows 89% bending and a red 'Governing Check: deflection — 145%' demanding reinforcement, while PV-4C's own (clipped) Structural Rules Check and PV-4A's compliance table print 'Rafter Bending OK — 73% — PASS'. PV-4C says uplift per attachment 210 lbs / capacity 500 lbs (SF 2.38); PV-4A says 370 lbs / 984 lbs for the same lag — and the 210 lb figure isn't even reproducible from the sheet's own 61.15 psf net pressure at 48" spacing (implies a 3.4 ft2 tributary; realistic tributary gives ~750-800 lbs, meaning the attachment likely FAILS). The snow card reports 'Snow per Attachment: 0 lbs' at 13.2 psf roof snow (physically impossible; ~45-170 lbs), and COMBINED ROOF DEAD LOAD 17.5 psf implies a 14.4 psf existing roof, outside the 'typically 8-12 PSF' range printed in the same cell. Structural pass/fail rows also sit inside the 'ELECTRICAL COMPLIANCE — NEC 2020' table where a structural reviewer is least likely to reconcile them. No AHJ accepts a set that certifies adequacy on one sheet and demands reinforcement on the next.

**Evidence:** PV-4C.png (89%, 0.868"/0.600", 'deflection — 145%', 210/500/2.38, snow 0 lbs, 17.5 PSF note); HTML 2395-2399 vs 2536-2544 ('73% <= 100% PASS' inside the same PV-4C block); PV-4A HTML 1941-1942, 1948-1949; recomputation 210/61.15 = 3.43 ft2 tributary vs ~13 ft2 at 48" O.C.; structuralPages.ts:687 prints snowAtt=0, :732 totalDL vs hardcoded note.

**Root cause:** lib/rules-engine.ts:227 (runStructuralCalc, engine #1 → 73%/370/984 on PV-4A + PV-4C rules table) vs lib/permit/generatePermit.ts:328-367 (runStructuralCalcV4 + mountLayout backfill, engine #2 → 89%/145%/210/500 on PV-4C cards); snow=0 from app/engineering/page.tsx:4689 mapping ml.downwardPerMountLbs, hardcoded 0 at lib/structural-engine-v4.ts:583; DL narrative hardcoded at lib/permit/sections/structuralPages.ts:732,738-739

**Fix sketch:** Single-source the structural results: run runStructuralCalcV4 once in generatePermit.ts and feed its mapped output into the rules engine (replace the independent runStructuralCalc call at rules-engine.ts:227 or inject resolvedStructural) so PV-4A rows and PV-4C cards print the same numbers. Fix snowLoadPerAttachment to panelSnowLoad × tributaryAreaPerMountFt2 (V4 exposes both) instead of downwardPerMountLbs=0, print V4's actual computed mount spacing (ml.mountSpacingIn) in the attachment card and Roof Attachment Requirements instead of the static 48", stop fabricating lagBoltCapacity as uplift×SF (use mount.upliftCapacityLbs directly), and derive the 'existing roof typically X PSF' prose from V4's roofDeadLoadPsf.

**Verifier notes:** Every sub-claim verified against the PNGs, generated HTML, and code. (1) PV-4C card: bending 89%, deflection 0.868"/0.600", red 'Governing Check: deflection — 145%' + 'REQUIRES REINFORCEMENT' interpretation; PV-4A HTML :1948 and the same rules table inside the PV-4C block (:2543) print 'Rafter Bending OK 73% PASS' — two unshared calculators (rules-engine's runStructuralCalc vs V4's overallUtilization=max(bending,shear,deflection)). (2) Attachment 210/500/2.38 vs 370/984: V4's calcMountLayout (structural-engine-v4.ts:567-580) silently tightens spacing to ~12" to force SF>=2.0 against a 500 lb mount-spec capacity (61.15 psf × ~3.4 ft² = 210 lbs), while the sheet's own detail text and PV-4A state 48" O.C. — at 48" the mount fails (~750-800 lbs > 500), so the finder's recomputation is correct; generatePermit.ts:365 additionally re-derives capacity as demand×SF (circular). (3) 'Snow per Attachment: 0 lbs' beside 13.2 psf roof snow is a genuine data bug, not a rounding artifact: page.tsx:4689 maps the hardcoded-zero downwardPerMountLbs into snowLoadPerAttachment. (4) Combined DL 17.5 implies 14.4 psf existing roof vs 'typically 8-12 PSF' hardcoded in the same cell (structuralPages.ts:732). (5) The structural PASS rows do sit under the 'ELECTRICAL COMPLIANCE — NEC 2020' header on PV-4A. P0 stands: the set simultaneously certifies adequacy and demands reinforcement for the same rafter, and the attachment design is only 'safe' under an undisclosed 12" spacing the drawings contradict — an AHJ plan reviewer rejects this, and it goes to the product's core selling point (permit-grade plansets).

_Reported by 12 agent(s)._

### P0-13 [SET-WIDE (PV-4A, PV-4C, PE-1, CERT, VAL-1)] Package certifies full compliance while its own analysis fails: '0 ERRORS' banner, CERT compliance letter, and 'ALL CHECKS PASSED 11/11' shipped alongside 'REQUIRES REINFORCEMENT' and 'STRUCTURAL REVIEW REQUIRED — DO NOT ISSUE' — all stamped ISSUED FOR PERMIT

**Description:** PE-1, titled 'LETTER OF STRUCTURAL COMPLIANCE', states the framing 'does not satisfy all code checks' and prints a red 'STRUCTURAL REVIEW REQUIRED — DO NOT ISSUE' box (deflection 145%); PV-4C repeats 'REQUIRES REINFORCEMENT per IBC 1607' with no reinforcement detail anywhere in the set. Yet PV-4A leads with a rules-engine telemetry banner '0 ERRORS / 0 WARNINGS / 0 AUTO-FIXED / 0 OVERRIDES' (false — PV-4C's clipped table records two auto-fixes) and concludes 'All calculations produce compliant results with no errors identified'; CERT says 'I hereby certify that the ... design complies with' IBC/IRC structural requirements; VAL-1's green banner claims 'ALL CHECKS PASSED — 11/11' with the structural row PASS; and every title block reads 'REV A — ISSUED FOR PERMIT — 2026-06-03'. The package simultaneously certifies compliance, admits failure, and ships a do-not-issue flag to the customer — guaranteed rejection and a credibility disaster next to the affirmatively-certifying Wyssling letter.

**Evidence:** PE-1.png red DO-NOT-ISSUE box + 'deflection — 145% (EXCEEDS LIMIT)' (HTML 3188-3212); HTML 2517, 2524 (REQUIRES REINFORCEMENT); PV-4A.png banner + HTML 1975 ('no errors identified'); CERT.png certification paragraph; VAL-1 HTML 3667, 3760-3762 (11/11, structural PASS); title blocks 'ISSUED FOR PERMIT — 2026-06-03'.

**Root cause:** lib/permit/sections/validationPage.ts:117 (roof structural check hardcoded 'PASS'); lib/permit/sections/certPages.ts:35 (unconditional CERT compliance paragraph, while the PE-1 builder in the same file at lines 486-514 computes _allPass and branches correctly); lib/rules-engine.ts:197-212 (no deflection rule + bending computed from different inputs than the structural module, so PV-4A counters show 0 errors); lib/permit/utils/titleBlock.ts:76 + sections/coverSheet.ts:468 (unconditional 'ISSUED FOR PERMIT' stamp)

**Fix sketch:** Compute one set-wide structural verdict (the same bending+deflection+lag _allPass logic certPages' PE-1 already uses) once in generatePermit.ts and thread it everywhere: validationPage's roof structural row status = that verdict; certPages' CERT paragraph branches on it exactly like PE-1 does; add a deflection rule to rules-engine (fed the structural module's actual util ratios) so PV-4A's counters and interpretation text reflect the failure; and when the verdict fails, replace 'ISSUED FOR PERMIT' with a 'NOT FOR CONSTRUCTION — STRUCTURAL HOLD' revision stamp in titleBlock/coverSheet.

**Verifier notes:** Every cited element verified verbatim in the shipped HTML and on PE-1.png: PE-1 red 'STRUCTURAL REVIEW REQUIRED — DO NOT ISSUE' box with deflection 0.868in vs 0.600in (145% EXCEEDS LIMIT) on a sheet titled Letter of Structural Compliance (HTML 3202/3208-3216); PV-4C 'REQUIRES REINFORCEMENT per IBC 1607' with no reinforcement detail in the set (2517); PV-4A '0 Errors/0 Warnings/0 Auto-Fixed/0 Overrides' + 'no errors identified' (1894-1903, 1975); CERT unconditional 'complies with IBC/IRC structural requirements' (3013-3020); VAL-1 green 'ALL CHECKS PASSED 11/11' with structural row PASS (3666, 3758-3762); 'ISSUED FOR PERMIT' 17x. Bonus contradiction: the rules table says rafter bending 73% while PE-1 says 89% bending/145% deflection — two disconnected calc paths. One finder sub-claim is wrong: the PV-4C rules table records zero auto-fixes (both rows PASS), so the '0 AUTO-FIXED is false' assertion fails — immaterial to the finding. Also note the deflection failure itself may be a default-assumption artifact (2x6@24" stick, 12ft span are modeled defaults; PE-1 itself says trusses frequently resolve it), but shipping self-contradictory certification either way is a P0 rejection/credibility defect vs the affirmatively-certifying Wyssling reference letter.

_Reported by 8 agent(s)._

### P0-14 [PE-1 / PV-4C] The structural math itself is wrong in both directions: L/240 misapplied to total load manufactures the false 145% 'failure', while the printed F'b (1521 psi) vs allowable moment (1246 ft-lb) inconsistency hides a real 116% bending question — with no lumber species/grade stated anywhere

**Description:** The governing 'failure' is an artifact: the engine computes deflection under full D+S (0.868") and compares to L/240 (0.600") — but code rafter limits are L/180 for snow/live and L/120 for total (recomputed: snow-only 0.37" vs 0.60" = 62% PASS; total 0.868" vs 0.80-1.20" = passes L/120, marginal L/180). Meanwhile the capacity side is unverifiable: PE-1 prints F'b (Adjusted) = 1521 psi, which for a 2x6 (S=7.5625 in3) gives 958 ft-lb allowable — making the 1114 ft-lb demand a 116% FAIL, not the 89% shown; the printed 1246 ft-lb only reproduces with the CF=1.3 size factor the displayed F'b omits (hardcoded 1150x1.15x1.15 in the generator, with Fb_base=1150 a generic fallback because the species matched no NDS key). No species, grade, or E is stated on the letter, the 'GOVERNING LOAD COMBINATION 0.9D + 1.0W (Uplift)' contradicts the governing gravity-deflection check two lines above it, and LRFD demand is compared against ASD lag capacity. A reviewing PE checking F'b x S concludes the numbers don't reconcile.

**Evidence:** PE-1.png ('F'B (ADJUSTED) 1521 psi', '1114.1 / 1246.0 lb-ft', '89%', 'deflection — 145%', 'GOVERNING COMBO 0.9D + 1.0W'); lib/structural-engine-v4.ts:507-511 (comment 'L/240 for total load', delta from total w) and :482-486 (Fb_prime includes CF_b=1.3) vs lib/permit/generatePermit.ts:357-359 (Fb_prime: 1150*1.15*1.15, no CF) and :310-311 (comment: species matched no NDS key, generic Fb used); 1521x7.5625/12 = 958 ft-lb.

**Root cause:** lib/permit/generatePermit.ts:357-359 (displayed Fb_prime hardcoded as 1150*1.15*1.15=1521, omitting CF_b=1.3 that the capacity calc includes at lib/structural-engine-v4.ts:482-486, so F'b×S=958 ft-lb contradicts the printed 1246 ft-lb capacity); lib/structural-engine-v4.ts:507-511 (deflection under full D+PV+S compared to L/240 — the strictest plaster-ceiling row of IBC Table 1604.3 — manufacturing the 145% DO-NOT-ISSUE failure; snow-only vs L/180 = 49% and total vs L/120 = 72% both pass); lib/permit/sections/certPages.ts:477 (static 'GOVERNING COMBO 0.9D + 1.0W (Uplift)' text contradicting the gravity-deflection governing check printed two rows above); certPages.ts:407 prints the hardcoded Fb_prime. Species/grade/E absent from the letter entirely.

**Fix sketch:** 1) Delete the hardcoded Fb chain at generatePermit.ts:357-359; have runStructuralCalcV4 return Fb_ref/CD/CF_b/Cr/Fb_prime/E in rafterAnalysis and map those into s.rafter, and print species+grade+E on PE-1/PV-4C. 2) In structural-engine-v4.ts:507-512, check snow-only deflection vs L/180 and total-load vs L/120 (IBC 1604.3 no-ceiling row, or the ceiling-specific row if known) and report whichever governs. 3) Make the certPages.ts governing-combo line dynamic: gravity D+S when bending/deflection governs, 0.9D+1.0W only when uplift governs.

**Verifier notes:** All math independently recomputed and matches the finder: demand 61.9 lb/ft × 12²/8 = 1114.2 ft-lb; δ = 5wL⁴/384EI = 0.868" (E=1.6e6, I=20.8); 144/240 = 0.600" → 145%; 1521×7.5625/12 = 958.5 ft-lb → true bending util 116%, vs printed 89% (capacity 1246 requires F'b=1977=1521×1.3). Verified on PE-1.png: '1521 psi', '1114.1 / 1246.0 lb-ft', '89%', '0.868 in (Δ_allow=0.600 in — 145%)', 'deflection — 145% (EXCEEDS LIMIT)', '0.9D + 1.0W (Uplift)', red DO-NOT-ISSUE box, and no species/grade/E anywhere ('2x6 Lumber' only). One correction to the finding: Fb_base 1150 is NOT the generic fallback — species normalization at generatePermit.ts:313-319 yields 'Douglas Fir-Larch' and NDS_FB DF-L 2x6 = 1150, so 1150 is a table hit; the display bug is purely the omitted CF_b. The 145% is an artifact of applying the plaster-ceiling L/240 total-load limit universally; under standard no-ceiling rafter limits the member passes all deflection checks, so the letter self-declares a false failure while simultaneously hiding a real 116% bending question behind the inconsistent 89%. Either defect alone gets the package rejected by a reviewing PE — P0 stands.

_Reported by 6 agent(s)._

### P0-15 [PV-4B] Conductor schedule is fabricated end-to-end: DC module current (8.22A) used for AC branches with undersized 15A OCPD, raw float '110.39999999999999A', conduit fill/size that match nothing, non-reproducing voltage drop, #14 EGC vs the set's own #12 minimum, and canned 60-ft lengths with no derate chain

**Description:** Every number an examiner would check fails. All six 'AC Branch' rows show 8.22A — the module's DC Isc x 1.25 (6.58 x 1.25), not an AC branch current; the real branches (10/10/10/10/7/6 IQ8A per the package's own branch tables) carry up to 14.5A continuous, requiring 20A OCPD (14.5 x 1.25 = 18.2A) vs the 15A breakers listed — an NEC 690.8/240.4 violation as printed. The AC Output ampacity cell prints the unrounded double '110.39999999999999A' (and it's DC-nameplate-derived; actual output is ~77A), while the same AMPACITY column holds load current for branches — internally incoherent semantics. The schedule says 1-1/2" EMT, the fill table (and PV-4A) say 1-1/4" at 34.3% — recomputation (0.384 in2) yields 25.7% or 18.9%, matching neither. The V-drop 'formula reference' with I=100A computes 2.33V, not the printed 1.79V/0.75% (which back-solves to 77A — the table labels OCPD rating as 'operating current'). The EGC row says '#14 AWG bare Cu' while PV-4A's methodology demands 'Min. #12'. All 8 circuits list exactly 60 ft, branch V-drop cells are all '—', conduit is 'EMT' with no size, and despite the title 'NEC 310.15 Ampacity Compliance' no base-ampacity -> correction -> adjustment -> adjusted-vs-applied chain appears anywhere (the reference tabulates the full chain per raceway, e.g. 30.00 -> 21.84 vs 16.64A). PV-4A adds an empty rapid-shutdown VALUE cell, a 'NEC 310' cite for voltage drop, inconsistent V-drop targets vs PV-4B, and a bottom half that is blank while sibling sheets overflow.

**Evidence:** HTML 2109 ('110.39999999999999A'), 2043-2098 (8.22A x6, 15A), 2107-2108 vs 2128-2129 (1-1/2" vs 1-1/4"/34.3%), 2143-2165 (Vd formula/result), 2115-2119 ('#14 AWG bare Cu') vs 1964 ('Min. #12'); branch composition 10/10/10/10/7/6 at HTML 1720-1746; ref-p8-PV4b.png full derate-chain table; electricalPages.ts:144-146 (_csBranchAmps = panelIsc x 1.25), :164/:189/:193 (60-ft global, no toFixed).

**Root cause:** lib/permit/sections/electricalPages.ts:144-146 (branch amps/OCPD from panel DC Isc×1.25 instead of micros-per-branch × inverter AC output current), :189 (acWireAmpacity printed with no rounding → 110.39999999999999A raw float from 115×0.96 in lib/electrical-calc.ts:907), :265 (V-drop formula prints I = busbar.backfeedBreakerRequired (100A) while the actual calc used ~77A → printed formula computes 2.33V not 1.79V), :207-221 (EGC sized from the fabricated 15A OCPD → #14, contradicting PV-4A's printed 'Min. #12'), :163-164/:192-193/:223-224 (single project.conduitType/wireLength → canned EMT/60 ft on all 8 rows), :188 (acConductorCallout embeds 1-1/2" while elec.conduitFill reports 1-1/4"/34.3% — fill computed from a different conductor set; recomputation gives 25.7% or 18.9%, matching neither).

**Fix sketch:** In the MICRO path, compute per-branch current from planMicroBranches group sizes × inverter max continuous output amps (IQ8A = 1.45A), OCPD = necNextStandardOcpd(current×1.25) (→ 20A), and size EGC from that OCPD (≥ #12); print acWireAmpacity with .toFixed(1) and drive the formula-reference I from the same current the V-drop calc actually used. Reconcile conduit size by deriving both the callout and the fill row from one computed conductor set (single source in electrical-calc.ts), and replace the generic factor tables with a per-raceway derate-chain table (base ampacity → temp correction → fill adjustment → adjusted vs applied) like the Cannon reference PV-4.A.

**Verifier notes:** Every checkable claim survives adversarial recomputation: 8.22 = 6.58×1.25 (DC Isc on AC branches); real branches are 10/10/10/10/7/6 IQ8A (HTML 1720-1747) → 14.5A continuous on a printed 15A breaker, an outright NEC 690.8(B)/240 violation as printed (20A required; Enphase's own limit is 20A per branch); raw float 110.39999999999999A at HTML 2109; 1-1/2" (HTML 2108) vs 1-1/4"/34.3% (HTML 2128-2129) with true fill 25.7% (1-1/4") or 18.9% (1-1/2") — printed value matches neither; printed Vd formula with I=100A yields 2.33V, not the printed 1.79V (back-solves to ~77A actual output); EGC '#14 AWG bare Cu' (HTML 2118) vs PV-4A's 'Min. #12 AWG Cu for ≤ 20A circuits' (HTML 1964); all 8 rows 60 ft; no applied derate chain while reference ref-p8 (Cannon PV-4.A) shows the full per-raceway chain (30.00→0.91→0.80→21.84 vs 16.64). One sub-claim of the finding is inaccurate: 110.4A is not DC-nameplate-derived — it is the derated conductor ampacity of #2 THWN-2 (115A × 0.96), which is legitimate content for an Ampacity column; the real defects there are the unrounded float and that the six branch rows put load current in the same column (incoherent semantics). This correction does not weaken the finding. P0 stands: a code violation as printed, a float artifact, and self-contradicting conduit sizes on the one sheet an electrical examiner recomputes line-by-line.

_Reported by 20 agent(s)._

### P0-16 [PV-5] Label sheet fails NEC across the board: required labels carry wrong data (8.2A on a ~77A system), the mandated rapid-shutdown placard text is invented, and mandatory 690.13(B)/690.56(C)(3)/705.10 labels are missing or marked N/A

**Description:** L-1 (NEC 690.54 interconnection) and L-6 (system disconnect) both print 'MAXIMUM CIRCUIT CURRENT: 8.2A' — the single-module DC Isc x 1.25 leaked from the DC wire-sizing table; the actual rated AC output of 53 IQ8A is ~77A (package's own load calc: '18.50 kW / 240V = 77.1A'), so a safety label understates the source ~10x (reference prints 'RATED AC OPERATING CURRENT 68.30 AMPS'). L-2 replaces the verbatim 690.56(C)(1)(a) placard ('SOLAR PV SYSTEM EQUIPPED WITH RAPID SHUTDOWN — TURN RAPID SHUTDOWN SWITCH TO THE OFF POSITION...') with an invented checkbox card ('STATUS: [ ] NORMAL OPERATION / [ ] RAPID SHUTDOWN ACTIVATED') citing the wrong subsection; L-3 mashes the raceway-marking title onto shutdown-map language, leaving no conduit 'WARNING: PHOTOVOLTAIC POWER SOURCE' marking anywhere. Missing outright: the 690.13(B) 'terminals on line and load sides may be energized' warning, the 690.56(C)(3) 'RAPID SHUTDOWN SWITCH FOR SOLAR PV SYSTEM' switch label, and the NEC 705.10 power-source directory plaque (L-11 is greyed 'N/A' — gated on necVersion==='2023' though it is mandatory for every interactive system; the reference's centerpiece is exactly this 'CAUTION! POWER TO THIS BUILDING...' placard with a disconnect-location diagram, which our sheet lacks entirely). Placements contradict the design (L-1 'at DC disconnect' on a system whose own schedule marks the DC disconnect N/A; L-6 cites 690.53, a DC label, with 'MAXIMUM INPUT VOLTAGE: 240V AC' nonsense text). All labels render as uniform white-on-red boxes with a blanket 3/8"-lettering note, erasing the ANSI Z535 orange-WARNING formats the real procurable labels carry.

**Evidence:** HTML 2629 (L-1/L-6 '8.2A', L-2/L-3 full text, 'NEC 690.56(B)' cite), 2786 (8.2 = 6.58 x 1.25 source), 2236-2237 ('77.1A PV'), 2689 (L-10/L-11 N/A rows, '6 OF 11 APPLICABLE'); compliancePages.ts:30 (maxCircuitCurrent = panelIsc*1.25), :58-89 (invented L-2/L-3), :208 (L-11 required only if necVer===2023), :258 + bg '#cc0000'; ref-p10-PV5-placards.png (correct 690.13(B), 690.56(C)(3), CAUTION directory placard, ANSI artwork).

**Root cause:** lib/permit/sections/compliancePages.ts:30 (maxCircuitCurrent = panelIsc*1.25 feeds L-1/L-6 for all topologies); :58-89 (invented L-2 checkbox text + L-3 raceway/shutdown mash-up); :43-210 (label library lacks 690.13(B), 690.56(C)(3), and 690.31(G)(3) raceway entries); :208 (L-11/NEC 705.10 directory gated on necVersion==='2023' though required under 2020); :214-232 (single white-on-red card renderer erases ANSI orange-WARNING formats)

**Fix sketch:** In pageWarningLabels, compute the 690.54/disconnect label current from rated AC output when isMicro (inverterCount × inverter max continuous output A, or system.totalAcKw*1000/240 ≈ 77A here) and keep panelIsc*1.25 only for DC-side labels; replace L-2 with the verbatim 690.56(C)(1)(a) placard text citing 690.56(C), restore L-3 as the 690.31(G)(3) raceway "WARNING: PHOTOVOLTAIC POWER SOURCE" marking, and add 690.13(B) and 690.56(C)(3) entries. Remove the necVer==='2023' gate on L-11 (705.10 directory is mandatory in 2017/2020/2023) and render it as the "CAUTION! POWER TO THIS BUILDING..." directory placard; add a `style` field (danger-red vs ANSI orange WARNING header) to LabelSpec and branch renderLabel on it, and make L-1/L-6 placements/citations topology-aware (micro → AC disconnect only, no DC-disconnect placement, no 690.53 cite).

**Verifier notes:** All checkable claims verified. PV-5 PNG shows L-1 and L-6 printing "MAXIMUM CIRCUIT CURRENT: 8.2A"; HTML DC wire table shows 6.58A×1.25=8.2A (single-module DC Isc) and the package's own load calc prints "18.50 kW AC ÷ 240V = 77.1A PV" — so the NEC 690.54 label understates the source ~9.4x. L-2 is an invented checkbox card citing 690.56(B) (wrong subsection); the mandated verbatim rapid-shutdown placard is absent; L-3 has no raceway marking and none exists elsewhere on the sheet. 690.13(B) and 690.56(C)(3) labels are missing from the code's label array entirely; L-11 (705.10 directory) is greyed N/A because of the necVer==='2023' gate despite the job being NEC 2020. Reference ref-p10-PV5-placards.png demonstrably does all of these correctly (verbatim RSD placard with diagram, 690.13(B) orange WARNING, RSD switch label, 690.31(G)(3) conduit label, and the CAUTION power-source directory with a disconnect-location site diagram as its centerpiece). Minor caveat: the reference's 68.30A is that project's own AC current (different system size), so it is a format precedent, not a value comparison. Severity re-assessed and kept at P0 — wrong-current safety labels plus missing mandatory placards are plan-review rejection and field-safety material.

_Reported by 24 agent(s)._

### P0-17 [E-1] Branch-circuit design overloads its own breakers: 53 IQ8A micros on 4x20A branches (needs 5-6), 'Max Micros/Branch: 16' false for IQ8A, 'Branch OCPD: 100 A' printed over '#10 AWG', and the count contradicts PV-2B's 6 branches

**Description:** E-1 shows '4 branches / 20A OCPD ea.' for 53 microinverters — at least one branch carries 14 IQ8A at 1.454A each = 20.4A continuous (25.4A required OCPD), over the 20A breakers before the 125% factor; Enphase's listing allows ~10-11 IQ8A per 20A branch, so a minimum of 5-6 branches is required (NEC 690.8(B)/210.20 + 110.3(B) violation). The note 'Max Micros/Branch: 16 (NEC 690.8)' is the base-IQ8 limit, wrong for IQ8A and mis-attributed to the NEC. The AC BRANCH CIRCUIT INFO table compounds it with 'Branch OCPD: 100 A' directly above 'Branch Wire: #10 AWG' — a gross 240.4 violation as printed (the feeder OCPD pasted into the branch row, contradicting the drawn 20A breakers two inches above). And the package disagrees with itself on the count: PV-2B's notes say '6 AC branches — IQ Combiner accepts 4; remaining branches land on AC subpanel, see E-1' while E-1 shows 4 branches and no subpanel — the sheet the note points at contradicts it.

**Evidence:** E-1.png combiner with four 20A breakers; HTML 3311-3315, 3374-3383 ('Branch Circuits 4', 'Max Micros/Branch 16 (NEC 690.8)', 'Branch OCPD 100 A', 'Branch Wire #10 AWG'), 3373 ('AC per Micro 349 W'); HTML 1760 (PV-2B '6 AC branches ... see E-1'); math 14 x 1.454 x 1.25 = 25.4A.

**Root cause:** lib/sld-professional-renderer.ts:2332-2333 and 2341 (ab=Math.ceil(md/16) hardcoded; ba falls back to system-output OCPD necNextStandardOcpd(77.1A×1.25)=100A when branchRun is undefined; 'Max Micros/Branch 16 (NEC 690.8)' hardcoded), vs drawn combiner fallback of 20A at lines 1734/1764; PV-2B note independently uses totalStrings=6 at lib/permit/sections/arrayPages.ts:432

**Fix sketch:** Compute one authoritative branch plan (unitsPerBranch from inverterCapabilities for the actual micro model — 11 for IQ8A; nBranches = ceil(deviceCount/unitsPerBranch); branchOcpd = nextStandardOcpd(unitsPerBranch × Iout × 1.25), i.e. 20A) and pass it into the SLD renderer so the drawn combiner, the AC BRANCH CIRCUIT INFO table, and arrayPages' PV-2B note all consume the same numbers. Delete the ceil(md/16) recomputation, replace the hardcoded '16 (NEC 690.8)' with the model-specific per-branch limit (attributed to the Enphase listing/110.3(B), not NEC 690.8), and never fall back branch OCPD to the system output OCPD at line 2333 — fall back to the computed branch value.

**Verifier notes:** All four sub-claims verified in the generated HTML: (1) 53 IQ8A on 4×20A branches — HTML 3311-3315 shows '4 branches / 20A OCPD ea.' with four drawn 20A breakers; 349W/240V=1.454A, worst branch 14×1.454=20.36A continuous exceeds the 20A breaker before the 125% factor (required OCPD 25.4A); IQ8A listing limit is 11 per 20A branch → min 5 branches. (2) 'Max Micros/Branch 16 (NEC 690.8)' at HTML 3377-3378 is the base-IQ8 figure, wrong for IQ8A and mis-attributed. (3) 'Branch OCPD 100 A' directly above 'Branch Wire #10 AWG' at HTML 3379-3383 — the 100A is the system output OCPD (18.497kW→77.1A×1.25→100A) pasted into the branch row, contradicting the 20A breakers drawn on the same sheet; #10 AWG on a 100A OCPD as printed violates NEC 240.4. (4) PV-2B (HTML 1760) says '6 AC branches ... see E-1' while E-1 shows 4 and no subpanel — arrayPages.ts:432 uses totalStrings while the SLD recomputes ceil(53/16). The active renderer for this package is lib/sld-professional-renderer.ts (its exact strings '#10 AWG' and '349 W' match; electricalPages.ts would print '#10 AWG THWN-2'). Finder's suspected code location was correct.

_Reported by 8 agent(s)._

### P0-18 [E-1] Every conductor callout and schedule row on the single-line diagram is corrupted or non-compliant: '3#32 THWN-2', two conduit sizes per run, fabricated 32% fill (actual ~76%), #10 EGC on 100A OCPD, impossible voltage-drop ordering, THWN-2 'open air', and the feeder spec cloned onto the branch run

**Description:** The three feeder callouts read '3#32 THWN-2 + 1#8 AWG GND in 1-1/2" EMT THWN-2' — #32 AWG does not exist (double-formatting of an already-formatted '3#2...' string), with a dangling duplicate 'THWN-2', and the third line of the same label says 'IN 3/4" EMT', contradicting line 1 (the bottom tables repeat the 1-1/2" vs 3/4" conflict). The Conduit & Conductor Schedule claims 3x#2 + #8 + #10 in 3/4" EMT at '32.0%' fill — actual is ~76%, nearly double the Chapter 9 limit and physically unpullable. Each run lists both a #8 GND and a '#10 GRN EGC' (NEC 250.122 requires #8 for 100A; SCHED buys #8). The AMPACITY column prints 77A (the load current) for #2 conductors, V-drop shows 1.20% for 50 ft but 1.40% for 20 and 15 ft (impossible ordering; recomputed 0.62/0.25/0.19%), row BR-1 assigns the #2 feeder bundle to the roof J-box segment that the SLD itself labels as a #10/20A branch — and the four real branch circuits plus the GEC appear nowhere in the schedule. The rooftop run is labeled '#10 THWN-2 — OPEN AIR — NEC 690.31' (THWN-2 is a raceway conductor; the actual product is the Enphase Q Cable the BOM buys), the utility service conductors carry the PV feeder callout, and presentation defects pile on: two complete title blocks with dueling seals/revisions, '(N) AC DISCONNECT' overprinted on 'AC DISCONNECT', and ~45% blank sheet.

**Evidence:** HTML 3320-3323 (3 occurrences of the '3#32 ... 1-1/2" EMT ... IN 3/4" EMT' stack); schedule rows HTML 3493-3526 (EMT 3/4", 32.0%, 77A, 100A, 1.20/1.40/1.40%); NEC Ch.9 recomputation 0.405 in2 / 0.533 in2 = 76%; HTML 3314/3356 (OPEN AIR THWN-2); root cause reproduced at lib/computed-system.ts:2077 (re-wraps a full callout string); e1_disco_msp.png overprint; dual title blocks + 'DWG NO. SLD-001'.

**Root cause:** lib/permit/utils/sldAdapter.ts:54 (feeds full callout string into acWireGauge) + lib/sld-professional-renderer.ts:1570,1902,1948,2232 (re-formats it into '3#32 ... EMT THWN-2' + contradictory 'IN 3/4" EMT' fallback) + :2557-2560 (hardcoded fallback schedule: fill 32%, vdrop 1.2/1.4/1.4, pass:true, amp=load current, feeder spec cloned onto BR-1 — always used because sldAdapter never populates input.runs) + :1893 ('(N) AC DISCONNECT' overprinted on renderDisco's own header)

**Fix sketch:** (1) In sldAdapter.ts:54 pass the bare gauge (compliance.electrical.acWireGauge, already stored by generatePermit.ts:533), never acConductorCallout; defensively, in sld-professional-renderer.ts have the wire-label/schedule builders detect a composite string (contains '+' or a conduit token) and use it verbatim as the label instead of re-templating. (2) Populate SLDProfessionalInput.runs from the computed-system runSegments in buildSLDInputFromPermit (or compute real Ch.9 fill, IR voltage drop, and 310.16 ampacity) and delete the hardcoded 32%/1.4%/PASS fallback rows at renderer:2557-2566. (3) Remove the duplicate '(N) AC DISCONNECT' txt at renderer:1893 (renderDisco already draws the header) and set drawingNumber to the sheet ID 'E-1' in sldAdapter.ts:112.

**Verifier notes:** Every element of the finding verified against the raw HTML and recomputed. (a) Three SLD runs (HTML lines 3320/3322/3323 — combiner→disco, disco→MSP, and the MSP→utility SERVICE run) read '3#32 THWN-2 + 1#8 AWG GND in 1-1/2" EMT THWN-2' with third line 'IN 3/4" EMT' — #32 AWG does not exist; reproduced exactly: acConductorCallout '3#2 AWG THWN-2 + 1#8 AWG GND in 1-1/2" EMT' passes through .replace('#','').replace(' AWG','') (first-occurrence only) then template `${3}#${...} THWN-2`. (b) Schedule rows (HTML 3493-3526) match the renderer's hardcoded fallback verbatim: 'EMT 3/4"' vs the 1-1/2" embedded in the same cell; '32.0%' fill is fabricated — recomputed 3×0.1158+0.0366+0.0211=0.4051 in² / 0.533 in² (3/4" EMT) = 76.0%, ~1.9× the 40% Chapter 9 limit; 'AMPACITY 77A' is the load current (input.acOutputAmps), not #2 Cu ampacity (115/130A); V-drop 1.20% @ 50 ft vs 1.40% @ 20/15 ft is hardcoded and physically impossible (recomputed 0.62/0.25/0.19% at 77A, #2 Cu, 240V); '1×#10 GRN EGC' violates NEC 250.122 (#8 Cu required for 100A OCPD); BR-1 assigns the feeder bundle + 100A OCPD to the roof J-box segment the SLD itself labels '#10 AWG / 20A OCPD ea.'; the four branch circuits and GEC are absent; PASS is hardcoded true. (c) '#10 AWG THWN-2 — OPEN AIR — NEC 690.31' on the rooftop run confirmed (HTML 3314) — THWN-2 is not permitted in free air; the installed product is the Q-cable/trunk the BOM specifies. (d) '(N) AC DISCONNECT' overprint confirmed in code and on e1_disco_msp.png; embedded SVG carries a second complete title block with 'DWG NO. SLD-001' while the package title block says E-1. Finder's suspected lib/computed-system.ts:2077 is the same anti-pattern but NOT the code that produced this sheet — actual sites are sldAdapter.ts + sld-professional-renderer.ts. P0 stands: this is the core electrical sheet; any plan reviewer rejects it on the nonexistent gauge alone, and the fill/EGC/schedule numbers are fabricated rather than computed.

_Reported by 21 agent(s)._

### P0-19 [PE-1] Package ships with FAILING structural check and 'DO NOT ISSUE' box — the 'Letter of Structural Compliance' certifies non-compliance

**Description:** The rafter deflection check fails: 0.868 in vs allowable 0.600 in (145% of limit), flagged 'deflection — 145% (EXCEEDS LIMIT)' as the governing check, and the sheet carries a red box reading 'STRUCTURAL REVIEW REQUIRED — DO NOT ISSUE'. The engineer's certification statement on a sheet titled LETTER OF STRUCTURAL COMPLIANCE reads that the modeled framing 'does not satisfy all code checks' — a self-contradictory document no PE can sign. PV-4C repeats 'deflection — 145%' and 'REQUIRES REINFORCEMENT' with no reinforcement detail provided. A permit package containing its own do-not-issue instruction is an automatic intake rejection; either the rafter inputs are wrong (12.0 ft span for 2x6 @ 24" O.C. is suspiciously long) or the design needs reinforcement. The Wyssling reference instead includes a signed PE letter certifying the roof IS adequate.

**Evidence:** PE-1.png: red-bordered box 'STRUCTURAL REVIEW REQUIRED — DO NOT ISSUE'; Deflection cell '0.868 in (Δ_allow = 0.600 in — 145%)'; certification paragraph '...determined that the modeled framing does not satisfy all code checks'; HTML lines 3188, 3209-3212 confirm exact text. PV-4C.png: 'REQUIRES REINFORCEMENT for the additional PV loading per IBC Section 1607'. ref-text.txt pp1-2: Wyssling letter signed, Indiana PE11900440, 'will adequately support the additional loading'.

**Root cause:** repo/lib/permit/generatePermit.ts:294 (rafterSpan hardcoded default 12 ft when input.project.rafterSpan is missing) feeding structural-engine-v4; fail-state letter rendered by repo/lib/permit/sections/certPages.ts:401,486-514; echoed by repo/lib/permit/sections/structuralPages.ts:824

**Fix sketch:** Derive the real rafter span from the CAD roof geometry (plane eave-to-ridge run / cos(pitch), halved for mid-support where detected) instead of the `|| 12` default at generatePermit.ts:294, and treat a missing span as a required input. Additionally, gate delivery: when certPages `_allPass` is false, fail permit generation at the existing validateCanonicalStrict hard gate (generatePermit.ts:276 area) with an actionable error ("deflection exceeds L/240 — verify framing/span or specify truss"), so a DO-NOT-ISSUE letter can never silently ship in a customer package.

**Verifier notes:** Independently recomputed: Δ_allow = 144/240 = 0.600 in; Δ = 5wL⁴/384EI with w=61.9 lb/ft, L=144 in, E=1.6e6 psi, I(2x6)=20.80 in⁴ gives exactly 0.868 in → 145% of limit. So the failure is a genuine engineering result of the guessed 12 ft span, not a display bug. All quoted text verified on PE-1.png and in the HTML (lines 3188, 3191, 3202, 3209-3216); PV-4C "REQUIRES REINFORCEMENT" confirmed at structuralPages.ts:824 with no reinforcement detail sheet anywhere in the package. The Wyssling reference ships a signed adequacy letter (ref-text.txt pp1-2). Mitigating nuance: the fail branch is honest and intentional (it does NOT falsely certify), so the engine's integrity is fine — the P0 is that the pipeline ships the failing package as a deliverable instead of blocking, and that the span input is a default rather than real geometry (matches the known open item "PE-1 real rafter span").

_Reported by 4 agent(s)._

### P0-20 [VAL-1 + PV-4A vs PE-1 + PV-4C] 'ALL CHECKS PASSED 11/11' and PV-4A 'Rafter Bending OK' contradict PE-1's failing structural check

**Description:** VAL-1's banner declares 'ALL CHECKS PASSED — 11/11 checks passed before planset generation' with 'Structural Method: Roof Lag Bolt Attachment (ASCE 7-22 §26/27)' shown PASS, and PV-4A reports 'Rafter Bending OK — 2x6 utilization 73% <= 100% — PASS' with '0 ERRORS / 0 WARNINGS' — while PE-1/PV-4C in the same package report deflection at 145% of limit with DO NOT ISSUE and bending utilization 89%. Both the pass/fail verdict AND the utilization number (73% vs 89%) disagree: two different structural models are printed as one analysis, and the validation gate either omits or ignores the deflection failure. A plan checker reading both sheets sees a self-contradicting package.

**Evidence:** VAL-1.png green 'ALL CHECKS PASSED' header + Structural Method PASS row ('Rafter utilization + lag bolt capacity from compliance engine') vs PE-1.png red DO NOT ISSUE box (HTML 3209-3212). PV-4A.png: 'Rafter Bending OK — 2x6 utilization 73% <= 100% ✓ PASS' and 0-errors banner. HTML confirms both '73% / 100% PASS' and 'Bending Utilization 89%' strings.

**Root cause:** repo/lib/permit/sections/validationPage.ts:117 (roof structural check status hardcoded 'PASS', never reads compliance.structural) + repo/lib/permit/generatePermit.ts:288-372 (server-side V4 engine writes 89%/145% into compliance.structural for PE-1/PV-4C, but input.rulesResult — rendered on PV-4A by electricalPages.ts:53 — still carries the frontend V1 runStructuralCalc result of 73% and is never recomputed) + repo/lib/rules-engine.ts:197-212 (ruleRafterBending checks bending only; no deflection rule exists, so the governing 145% failure can never appear on PV-4A)

**Fix sketch:** In validationPage.ts:117, derive roof structural status from the same numbers PE-1 uses: PASS only if bendingMoment/allowableBendingMoment <= 1.0 AND deflection/allowableDeflection <= 1.0 AND attachment.safetyFactor >= 2.0 (else FAIL, which flips the banner via failCount at line 155). In generatePermit.ts after the V4 calc (~line 372), patch or regenerate rulesResult: overwrite the ASCE_RAFTER_BENDING row from V4's bendingUtilization and add an ASCE_RAFTER_DEFLECTION rule from V4's deflection ratio, so PV-4A, VAL-1, PV-4C and PE-1 all report one model.

**Verifier notes:** Verified on all four PNGs and in the raw HTML (lines 1948/2543 '73% <= 100%' vs 2397/3188 '89%'+145% and 3209 DO NOT ISSUE vs 3666-3667 ALL CHECKS PASSED 11/11). PE-1/PV-4C are the honest sheets (certPages.ts:391-404 computes per-check ratios and gates DO NOT ISSUE correctly); the defect is that VAL-1's gate is a hardcoded PASS and PV-4A prints a stale V1-engine result that uses a different rafter model and lacks any deflection check. Corroborating detail: at the V4's 89% bending, rules-engine.ts:204 would emit a 'Rafter Near Capacity' WARNING — PV-4A showing clean 73%/0-warnings proves its numbers came from a different model, not just a rounding difference.

_Reported by 3 agent(s)._

### P0-21 [APP-A (also SCHED)] Module Voc 75.6V exceeds inverter max DC input 60V — on the same sheet

**Description:** The left column specifies the module at Voc 75.6 V (and the sheet's own NEC table computes 94.5 V max corrected per 690.7), while the right column specifies the Enphase IQ8A with 'MAX DC VOLTAGE 60 V'. SCHED repeats both values. The specified module cannot legally or physically be connected to the specified microinverter (NEC 110.3(B) — equipment used per listing); every one of the 53 module/micro pairs would operate far beyond the inverter's DC rating. The real IQ8A is a 54/60/66-cell-class micro; a Maxeon 3 400W requires an IQ8H-class or different inverter. As printed the design is unbuildable and any competent plan checker rejects it.

**Evidence:** APP-A.png: 'OPEN CIRCUIT VOLTAGE (VOC) 75.6 V' + 'Voc ×1.25 → 94.5 V max' in NEC table vs 'MAX DC VOLTAGE 60 V' under INVERTER #1: ENPHASE IQ8A. HTML lines 2879, 2907, 2926. SCHED.png inverters table 'MAX DC V 60V' with modules Voc 75.6V.

**Root cause:** lib/system/panelCompatibilityGate.ts:318-337 (micro topology short-circuits to 'compatible' with no Voc check; the gate only ever checks current) + lib/electrical-calc.ts:385-390 (micro branch skips the E-VOC-EXCEED voltage validation that runs at :422 for string topologies). Permit layer (lib/permit/sections/compliancePages.ts:432, lib/permit/generatePermit.ts:468) prints both DB values with no cross-check.

**Fix sketch:** In panelCompatibilityGate's micro short-circuit, before returning 'compatible', check panel.voc × 1.25 (or cold-corrected factor) against the brand's micro maxDcVoltage (resolve from MICROINVERTERS in equipment-db) and return 'incompatible' with low-Voc panel suggestions when exceeded — mirroring the existing optimizer check in lib/optimizer-binding.ts:141. Also add a per-module E-VOC-EXCEED issue in electrical-calc.ts's micro branch (panelVoc × vocCorrectionFactor > inv.maxDcVoltage), and a hard validation in generatePermit that refuses/flags printing a module/micro pair whose Voc exceeds the inverter's maxDcVoltage.

**Verifier notes:** Verified on APP-A.png and HTML: module Voc 75.6 V + sheet-computed 94.5 V max sit inches from 'Max DC Voltage 60 V' under Enphase IQ8A; SCHED repeats both. Math checks (75.6 × 1.25 = 94.5). Not a data typo — equipment-db.ts:328 (Maxeon 3 voc 75.6) and :1920 (IQ8A maxDcVoltage 60) are both datasheet-correct; the pairing itself is electrically impossible and violates the listing (NEC 110.3(B)). Root cause is a systemic gap: the Panel Compatibility Gate checks only Isc/current and returns 'compatible' unconditionally for micro topology, and electrical-calc's micro branch skips all voltage checks (optimizers DO get a Voc check at optimizer-binding.ts:141). Ironically Maxeon 3 was added in v47.425 specifically to always pass the current-only gate (equipment-db.ts:314-319), guaranteeing this high-Voc panel could be paired with any 60 V micro. P0 stands: self-contradictory on one sheet, unbuildable as printed, certain plan-check rejection for all 53 module/micro pairs.

_Reported by 3 agent(s)._

### P0-22 [PV-0 + PV-4B vs PV-4A + E-1 + PV-5 + SCHED] Interconnection method contradicts itself; PV-4B prints an explicit '120% Busbar Rule FAIL — UPGRADE PANEL'

**Description:** The package tells two incompatible interconnection stories. PV-0's System Summary lists '(N) 100A BACKFEED BREAKER (NEC 705.12(B))' and flags 'NEC 705.12(B): REQUIRES REVIEW', while the same sheet's System Information says 'INTERCONNECTION: Supply Side Tap'. PV-4B's Load Calculations row 9 reads '120% Busbar Rule — NEC 705.12(B): 200A bus × 120% = 240A max; minus 200A main = 40A for PV — FAIL — UPGRADE PANEL', with interpretation text stating the 100A backfeed exceeds the 40A max and 'a panel upgrade or supply-side connection is required.' Meanwhile PV-4A states 'Supply-side tap (NEC 705.11) — 120% busbar rule does not apply. PASS / 0 errors', E-1 says 'Interconnection: Supply Side Tap / Backfeed Breaker: N/A / 120% Rule: N/A', and PV-5 label L-5 says supply-side tap. Backfed breaker vs supply-side tap are mutually exclusive designs; a permit sheet that literally prints FAIL next to sheets claiming zero errors is an instant rejection.

**Evidence:** PV-4B HTML (sheet 7): 'FAIL — UPGRADE PANEL' and '120% RULE INTERPRETATION: The PV system requires a 100A backfeed breaker... 300A, which exceeds the 120% limit of 240A.' PV-0.png: '(N) 100A BACKFEED BREAKER (NEC 705.12(B))', 'INTERCONNECTION: Supply Side Tap', 'NEC 705.12(B) — REQUIRES REVIEW'. PV-4A.png 'Supply-side tap... 120% busbar rule does not apply... PASS'. e1_tables.png: 'Backfeed Breaker N/A — Tap Connection'.

**Root cause:** lib/permit/sections/electricalPages.ts:342-399 (row 9 + interpretation + formula blocks compute load-side 120% rule unconditionally, printing 'FAIL — UPGRADE PANEL') and lib/permit/sections/coverSheet.ts:60-67, 206-207, 303 (unconditional backfeed-breaker equipment row and 'REQUIRES REVIEW' while line 70-71 prints 'Supply Side Tap'). Neither checks project.interconnectionMethod; PV-4A/E-1 use rules-engine.ts:283-286 / sldAdapter.ts:74-78 which DO check it, producing the cross-sheet contradiction.

**Fix sketch:** Gate both sections on the interconnection method (e.g. const isSupplySide = (project.interconnectionMethod||'').toUpperCase().includes('SUPPLY') or elecResult.busbar.busbarRule === 'supply-side'). In electricalPages.ts, when supply-side render row 9 as 'N/A — Supply-Side Tap (NEC 705.11), 120% rule does not apply' and replace the FAIL interpretation/formula blocks with supply-side text (tap conductor sizing per 705.11). In coverSheet.ts, when supply-side replace the '(N) 100A BACKFEED BREAKER' equipment row with '(N) SUPPLY SIDE TAP CONNECTION (NEC 705.11)' and print the NEC 705.12(B) info row as 'N/A — SUPPLY SIDE TAP' instead of 'REQUIRES REVIEW'.

**Verifier notes:** Every quoted string verified verbatim in the generated HTML: PV-0 lines 1019 ('100A BACKFEED BREAKER (NEC 705.12(B))'), 1240 ('Supply Side Tap — NEC 705.12(A)'), 1242-43 ('REQUIRES REVIEW'); PV-4B lines 2238-2256 ('FAIL — UPGRADE PANEL', red FAIL badge, '300A exceeds the 120% limit of 240A... panel upgrade or supply-side connection required'); PV-4A lines 1919-20 ('Supply-side tap (NEC 705.11) — 120% busbar rule does not apply'). The arithmetic in PV-4B is internally correct (18.5kW/240=77.1A, ×1.25=96.4→100A OCPD; 200×1.2−200=40A; 100>40 → fail), so the defect is not a math bug — it's the wrong (load-side) analysis being rendered for a supply-side-tap project, contradicting PV-4A/E-1/PV-5/provenance (sld_metadata: SUPPLY_SIDE_TAP). A permit set that prints an explicit code-compliance FAIL alongside sheets claiming the rule is N/A is an instant plan-check rejection, so P0 stands. Note docs/action-plan/BUG_FIX_LOAD_SIDE_TAP.md documents the identical class of bug (120% check run unconditionally) that was fixed in rules-engine.ts but never applied to the permit-sheet renderers.

_Reported by 2 agent(s)._

### P0-23 [PV-2 vs PV-0/PV-2B/SCHED/E-1] PV-2 roof description table sums to 52 modules, not the 53 claimed everywhere else

**Description:** The MAIN HOME ROOF DESCRIPTION table on PV-2 lists Roof 1 = 23, Roof 2 = 21, Roof 3 = 4, Roof 4 = 4 modules — total 52 — directly under a header that says '53 MOD @ 21.20 kWDC'. Every other sheet (PV-0, PV-2B branch legend 10+10+10+10+7+6=53, SCHED, E-1, VAL-1) says 53 modules / 21.20 kW DC. APP-CAD's warning 'removed 1 panel(s) blocked by obstructions' suggests the true post-obstruction count is 52 and the 53 headline was never updated. 52 × 400W = 20.8 kW, so either the count or the 21.20 kW DC rating is wrong.

**Evidence:** PV-2.png roof table rows 23/21/4/4; HTML extraction: 'MAIN HOME ROOF DESCRIPTION|ROOF|MODULES|...|1|23|...|2|21|...|3|4|...|4|4...'. APP-CAD.png WARNINGS: '[OBSTRUCTION] plane aa72d4e3...: removed 1 panel(s) blocked by obstructions'.

**Root cause:** C:\Users\Ray\Solarpro Claude\repo\lib\cad\roof\roofCAD.ts:526-529 (totalPanels override keeps spec-side 53 after the obstruction filter at roofCAD.ts:374-377 removed 1 placed panel; geometry → PV-2 table/drawing = 52 via lib/drafting/templates/roof.ts:571-584, while all headline text reads cad.totalPanels = 53)

**Fix sketch:** In buildRoofCAD's aggregate step, when panels were actually placed (allPanels.length > 0) and obstruction filtering removed any, use the real placed count: totalPanels = Math.min(projectTotalPanels || Infinity, allPanels.length) and recompute totalDcKw = totalPanels * panelWatts / 1000 (keep the spec override only when CAD OVER-places vs spec). Additionally, propagate the post-filter count upstream so branch grouping (PV-2B legend 10+10+10+10+7+6) and E-sheets regroup on the surviving panels instead of the spec count, or escalate the [OBSTRUCTION] warning to a blocking error so the design is re-run at 52 modules.

**Verifier notes:** Verified all three legs independently: (1) PV-2 SVG contains exactly 52 module rects (fill #fdfdfd, stroke-width 0.8) — the 53rd matching rect is the legend swatch at stroke-width 0.7; (2) the MAIN HOME ROOF DESCRIPTION table rows are 23/21/4/4 = 52 (extracted from HTML line 1619 and confirmed on PV-2.png) directly under the 'ROOF PLAN — 53 MOD @ 21.20 KWDC' header; (3) every other sheet says 53 / 21.20 kW (HTML lines 940, 978, 1616, 1688, 3010, 3160, etc.) and APP-CAD shows '[OBSTRUCTION] plane aa72d4e3...: removed 1 panel(s) blocked by obstructions' (generated at roofCAD.ts:377). 52 × 400 W = 20.8 kW, so the 21.20 kW headline is also overstated. The finder's one soft spot — it implied the table might be the bug — is actually backwards: the table and drawing are truthful (52); the spec-side 53/21.20 headline is the stale value, held in place by the projectTotalPanels override at roofCAD.ts:527-529 whose comment shows it was written for the CAD-over-places case and never considered obstruction under-placement. Internally contradictory module count across sheets plus a DC-size overstatement is submission-blocking, so P0 stands.

_Reported by 1 agent(s)._

### P0-24 [PV-2/PV-3 vs PV-0/APP-A/PE-1/VAL-1] Two different racking systems specified in the same package: IronRidge XR100 vs Roof Tech RT-Mini

**Description:** PV-0 (title 'PHOTOVOLTAIC IRONRIDGE XR100 SYSTEM', System Summary), APP-A (full racking spec: IronRidge XR100, FlashFoot2 w/ L-Foot, 5/16" lag), PE-1 ('Mounting System: IronRidge XR100') and VAL-1 ('Mount System Verified: IronRidge XR100 — PASS, Locked via MOUNT_SYSTEM_MAP') all say IronRidge. But the actual install sheets show a different manufacturer's product: PV-2 header/system data 'ROOF TECH RT-MINI' and PV-3's entire attachment detail ('ATTACHMENT DETAIL — ROOF TECH RT-MINI', 3/8" lag). These are different products from different companies with different UL 2703 listings; the PE structural letter certifies hardware the construction detail doesn't show, and the AHJ cannot tell what is being installed.

**Evidence:** PV-0.png header/system summary (IronRidge XR100); APP-A.png racking table (IronRidge XR100, FlashFoot2); PE-1.png 'IronRidge XR100'; VAL-1.png 'Mount System Verified IronRidge XR100 PASS'; PV-2.png header 'ROOF TECH RT-MINI'; PV-3.png all callouts RT-Mini. HTML grep shows both strings, RT-MINI only inside PV-2/PV-3 zones.

**Root cause:** repo\lib\permit\utils\canonical.ts:19-23 and :159 — MOUNT_SYSTEM_MAP hardcodes roof→'IronRidge XR100' and buildCanonical assigns it while deliberately ignoring project.mountingSystem ("locked... no input drift"); meanwhile PV-2/PV-3 drafting templates (repo\lib\drafting\templates\roof.ts:121, :916; repo\lib\drafting\sheetComposition.ts:391) read the real project.mountingSystem ('Roof Tech RT-Mini'). Aggravating: repo\lib\permit\sections\compliancePages.ts:450-451 hardcodes IronRidge FlashFoot2 + 5/16" lag into APP-A for all roof jobs, contradicting PV-3's 3/8" lag detail.

**Fix sketch:** In canonical.ts change line 159 to honor the project's actual racking with the map as fallback only: `const mountSystem = project.mountingSystem || MOUNT_SYSTEM_MAP[rawType];` so PV-0/APP-A/PE-1/VAL-1 match PV-2/PV-3. Then make APP-A's attachment rows (compliancePages.ts:447-452) racking-aware — drive attachment type/lag diameter/rail-vs-railless from the resolved mount system (reuse the isRailless regex from roof.ts:124 or lib/mounting-hardware-db.ts) instead of hardcoded FlashFoot2/5-16" IronRidge specs.

**Verifier notes:** Verified directly in the generated HTML, not just PNGs. IronRidge XR100 appears on PV-0 (HTML lines 974/982/991/1022/1237), APP-A (2940-2957 incl. FlashFoot2 + 5/16" lag), PE-1 (3162), VAL-1 (4104 PASS); Roof Tech RT-Mini appears on PV-2 (1619, 1629) and PV-3 (1819, 1834, 1840, with 3/8" SS lag). Two different manufacturers with different UL 2703 listings AND contradictory lag-bolt specs (5/16" vs 3/8") in one package; the PE letter and validation sheet certify hardware the construction detail does not show. VAL-1's "Locked via MOUNT_SYSTEM_MAP — PASS" validates the hardcoded constant against itself, so the gate can never catch this. P0 stands: an AHJ reviewer comparing APP-A to PV-3 gets an immediate rejection, and the structural certification does not cover the depicted install (RT-Mini is rail-less; PV-3 even labels a "MOUNTING RAIL — ROOF TECH RT-MINI", which is incoherent).

_Reported by 2 agent(s)._

### P0-25 [PV-2B + PV-4B vs E-1] Branch circuit count is 6, 4, or 1 depending on the sheet

**Description:** PV-2B's branch legend and circuit map show 6 AC branches (10/10/10/10/7/6 = 53), and PV-4B's conductor schedule lists AC Branch 1-6. E-1 — the single-line diagram an electrical reviewer actually checks — says 'Branch Circuits: 4' in three separate tables and 'Max Mods/Branch: 16 (NEC 690.8)'. VAL-1's electrical line says '1 string(s)'. Three different topologies for the same system; the SLD does not match the layout or the conductor schedule. PV-2B also says 'IQ Combiner accepts 4; remaining branches land on AC subpanel, see E-1' but E-1 shows no subpanel.

**Evidence:** PV-2B.png branch legend (Branch 1-6, qty 10/10/10/10/7/6); PV-4B.png rows 'AC Branch 1'...'AC Branch 6'; HTML: 'Branch Circuits | 4' (two occurrences in E-1 SVG); e1-left-zoom.png 'Max Mods/Branch: 16 (NEC 690.8)'.

**Root cause:** lib/permit/utils/sldAdapter.ts:97-100 — plane-aware nBranches (planMicroBranches, same planner as PV-2B) is computed but never placed in the returned SLDProfessionalInput, so lib/sld-professional-renderer.ts falls back to hardcoded Math.ceil(md/16)=4 at lines 2332 and 2460 (tables) and 1733/1763 (combiner drawing), while PV-2B/PV-4B use planMicroBranches=6.

**Fix sketch:** In buildSLDInputFromPermit, pass the branch plan through: e.g. set `microBranches` (or a new `branchCircuitCount` + `maxMicrosPerBranch`) on the returned input from planMicroBranches(...).sizes. In sld-professional-renderer.ts use `input.microBranches?.length` at lines 2332/2460 (as 1733/1763 already do) and replace the literal '16 (NEC 690.8)' at line 2341 with microMaxPerBranch(input.inverterModel). Separately fix VAL-1's '1 string(s)' line and either render the AC subpanel on E-1 or drop PV-2B's 'see E-1' subpanel note.

**Verifier notes:** Verified on artifacts: PV-2B legend B1-B6 qty 10/10/10/10/7/6=53 and 'AC Branches: 6'; PV-4B rows AC Branch 1-6 (HTML 2039-2094); E-1 'Branch Circuits 4' twice (HTML 3374, 3440) + 'Max Micros/Branch: 16 (NEC 690.8)' (3378); VAL-1 '53 panels | 1 string(s)' (3766); 'subpanel' exists only in PV-2B's note (1760) — E-1 renders none. E-1 provenance pinned to renderSLDProfessional by the 100A Branch OCPD fingerprint (inline buildSLD would print 20A). Aggravating factor: branching.ts's own header documents IQ8A max ~10/branch, so E-1's '16 per branch' is also a standalone NEC-loading error, and 4 branches of 13-14 IQ8A would overload 20A branch circuits. Minor correction to finding text: 'Branch Circuits: 4' appears in two tables plus the drawn combiner, not three tables. P0 stands — the SLD is the sheet the electrical reviewer checks and it contradicts the layout, the conductor schedule, and the validation sheet in three mutually exclusive topologies (4 vs 6 vs 1).

_Reported by 2 agent(s)._

## P1 — Sub-professional vs reference / materially incomplete (32)

### P1-1 [SET-WIDE (all sheets)] Unsigned set: empty 'PE SEAL' placeholder boxes and 'AFFIX PE STAMP HERE' on every sheet of a package stamped 'ISSUED FOR PERMIT'

**Description:** Every title block carries a blank 'PE SEAL' box while REV A reads 'ISSUED FOR PERMIT — 2026-06-03'. The cover additionally has a second empty 'ENGINEER OF RECORD' block with literal placeholders ('AFFIX PE STAMP HERE / LICENSE NO. ____ / STATE: ____'), General Note 4 says 'PE STAMP REQUIRED FOR PERMIT SUBMISSION PER AHJ', and the Engineering Summary admits the document 'requires PE review and wet stamp before AHJ submission'. The set declares itself simultaneously issued-for-permit and not-submittable. The competing Wyssling reference carries an actual signed PE stamp (Scott E. Wyssling) on every sheet — this is the single most visible professionalism gap in the comparison, and in seal-requiring jurisdictions it is an automatic rejection.

**Evidence:** PV-0.png Engineer of Record block + title-block PE SEAL box (HTML 1277, 1079); empty pe-seal-box divs on PV-1, PV-2, PV-2B, PV-3 (HTML 1797-1800), PV-4C, PV-5 (HTML 2722-2725); ref-p3/p4/p5/p6 PNGs show the signed Wyssling PE11900440 stamp in the same position.

**Root cause:** repo/lib/permit/utils/titleBlock.ts:76-81 (unconditional 'REV A — ISSUED FOR PERMIT' + empty pe-seal-box on every sheet) and repo/lib/permit/sections/coverSheet.ts:482-486 (literal 'AFFIX PE STAMP HERE / LICENSE NO. ____ / STATE: ____' placeholder)

**Fix sketch:** Add an engineerOfRecord/seal field to PermitInput: when a stamped seal image + license data is supplied, embed it in the titleBlock.ts seal slot and coverSheet.ts EOR block (matching the Wyssling stamp position); when absent, render the REV A row as 'ISSUED FOR PE REVIEW' (or watermark 'PRELIMINARY — NOT FOR CONSTRUCTION') instead of 'ISSUED FOR PERMIT', and replace the instructional 'AFFIX PE STAMP HERE / LICENSE NO. ____' text with a clean empty seal area.

**Verifier notes:** Every element of the finding verified: 16 title blocks pair 'ISSUED FOR PERMIT — 2026-06-03' with an empty PE SEAL box (HTML lines 947/950 through 4156/4159); cover has the placeholder EOR block (HTML 1271-1277), General Note 4 (1323), and the Engineering Summary wet-stamp admission (1079, repeated at 3085 on the cert page). PV-0.png confirms visually; ref-p4-PV1-site.png shows the competing set carrying an actual signed Scott E. Wyssling PE11900440 Indiana stamp in the same title-block position on every sheet. Downgraded P0→P1: the absence of a seal is inherent (software cannot produce a licensed engineer's stamp — that is a business/workflow gap, e.g. a stamping-partner integration); the code defect proper is the self-contradiction of declaring the set issued-for-permit while its own notes declare it unsubmittable, plus printing fill-in-the-blank instructional text on the deliverable. Still the most visible professionalism gap vs the reference and an automatic-rejection risk in seal-requiring jurisdictions (Illinois requires SE/PE seals for structural sheets), so it should be fixed before any head-to-head comparison.

_Reported by 12 agent(s)._

### P1-2 [SET-WIDE (codes)] Stale, wrong, or nonexistent code citations throughout the set

**Description:** The package repeatedly cites sections that do not exist in its own declared code editions: (a) IFC 2021 'CH. 6 §605' on PV-0/PV-5/CERT — rooftop PV moved to §1204 (2018) / §1205 (2021); the set even contradicts itself, correctly citing §1204 in one note; (b) NEC '690.64' on PV-5's L-4 — deleted from the NEC in 2011; (c) 'Supply Side Tap — NEC 705.12(A)' on PV-0/PV-5 — 2017 numbering; NEC 2020 is 705.11 (PV-4A cites it correctly); (d) NEC Table 310.15(B)(3)(c) reproduced on PV-4B — deleted in NEC 2017; (e) the 120% rule misstated as 'backfeed breaker <= 20% of busbar' on PV-4B; (f) ASCE 7-22 paired with IBC/IRC 2021 (which adopt 7-16) plus wrong ASCE sections (§26.10 'Attachment Spacing', §26/27 for rooftop PV instead of §29.4/Ch.30, §2.3 'safety factor 2.0'); (g) UL 1741 SA cited for IQ8A, which is UL 1741 SB certified — the edition Ameren Illinois requires; (h) NEC 2020 cited 33x set-wide, unverified vs Granite City's adopted cycle. Reviewers who check citations — the exact audience — will find them wrong.

**Evidence:** HTML 1065 ('CH. 6 §605') vs 1168 ('§1204'), 3021 (CERT §605); PV-5 L-4 'NEC 705.12 / 690.64' + 'IFC §605.11' x3 (HTML 2629, 2655, 2670); HTML 1240 ('705.12(A)') vs 1919 ('705.11'); PV-4B rooftop adder table (HTML 2183-2208); HTML 2210 ('<= 20% of busbar'); PV-1/PV-2/PV-4C 'IBC 2021 ... ASCE 7-22'; PV-4A 'ASCE 7-22 §26.10'; PV-0/SCHED 'UL 1741 SA' (HTML 1065, 2773, 2929).

**Root cause:** lib/permit/sections/coverSheet.ts:261 ('CH. 6 §605'); certPages.ts:43 (Chapter 6 §605); compliancePages.ts:92 (690.64), :106/:291 (IFC §605.11), :283 (UL 1741 SA); utils/helpers.ts:101 ('Supply Side Tap — NEC 705.12(A)'); electricalPages.ts:286-331 (deleted Table 310.15(B)(3)(c) + '≤20% of busbar' + 705.12(B)(2)), :388 (705.12(A)); utils/titleBlock.ts:86 (ASCE 7-22 paired with IBC/IRC 2021 set-wide); lib/equipment-db.ts:1761-1996 & lib/equipment-registry-v4.ts:437+ (IQ8 hardcoded 'UL 1741-SA', actual cert is SB)

**Fix sketch:** Add a citation map to lib/permit/constants.ts keyed by the AHJ's declared cycles (necVersion/ifcVersion already flow into titleBlock): supplySide = '705.11' (NEC 2020+) vs '705.12(A)' (2017), rooftopAdder = '310.15(B)(2)' with the 33°C/<7/8" note (drop the deleted graduated table), fireSetback = 'IFC §1204' (2018/2021) vs '§605.11' (pre-2018), delete '690.64', restate the 120% rule as 'main breaker + 125% inverter output ≤ 120% of busbar', and emit 'ASCE 7-16' with IBC/IRC 2021 (7-22 only with 2024 codes). Replace all hardcoded strings in the section files with lookups; fix IQ8 ulListing to 'UL 1741 SB' in the equipment DBs.

**Verifier notes:** All eight sub-claims verified against the generated HTML (lines 1065/3021/2655/2670 §605 vs 1754-1756 §1204 self-contradiction; 2629/2689 'NEC 705.12 / 690.64' — 690.64 deleted in NEC 2011; 1240/2246 '705.12(A)' vs 1918-1920 '705.11'; 2183-2208 deleted NEC 2014 rooftop-adder table under NEC 2020; 2210 120% rule misstated as '≤20% of busbar', only true when main=bus rating; 35 title blocks pairing ASCE 7-22 with IBC/IRC 2021 which adopt 7-16; 2645/2773/2929 UL 1741 SA for the SB-certified IQ8A). Two finder details are overstated: IFC 2021 keeps PV at §1204 (§1205 is the 2024 renumber), and the ASCE 7-22 §2.3 governing-combination cites are legitimate. Severity lowered P0→P1: no unsafe engineering and many AHJs will pass it, but citation-checking reviewers will red-line it and the internal contradictions undercut the permit-grade claim. constants.ts:120 shows a prior §605→§1204 migration pass that missed these stragglers.

_Reported by 25 agent(s)._

### P1-3 [SET-WIDE (PV-0, PV-1, PV-2, PV-2B, PV-3, PE-1)] Roof pitch/azimuth data wrong and inconsistent set-wide; Array Parameters state a single 'Azimuth 3° (N)' for a four-plane N/S/E/W array

**Description:** PV-2B's Array Parameters and notes give ONE azimuth — '3° (N)' — and 'Rows 2' for an array whose own plane labels read N/S/W/E facing (real azimuths ~3/183/273/93); the stated azimuth is false for most of the 53 modules and will conflict with the interconnection application. PV-2's header does the same ('SHINGLE ROOF @ 3.6:12 | AZ: 3°' for a 4-pitch, 4-azimuth roof). Pitch values also disagree everywhere: cover says 4:12, planes are labeled 3.6/3.8/4/4.1:12 (physically implausible for one symmetric hip — regularizer noise), the same 18° tilt is labeled both 4:12 and 3.8:12, PV-3's section says 4:12 vs PV-2's 3.6:12, PE-1 prints the self-contradictory '4/12 (20.0°)' (4/12 = 18.4°), reported azimuths of the rectangular hip are non-orthogonal (3/89/180/273), and PV-1's north-plane modules are skewed 3.1° vs the south plane. Structural loads and production are computed per plane, so these are load-bearing inputs, not cosmetics.

**Evidence:** PV-2B.png 'Azimuth 3° (N)' + plane labels S/W/E FACING (HTML 1701, 1765); PV-2.png table azimuths 3/180/273/89 and tilts 17/18/18/19 vs labels 3.6/4/3.8/4.1:12; PV-0 'ROOF PITCH 4:12' (HTML 1039); PE-1 'ROOF PITCH 4/12 (20.0°)' (HTML 3168, certPages.ts:420); PV-1 SVG module transforms rotate(3.2) vs rotate(180.1).

**Root cause:** lib/drafting/sheetComposition.ts:353 + 385-387 (getRoofData emits planes[0] pitch/azimuth as the set-wide system value → PV-2 header/System Data); lib/permit/sections/arrayPages.ts:184-185/413/480 (roofPlane0.azimuth printed as the single 'Azimuth 3° (N)' on PV-2B); lib/permit/sections/certPages.ts:420 (Math.round(tan(20°)*12)=4 → self-contradictory '4/12 (20.0°)' on PE-1, from a project-level roofPitch scalar that also feeds PV-0's '4:12'); upstream, the roof-plane regularizer leaves noise pitches 3.6/3.8/4/4.1:12 and non-orthogonal azimuths 3/180/273/89 on one symmetric hip.

**Fix sketch:** (1) In getRoofData and arrayPages: when cad.roof.planes has >1 distinct azimuth/pitch, print 'MULTI — SEE ROOF TABLE' (or a per-plane list) instead of plane[0]'s value, and drop 'AZ: X°' from the PV-2 header for multi-plane roofs. (2) In the regularizer, snap hip-roof planes to a single shared pitch (median) and force azimuths orthogonal to the dominant ridge bearing (3/93/183/273 here), then derive the project.roofPitch scalar from that snapped value so PV-0/PE-1 agree. (3) In certPages.ts:420 print a consistent pair — e.g. keep one decimal on the ratio (`(tan(p)*12).toFixed(1)/12`) or derive both from the snapped plane pitch.

**Verifier notes:** Every cited artifact verified against the sheet PNGs and raw HTML: PV-2B Array Parameters 'Azimuth 3° (N)' + 'Rows 2' + note 'Tilt/Azimuth — 17–19° tilt / 3° (N)' while its own plane labels read N/S/W/E FACING at 3.6/4/3.8/4.1:12 (HTML 1701, 1765); PV-2 header 'SHINGLE ROOF @ 3.6:12 | AZ: 3°' (HTML 1616) and System Data PITCH 3.6:12 / AZIMUTH 3° (N) while its own roof-description table lists azimuths 3/180/273/89 and tilts 17/18/18/19; PV-0 'ROOF PITCH 4:12' (HTML 1038); PE-1 'ROOF PITCH 4/12 (20.0°)' (HTML 3168) — recomputed: 4/12 = atan(4/12) = 18.4°, and tan(20°)*12 = 4.37 rounds to 4, which is exactly what certPages.ts:420 does, so the printed pair contradicts itself; azimuths 3→180 (177° apart) and 3→89 (86° apart) are non-orthogonal for a rectangular hip — recomputed and confirmed physically implausible. Severity downgraded from P0 to P1: the correct per-plane azimuth/tilt table is present on PV-2 itself and no life-safety calculation is shown to be wrong on its face, but stating a single north azimuth for a 4-plane array misdescribes ~30 of 53 modules, will conflict with interconnection/production docs, and set-internal contradictions of this kind are a realistic plan-check rejection trigger.

_Reported by 12 agent(s)._

### P1-4 [SET-WIDE (VAL-1, CERT, PV-3, E-1, SCHED)] Internal QA/dev artifacts shipped in the customer/AHJ deliverable: VAL-1 audit sheet, raw DB slugs, engine version strings, app navigation text, incoherent dates

**Description:** The package leaks its own plumbing to the plan checker. VAL-1 is an internal validation dump shipped as sheet 15: it publishes 'No survey evidence attached to this permit run', code-path notes ('Hard-resolved from system.inverters[].strings[] — throws if missing'), internal hashes, a snow-load justification reading '0 psf is valid for warm climates' (on an Illinois job), and a layout check using a 40"x66" panel that matches neither dimension of the specified 79.9"x40.9" module. PV-3 prints the raw jurisdiction slug 'il-ameren-illinois' in near-invisible white-on-white dark-theme styling under an empty 'UTILITY ANALYSIS' header. E-1 prints 'v60.5 · 2026-04-29 | SLD SYMBOLS V2'. CERT's SLD row says the diagram is 'generated separately via Engineering → Diagram tab → Download SLD PDF' — internal UI instructions that are also false (E-1 is sheet 14 of this set); CERT is dated 2026-06-03 but the footer says 'Generated 7/3/2026', the pre-filled date renders under 'LICENSE EXPIRATION DATE', and the document ID truncates the client name ('BRAIDONMPILL'). SCHED's BOM exposes a 'Derived From' column of engine formulas ('ceil(modules/16)*2', 'inverterCount + 1') and manufacturer 'TBD'.

**Evidence:** VAL-1 HTML 3738, 3744, 3756, 3778, 3786, 3974; PV-3 HTML 1821-1829 (fill #e2e8f0 'il-ameren-illinois'); E-1 HTML 3535; CERT HTML 3032-3033, 3067, 3084 + SLD reference row vs tb-sheet-id E-1 (HTML 3290); SCHED.png DERIVED FROM column.

**Root cause:** lib/permit/generatePermit.ts:789 (VAL-1 shipped unconditionally); lib/permit/sections/validationPage.ts:106,196,343 + hash/decision dump; lib/drafting/renderContext.ts:206-217 (drawUtilityAnalysis dark-theme fills #94a3b8/#e2e8f0 on white sheet + raw utilityName slug); lib/sld-professional-renderer.ts:2607 (getBuildBadge()+' | SLD SYMBOLS V2'); lib/permit/sections/certPages.ts:81 (Engineering→Diagram-tab text) + truncated doc ID / footer date vs cert date; lib/bom-engine-v4.ts derivedFrom fields rendered verbatim in SCHED BOM

**Fix sketch:** Gate VAL-1 behind an internal/debug flag in generatePermit.ts (never in the AHJ deliverable) and make validationPage pull real module dims instead of 40x66 defaults. Fix drawUtilityAnalysis to print-black text with a humanized utility name (map slug via jurisdiction display name), strip the getBuildBadge version banner from the E-1 title block, replace certPages.ts:81 with 'See Sheet E-1 of this set', use one generation date for cert date/doc ID/footer, and drop or humanize the BOM 'Derived From' column (e.g. 'Basis: 1 per module').

**Verifier notes:** Every cited leak verified verbatim in the generated HTML: VAL-1 internals at 3738/3756/3778/3804/3970-3984 including '0 psf is valid for warm climates' on a 20-psf Illinois job and a 40"x66" layout-check panel vs the 79.9"x40.9" module specified at 2891-2892; PV-3 'il-ameren-illinois' in fill #e2e8f0 at y=297 below the dark header rect (ends y=287), i.e. light-gray on #fafafa; E-1 'v60.5 · 2026-04-29 | SLD SYMBOLS V2' at 3535; CERT internal UI instruction (false — E-1 is in the set), doc ID 'BRAIDONMPILL', cert date 2026-06-03 vs footer 'Generated 7/3/2026'; SCHED 'Derived From' column with ceil(modules/16)*2 / inverterCount + 1 / manufacturer TBD at 2813. Only soft spot: the 'date under LICENSE EXPIRATION DATE' sub-claim is a value-above-label visual adjacency, semantically correct in HTML. Reference set has no analogous internal artifacts. P1 stands: high rejection/credibility risk with the plan checker, not a safety miscalc.

_Reported by 11 agent(s)._

### P1-5 [PV-0] Sheet index lists 15 sheets while the set contains 16 — VAL-1 missing from the index

**Description:** SHEET IDENTIFICATION says TOTAL SHEETS 16 / PAGE 1 OF 16 and the HTML contains 16 title-blocked pages, but the SHEET INDEX has only 15 entries — the VAL-1 'ENGINEERING VALIDATION SUMMARY' (sheet 15 of 16) has no index entry. An index that doesn't reconcile with the declared sheet count is exactly the consistency error plan reviewers use to bounce a set (and it compounds with the index rows already clipped off the page).

**Evidence:** HTML index rows 1087-1135 = 15 entries, no VAL-1; 16 unique tb-sheet-id values including VAL-1 at HTML 3655-3659 ('SHEET 15 OF 16'); PV-0.png 'TOTAL SHEETS 16'.

**Root cause:** C:/Users/Ray/Solarpro Claude/repo/lib/permit/sections/coverSheet.ts:138-154 — hardcoded sheet-index array omits VAL-1 (ends at E-1 + conditional APP-CAD), while generatePermit.ts:789 always renders VAL-1 as sheet 15 of 16.

**Fix sketch:** Insert { id: 'VAL-1', title: 'ENGINEERING VALIDATION SUMMARY' } after the E-1 entry (coverSheet.ts:152), before the conditional APP-CAD spread, so index order matches render order. Better long-term: derive the cover index from the single SHEET_MANIFEST already defined in lib/permit/types.ts:760 instead of maintaining a duplicate hardcoded list that can drift.

**Verifier notes:** Verified against raw HTML: 16 unique tb-sheet-id title blocks with consistent "SHEET n OF 16" numbering (VAL-1 = 15 of 16 at HTML 3656-3659), but the SHEET INDEX table (HTML 1087-1135) contains only 15 rows and skips from E-1 to APP-CAD with no VAL-1 entry, while the same cover sheet declares TOTAL SHEETS 16. All three evidence claims in the finding check out exactly. Severity kept at P1: an index that fails to reconcile with the declared sheet count on the cover sheet is a standard plan-check completeness flag and a realistic bounce/resubmittal trigger, though it involves no incorrect engineering data — the fix is a one-line index addition.

_Reported by 3 agent(s)._

### P1-6 [PV-0] Cover composition materially thinner than the reference: defective vicinity map, no headline title, missing BOS equipment/notes/criteria/contact info, plus consistency nits

**Description:** Against the Wyssling cover the sheet is both incomplete and unedited. The vicinity map's site-pin label div is generated EMPTY (no 'PROJECT SITE'), with no street names, no parcel outline, and the compass/address strip clipped off the page. There is no headline title — the largest text leads with the rail brand ('PHOTOVOLTAIC IRONRIDGE XR100 SYSTEM') instead of the reference's giant 'PHOTOVOLTAIC ROOF MOUNT SYSTEM — 30 MODULES ... kWDC/kWAC'. The System Summary omits mandatory bill-of-system items for a micro job (IQ combiner, AC disconnect, RSD initiator). General notes are 5 boilerplate disclaimers vs the reference's substantive grounding/signage/clearance notes. Design Criteria omits stories, roofing layers, and roof dead load (generator supports the rows but data is never populated). The title block has no company address, phone, or contractor license. Nits: project data triplicated across three tables, monospace/proportional mix inside one table, '18.50' vs '18.497 kW' precision, and 'GRANITE CITY, IL — GRANITE CITY, Illinois' double-city strings.

**Evidence:** HTML 1336-1343 (empty aerial-pin-label div); HTML 974 (sec-hdr with racking brand); PV-0.png System Summary 5 rows vs ref 9; coverSheet.ts:232-237 (layers/stories rows gated on empty data); HTML 1368-1371 (tb-company, no contact fields); HTML 979 vs 1228 (precision); HTML ~1348 (city duplication); ref-p3-PV0-cover.png for all reference comparisons.

**Root cause:** lib/permit/sections/coverSheet.ts:93-94 (pin-label div generated literally empty — no 'PROJECT SITE' text exists anywhere in the template), :324 (sec-hdr interpolates mountLabel = racking brand → 'PHOTOVOLTAIC IRONRIDGE XR100 SYSTEM'; no headline element exists), :192-212 (summaryRows has no combiner/AC-disconnect/RSD rows), :232/236/237 (layers/stories/roof-load rows gated on project.roofLayers/stories/roofLoadPsf which arrive empty), :509-513 (hardcoded 5 boilerplate general notes), :524 (address already contains city/state, then appends ' — ${city}, ${state}' → double-city), :327-329 toFixed(2) vs :296-297 toFixed(3) precision mismatch; lib/permit/utils/titleBlock.ts:55-56 (hardcoded 'SOLARPRO ENGINEERING', zero contact/address/license fields)

**Fix sketch:** In coverSheet.ts: put 'PROJECT SITE' (or the address) inside the aerial-pin-label div; add a real headline banner 'PHOTOVOLTAIC {ROOF MOUNT} SYSTEM — {n} MODULES — {dc} kWDC / {ac} kWAC' using systemType (not mountSys) and reserve mountSys for the racking row; extend summaryRows with (N) combiner + (N) AC disconnect rows for micro topology (data exists in getEquipmentContext/electrical pages); dedupe the vicinity address strip (drop the ' — city, state' suffix when project.address already contains the city); unify kW precision to toFixed(3); constrain the vicinity map so the compass/address strip stays on-page (the f1 sec + fixed 280px img currently pushes it off). Populate stories/roofLayers/roofLoadPsf upstream in generatePermit.ts input assembly, and add company address/phone/license fields to titleBlock.ts.

**Verifier notes:** Every specific claim reproduced. HTML line 1341-1342: <div class="f-xs fw9 aerial-pin-label"> immediately closed — empty; template at coverSheet.ts:93 never had label text. HTML line 1348: '3 MELVIN DR APT A, GRANITE CITY, IL 62040 — GRANITE CITY, Illinois' double-city confirmed. PNG shows: largest text on sheet is the section header leading with the rail brand; System Summary = 5 rows (modules, micros, MSP, backfeed, racking) vs reference's 9 incl. IQ combiner, two AC disconnects, batteries, backup panel — for a 53-micro Enphase job the missing IQ combiner + AC disconnect are genuine bill-of-system omissions, not styling; Design Criteria lacks layers/stories/roof dead load which the reference lists (NUMBER OF LAYERS -01, STORY -TWO STORY); General Notes = 5 generic disclaimers vs reference's substantive grounding/signage/clearance construction notes; title block has no address/phone/license vs reference's full Cannon Solar contact block; '18.50 kW AC' (overview) vs '18.497 kW AC' (system info) on the same sheet; vicinity map's compass + address strip are clipped off the bottom page edge on the PNG; project data repeated across Project Overview, Project Information, System Information and title block. One nuance: coverSheet.ts DOES build a bucketed CONSTRUCTION NOTES section (lines 405-427), but it is pushed off-page on the rendered sheet (left column ends at the sheet index), so the reference-style substantive notes never actually appear — an overflow bug compounding the finding. Severity P1 stands: no code violation, but the cover is the first sheet an AHJ reviewer sees and it is materially below the reference standard with several outright defects (empty label, clipped content, contradictory kW values).

_Reported by 17 agent(s)._

### P1-7 [PV-1] Property line geometry is wrong: giant off-canvas parcel polygon whose only visible edge cuts through parked cars, no closed boundary, no setback dimensions

**Description:** The 'PROPERTY LINE (COUNTY GIS)' polygon spans X -1676..+1163 / Y -2716..+932 against a 408-1192 x 174-615 viewBox — a ~8.6-acre 'parcel' with zero of its 133 vertices in view. The only on-canvas sliver is a single dashed vertical line running down the middle of the neighboring parking area with vehicles on BOTH sides, and the 'MELVIN DR' street label sits inside the drawn boundary. No north/south/west boundaries are drawn and there are no array/equipment-to-lot-line dimensions, so zoning setbacks cannot be verified and it's impossible to confirm the boundary belongs to the stated APN. The reference draws the complete closed parcel around the dwelling. A site plan whose one boundary line bisects a parking lot fails any completeness check.

**Evidence:** HTML line 1538: parcel polygons stroke-dasharray '14 7', computed bounds X[-1676.6,1163.6] Y[-2716.2,932.2] vs viewBox 408.3 174.3 783.5 440.7 (0/133 points inside); PV-1.png dashed line between two rows of parked cars; ref-p4-PV1-site.png full dashed parcel rectangle.

**Root cause:** lib/permit/sections/sitePlan.ts:233-234 (inFrame gate tests full image imgW/imgH instead of the crop window, and the :224-226 design accepts clipped slivers); upstream lib/aerial/parcelBoundary.ts:87-90 (MultiPolygon takes coordinates[0][0] instead of the ring containing the query point)

**Fix sketch:** In sitePlan.ts, test parcel visibility against the crop rect (cropX..cropX+cropW, cropY..cropY+cropH) and require a minimum fraction of the ring in frame; when the parcel exceeds the crop, either zoom the site-plan crop out to enclose the full closed parcel (matching the reference) or suppress the property-line layer and its legend entry instead of drawing an unverifiable sliver. In parcelBoundary.ts, select the MultiPolygon ring that actually contains the query lat/lng. Once the closed parcel is in frame, add array/equipment-to-lot-line setback dimensions.

**Verifier notes:** All quantitative claims independently recomputed and confirmed: 133-vertex dashed parcel polygon bounds X[-1676.6,1163.6] Y[-2716.2,932.2] vs viewBox 408.3-1191.8 x 174.3-615.0, 0 vertices in view, single visible edge (1019,794)->(1008,-231) rendering as the lone dashed vertical line through the parking lot with cars on both sides (verified on PV-1.png). Point-in-polygon test confirms the MELVIN DR street label (890.8,341.4) sits inside the drawn boundary, and the ring contains a geometric spike ((-940,-2120)->(-1676,-2716)->(-906,-1133)) indicating a wrong or corrupted ring, so the boundary cannot be trusted as the stated APN's lot line. Reference ref-p4-PV1-site draws the complete closed parcel around the dwelling. One caveat: the reference also lacks numeric setback dimensions, so the 'no setback dims' sub-claim is a gap vs plan-check expectations, not vs the reference. Severity lowered P0->P1: permit-completeness/correction-letter grade, not falsified engineering data.

_Reported by 3 agent(s)._

### P1-8 [PV-1] Site plan is incomplete and uncoordinated: legend keys 1-6 never appear on the plan, all equipment 'APPROX. — FIELD VERIFY', no roof obstructions/JB/conduit, leader-text collisions, emoji in the footer

**Description:** The sidebar legend numbers equipment 1-6 with chips, but the drawing uses undefined letter tags (MSP/UM/AC) and the numbers appear nowhere — items 3, 4, and 6 (including the mandatory Enphase combiner) are never located at all. The three tags that exist are auto-stacked in a uniform column at one corner, every one flagged 'APPROX. — FIELD VERIFY' (including the NEW AC disconnect the designer is supposed to commit, and despite the note claiming 'AC disconnect within 10 feet of meter' with no dimension). The 'SITE PLAN WITH ROOF PLAN' shows no roof linework, zero vent/skylight obstructions (despite its own vent-preservation note), and no junction box or conduit route. Drafting: the MSP leader strikes through its own 'APPROX — FIELD VERIFY' text and all three leaders cross the street and parked cars; every non-module legend swatch misrepresents the drawn linework (dash patterns, colors); the footer prints a satellite emoji ('🛰️ Nearmap HD aerial') with a resolution claim that disagrees with the sheet's own scale bar; the notes are one run-on bullet strip; and the cover index calls the sheet 'EQUIPMENT LAYOUT & VICINITY' though it contains no vicinity map.

**Evidence:** PV-1 SVG: exactly 3 equipment tag rects at x=884-890, y stacked 20px apart; 3x 'FIELD VERIFY' strings; no numbered markers, obstruction symbols or JB in the element inventory; legend swatches (#6b7280 dash 4,2; rgba(30,80,200,0.60)) vs drawn styles (no dash; rgba(23,58,161,0.82)); HTML 1556 ('🛰️ Nearmap HD aerial · 7.5 cm/px' vs measured 5.8 cm/px); zoomed crop shows leader over 'APPROX.' glyphs; cover index row vs sheet title.

**Root cause:** lib/permit/sections/sitePlan.ts:347-351 (TAGS covers only 3 of 6 legend items; letters never defined in legend), :356-378 (fixed margin-column leader layout, no collision avoidance — leaders cross street and strike the APPROX sub-label), :365 + lib/permit/utils/equipmentLocator.ts:94-145 (street-side heuristic → blanket APPROX—FIELD VERIFY incl. the NEW AC disconnect), :421 (hardcoded 🛰️ + '7.5 cm/px' footer), :426-431 vs :279 (legend chips hardcoded, mismatch drawn styles), :472-483 (sidebar auto-numbers 1-6 unlinked to plan tags); obstructions/JB/conduit never routed into PV-1 aerial overlay; coverSheet.ts:140 stale 'EQUIPMENT LAYOUT & VICINITY' index title.

**Fix sketch:** Make one shared equipment list drive both sidebar and plan: extend equipmentLocator + TAGS to all 6 items (place controller/backup panel/combiner on the exterior wall adjacent to MSP), key the sidebar chips by the same tag letters, and for the (N) AC disconnect emit a dimension tie to the meter instead of APPROX. Derive legend swatch fills/dashes from the same constants used to draw (module fill rgba(23,58,161,0.82), property-line dash 14 7, white building outline), compute the printed cm/px from the actual ppm instead of the hardcoded 7.5, drop the emoji, and reuse the roofCAD obstruction/JB projection to draw them on the PV-1 aerial; add a simple label-bbox collision check that lengthens the leader past the text block. Rename coverSheet.ts:140 index row to match the sheet ('SITE PLAN WITH ROOF PLAN').

**Verifier notes:** Every sub-claim reproduced: 3 tag rects at x=884.3 (y 430/451/472), 3x FIELD VERIFY, no numbered markers/obstructions/JB/conduit in the PV-1 SVG inventory; leader-text strike recomputed geometrically (leader y 412→402 across the label's x-span, text baseline 411.4); swatch mismatches confirmed in markup; resolution recomputed from module px (28.8px/1.69m = 5.87 cm/px vs claimed 7.5); reference sheet demonstrably locates all 6+ equipment items with letter-keyed legend, roof linework, vent obstruction symbols and JB. Minor nuance: '7.5 cm/px' may describe native Nearmap capture rather than the rendered raster, and the vicinity map does exist — but on the cover sheet, so the PV-1 index title is still wrong. Severity stays P1: legend/plan disconnect (combiner box never located) and missing roof obstructions on a 'SITE PLAN WITH ROOF PLAN' are classic AHJ correction triggers, but the sheet is legible with core array/site data present, so not P0.

_Reported by 19 agent(s)._

### P1-9 [PV-2] Roof plan annotation and coordination broken: notes struck through by dimension lines, callout schedule 1-5 never anchored, plane labels covering modules, legend gaps, fabricated area calc, and no equipment/conduit/microinverters shown

**Description:** The GENERAL NOTES block is overprinted by the drawing — the 31'-6" dimension extension line strikes through four note lines and the roof corner/hip hatch hides more, leaving code-compliance notes partially illegible. The CALLOUT SCHEDULE defines items 1-5 and the legend defines a 'CALLOUT REF' bubble, but zero numbered bubbles exist on the plan (PV-2B's numbered notes 1-4 are likewise decorative), so callouts for fire setbacks, conduit and attachment zones are unlocatable. Plane label boxes are dropped on centroids covering module linework. The legend omits symbols that are drawn (JB, dashed conduit, 4-per-module attachment dots) and defines one that isn't. ARRAY & ROOF CALC prints NEW ARRAY AREA 971.67 ft2 = 53 x a default 66"x40" module (18.33 ft2) instead of 52 x the actual 19.03 ft2 Maxeon footprint — and the resulting 48.0% coverage is the exact figure the fire-setback exception hinges on. No service equipment, interconnection point, conduit route (beyond an 11-ft dashed stub 'ROUTE FIELD-VERIFIED'), microinverter symbols, or branch wiring appear, though callouts promise them; the framing hatch runs vertically even on hip-end planes where jacks run perpendicular; and callouts say 'see equipment schedule' where the reference prints full part identity on-plan.

**Evidence:** notes-collision crop (dimension line through 'PATHWAY PER', 'OBSTRUCTION(S)', 'CLEARANCES (NEARMAP'); circle inventory of PV-2 SVG: no numbered bubbles (only the view-title circle at 130,725); 971.67/53 = 18.333 = 66x40/144 exactly (roof.ts:119-120 defaults); SVG: single dashed polyline 954,505.9->950,648; 111 vertical framing lines x1=x2 across all 4 clipPaths; legend rows vs drawn JB/conduit/dots; ref-p5-PV2-roof.png shows UM/MSP/AC/QSC/BLP blocks, circuit legend, per-module micro symbols.

**Root cause:** lib/drafting/templates/roof.ts:656-691 (notes placed in left column with no reservation against the roof viewport/dimension zone), roof.ts:774 + lib/drafting/sheetComposition.ts:584-591 (CALLOUT REF legend row + numbered schedule 1-5 kept while roof.ts:788-792 deliberately replaced bubbles with text callouts), roof.ts:119-120 + 594-595 (panelLengthIn||66 / panelWidthIn||40 defaults feed arrayAreaFt2 despite named Maxeon 3 module), roof.ts:760-775 (legend omits drawn JB/dashed-conduit/attachment dots), roof.ts:530-537 (plane-label dodge tests only panel points in a 40x26 window, smaller than the label box)

**Fix sketch:** (1) Reserve the left data-column rectangle (tables + measured GENERAL NOTES height) when computing the roof drawing viewport and dim margins so linework can never enter it. (2) On PV-2, delete the CALLOUT SCHEDULE panel and the CALLOUT REF legend row (the sheet intentionally uses direct text callouts), or alternatively emit anchored numbered bubbles at the referenced features; add legend rows for JB, dashed conduit, and attachment dots. (3) Plumb the selected module's real dimensions into project.panelLengthIn/WidthIn (Maxeon 3 400W = 66.5x41.2 in, 19.03 ft2) so NEW ARRAY AREA/coverage stop printing the 66x40 default (971.67 ft2 / 48.0% should be ~1008.4 ft2 / ~49.8%), and enlarge the plane-label collision window to the actual label box against module rects.

**Verifier notes:** Verified on PNG + SVG + code. Notes strike-through confirmed in zoomed crop (dim tick through 'PROVIDE EAVE-TO-RIDGE ROUTES', roof corner/hatch over notes 3-4). SVG digit/circle inventory: zero numbered callout bubbles on the plan (only roof-table row numbers, view-title '1', scale-bar '5'), while the side panel defines items 1-5 and the legend defines a '#' bubble. Area math recomputed: 971.67/53 = 18.3333 = 66x40/144 exactly, proving defaults were used for a Maxeon 3 (19.03 ft2) job; printed 48.0% coverage understates actual ~49.8%. Reference ref-p5 demonstrably better: legend defines JB/micro/rail/attachment/rafters/pathway, per-module micro symbols + colored circuit wiring + UM/MSP/AC/BLP/QSC blocks on-plan, and its area calc uses the real module footprint (630.58/30 = 21.02 ft2 = actual Jinko 425W). PLANE 3 label box visually overlaps a module rectangle. One finder sub-claim REJECTED: '52 x actual' module count — the sheet consistently says 53 and 53x400W = 21.20 kW is internally consistent; no 52 appears in the HTML. Framing-hatch-direction-on-hip-planes sub-claim not independently verified (secondary). Severity stays P1: multiple coordination/legibility defects plus an understated coverage figure on the primary permit roof plan, but design data itself is intact.

_Reported by 22 agent(s)._

### P1-10 [PV-3] Attachment detail is not a buildable construction detail: no fastener drawn anywhere, incoherent cross-section with colliding/garbled dimensions, no Bill of Materials, and half the sheet blank

**Description:** DETAIL 1/PV-3 — the sheet's entire purpose — is seven flat full-width rectangles: no lag bolt, thread, washer, L-foot profile, clamp, flashing lap, or embedment dimension is drawn (the reference draws screws with a dimensioned 1.75" MIN EMBEDMENT, clamp assembly and butyl base). The lower-left cross-section shows a horizontal array block floating over a sloped roof triangle, painted over the '4:12 SLOPE' text and two rafters; the '24" RAFTER SPACING (TYP.)' dimension physically spans 6 feet, overprinting a redundant '2'-0" O.C.' dimension into an illegible knot; callout bubbles 2-5 overlap into an unreadable stack. There is no Bill of Materials anywhere — although PV-0's index promises 'ATTACHMENT DETAIL — MOUNTING & BILL OF MATERIALS' — vs the reference's fully quantified BOM (rails, splices, clamps, 96 attachments, lugs, part numbers); ~45% of the sheet is empty white space where it should go. An inspector cannot verify the assembly and a crew cannot order material from this sheet.

**Evidence:** pv3-detail2.png (rectangles only, callout blob, ghost 'ATTACHMENT DETAIL' caption, stray wind arrow); SVG dim coords: '24" RAFTER SPACING' x=78.7-294.7 vs 36px/ft scale, overlapping '2'-0" O.C.' at y374-387; bubble centers 7-11.5px apart with r=9; HTML 1104 (index 'BILL OF MATERIALS') vs 1804 (title 'MOUNTING & CROSS-SECTION'); ref-p6-PV3-attach.png BOM + dimensioned blow-up; PV-3.png blank below y~810.

**Root cause:** repo\lib\drafting\templates\roof.ts:997-1053 (rect-only layer stacks, no fastener geometry), roof.ts:1051 (callout bubbles at zy+h/2 with r=9 vs adjacent-center gaps of 6-8.5px → overlap), roof.ts:1074-1086 (full-run dimension mislabeled with single-bay 24" value, overprinting redundant 2'-0" O.C. dim 14px above), roof.ts:989-1014 (flat horizontal stack painted over sloped-roof triangle and the 4:12 SLOPE text at :967); no mounting-BOM builder in lib\permit\sections\structuralPages.ts (PV-3, title at :35) while index title promises one via lib\permit\utils\helpers.ts:48 and lib\system\systemVocabulary.ts:241

**Fix sketch:** In roof.ts draw a real lag-bolt in the detail circle (hex head + washer above the L-foot, threaded shank penetrating flashing/sheathing into the rafter, with a drawn embedment dimension); space callout bubbles at a fixed >=20px pitch with bent leaders instead of zy+h/2; label the overall dimension with the true span (nBays x 24") and offset/remove the redundant one-bay dim; shift the layer stack off the slope-text footprint. Add a mounting BOM table (rails/splices/mid+end clamps/attachments/lugs with layout-derived quantities, mirroring the reference) into the blank lower half of PV-3 in structuralPages.ts, and make the sheet title match the index ("MOUNTING & BILL OF MATERIALS").

**Verifier notes:** All core claims verified: detail is 7 flat rectangles with no fastener drawn (reference draws dimensioned 2X12-14 screws w/ 1.75" MIN EMBEDMENT + clamp assy); bubble overlap recomputed (centers 6-8.5px apart, r=9); dim knot confirmed on PNG and in code (full-run line labeled 24"); index row (HTML:1104) vs sheet title (HTML:1804) mismatch confirmed; bottom ~45% of drawing zone blank. One overstatement: a package-level BOM does exist on the Equipment Schedule sheet (HTML:2813, 22 line items), but it contains zero racking/mounting hardware (no rails, clamps, attachments, lag bolts, or even modules), so the mounting BOM promised for PV-3 is genuinely absent from the package. Embedment is stated textually (spec table + 2.5" MIN. EMBED vertical dim) but never drawn on a fastener. P1 stands: not a data-integrity P0, but a likely plan-review redline.

_Reported by 14 agent(s)._

### P1-11 [SCHED] Bill of Materials cannot build the system: garbage part numbers, modules and the entire racking system omitted, order-of-magnitude quantity errors, wrong fuse class, and cross-sheet conduit/equipment contradictions

**Description:** Row 12's PART NUMBER is the corrupted string 'THWN2-32 THWN-2 + 1#8 AWG GND in 1-1/2" EMT' (the same '3#2'->'32' mangling as E-1) and specifies 1-1/2" EMT while row 4 buys 3/4" EMT for the same home run. The 22-line 'Full Equipment Schedule' contains NO line for the 53 modules and NOTHING for the IronRidge XR100 racking (no rails, attachments, clamps, flashings — 'Stage 5 Structural' is only grounding hardware, and stages jump 1,2,4,5,6,7), no AC combiner or 20A breakers though E-1 draws them (E-1's equipment panel answers 'AC Combiner' with 'AC Trunk Cable', a category error) — while the summary falsely claims quantities are 'derived from CAD geometry ... no manual estimates'. Quantities that do exist are wrong: Q Cable qty 4 'sections' at '1 per 16 microinverters' (real ordering is ~53 connector drops, and the per-16 premise is the same wrong IQ8 branch limit), 54 'Disconnecting Means Labels' via 'inverterCount + 1', 69 ft of conduit vs E-1's 85 ft of scheduled runs, a single ground rod with no 250.53(A)(2) supplement, a 'Backfeed Breaker Label' for a design whose SLD says no backfed breaker exists. Data errors: fuse described Class RK5 with a Class RK1 part number (LLNRK125SP), invented Enphase SKU 'IQ8A-72-M-US' (real: IQ8A-72-2-US), micro rated 0.35 kW in the table vs '0.366kW' in row 1 (vs 349W on E-1), junction boxes from manufacturer 'TBD', AC trunk cable staged as 'DC Wiring' per NEC 690.31, and the OCPD formula cited to the nonexistent '690.8(A)(1)x(B)(1)'.

**Evidence:** HTML 2813 (all rows: 'THWN2-32...' PN, Q-12-10-240 qty 4 'ceil(deviceCount/16)', label qty 54 'inverterCount + 1', LLNRK125SP 'Class RK5', TBD, IQ8A-72-M-US, stages); row 4 vs row 12 conduit conflict; E-1 schedule 50+20+15 ft vs 69 ft purchased; HTML 3450-3451 ('AC Combiner -> AC Trunk Cable'); summary text 'derived from CAD geometry'; bom-engine-v4.ts:363, :723-730, :1146-1149.

**Root cause:** lib/permit/utils/bomForPermit.ts:370 (full conductor callout passed as acWireGauge) + lib/bom-engine-v4.ts:729-730 (naive replace builds mangled PN), :359 (microPerBranch=16 hardcoded; IQ8A limit is 11), :866-870 (RK5 text on RK1 LLNRK PN), :1142-1149 (unconditional backfeed label; inverterCount+1=54 disc labels), :727 (fallback 60ft×1.15=69ft because bomForPermit never populates input.runs, contradicting E-1's 85 ft); lib/bom-system-profiles.ts:111-115 (roof structural returns empty "V4 owns it") + bomForPermit.ts:393 (project.rackingId unset) = racking emitted nowhere; lib/equipment-registry-v4.ts:818,822 (IQ8A-72-M-US SKU, 0.366kW peak vs 349W continuous on E-1)

**Fix sketch:** In bomForPermit: parse only the gauge out of acConductorCallout (regex /#?\d+(\/0)?\s*AWG/ on the first conductor) before passing as acWireGauge, and populate BOMGenerationInputV4.runs from the same run schedule E-1 renders so footage/conduit sizes single-source. In bom-engine-v4: read maxPerBranch from the inverter registry entry (IQ8A=11), gate the backfeed-breaker label on interconnectionMethod!=='SUPPLY_SIDE', count disconnect labels per disconnect (not per micro), and fix the fuse line to Class RK1/LLNRK (or FLNR if RK5 intended). For racking, default rackingId from the racking shown on PV-3 (IronRidge XR100) and emit rail/attachment/clamp quantities from attachmentCount/railSections instead of returning empty for roof in bom-system-profiles.

**Verifier notes:** Verified nearly every sub-claim verbatim in the HTML (line 2813 BOM table, ~3450 'AC Combiner -> AC Trunk Cable', E-1 schedule 50+20+15=85ft of 3/4" EMT vs BOM's 69ft and row-12's embedded 1-1/2" EMT vs row-4's 3/4" on the same sheet). Math recomputed: 60×1.15=69, 60×4×1.15=276, ceil(53/16)=4 (correct IQ8A branch limit is 11 -> 5 branches; Q cable actually ordered per connector ~53), 53+1=54 labels. Interconnection is supply-side tap per the sheet's own placards, so the backfeed-breaker label is contradictory. One overstatement: modules are NOT omitted from the sheet — renderBOMTable (structuralPages.ts:1037) deliberately skips solar_panel because a 'Solar Modules' qty-53 table renders above it; racking however is genuinely absent from the entire package BOM despite IronRidge XR100 appearing 10x elsewhere. IQ8A-72-M-US vs real IQ8A-72-2-US could not be web-verified but LLNRK=RK1 (not RK5) is a standard Littelfuse catalog fact. Re-assessed P0->P1: the sheet is internally contradictory and unusable for procurement, and the 'no manual estimates' summary claim is false, but modules/inverters are correctly scheduled above and most errors are supplementary-schedule quality issues rather than code-compliance failures; the same conductor-string mangling on E-1 (reviewer-facing) is the P0-carrying instance.

_Reported by 19 agent(s)._

### P1-12 [APP-A] Module spec block is generic/fabricated data under the 'SunPower Maxeon 3 400W' name (Vmp x Imp = 388W ≠ 400W)

**Description:** Multiple values are wrong for the named SKU and internally inconsistent: printed Vmp 62.7 V × Imp 6.19 A = 388.1 W, 3% below the 400 W STC rating two rows up (real SPR-MAX3-400: Vmp 65.8 V, Imp 6.08 A = 400 W). Cell type 'Monocrystalline PERC / TOPCon' is flatly wrong — Maxeon 3 is SunPower's interdigitated back-contact (IBC) cell, its defining technology. Dimensions 79.9" × 40.9" (2029×1039 mm) and 'Module Efficiency 19.0%' describe a generic 72-cell commercial module (real Maxeon 3 400W: ~1690×1046 mm at 22.6%; 19.0% is exactly 400W over the wrong area). Weight 44 lbs (real ~41.9). These values propagate to the NEC 690.7/690.8 numbers used across the electrical sheets; an AHJ or installer comparing this sheet to the actual datasheet will find the equipment reference unreliable.

**Evidence:** APP-A.png PV MODULE tables: Pmax 400 Wp, VMP 62.7 V, IMP 6.19 A (product = 388.1 W), LENGTH 79.9" (2029mm), WIDTH 40.9" (1039mm), EFFICIENCY 19.0%, CELL TYPE 'Monocrystalline PERC / TOPCon'. HTML lines 2877-2896, 2909-2910 confirm values.

**Root cause:** C:/Users/Ray/Solarpro Claude/repo/lib/permit/sections/compliancePages.ts:360-365, 392, 402 (pageSpecSheetReference)

**Fix sketch:** Stop fabricating specs in pageSpecSheetReference: use the real panelVmp/panelImp already in the data model (lib/computed-system.ts:71-72, 344-345) instead of vmp = voc*0.83 / imp = isc*0.94; back it with a per-SKU module spec table (Vmp, Imp, dims, weight, cell tech, temp coeffs) keyed by manufacturer+model so Maxeon 3 400W renders 65.8V/6.08A, 1690x1046mm, 41.9 lbs, 22.6%, IBC. Derive efficiency from the actual dims and replace the hardcoded 'Monocrystalline PERC / TOPCon' cell type with the SKU value (or omit when unknown).

**Verifier notes:** Verified on APP-A.png and HTML lines 2877-2910: Vmp 62.7 V x Imp 6.19 A = 388.11 W, contradicting the 400 Wp printed two rows above. Root cause is explicit fabrication in code: vmp = voc*0.83 (75.6*0.83 = 62.748 -> 62.7) and imp = isc*0.94 (6.58*0.94 = 6.185 -> 6.19) at compliancePages.ts:360-361; dimensions/weight are generic fallbacks 79.9/40.9/44 (lines 363-365); efficiency 19.0% is computed at line 392 as Pmax over that generic area (400/(2.029*1.039 m2) = 18.97%); 'Monocrystalline PERC / TOPCon' is hardcoded at line 402 and is wrong for Maxeon 3 (IBC). Real SPR-MAX3-400: Vmp 65.8 V, Imp 6.08 A, ~1690x1046 mm, 41.9 lbs, 22.6%. One finder exaggeration: the propagation claim is overstated — the NEC 690.7/690.8 max values on this sheet (94.5 V, 8.22 A) derive from Voc 75.6 V and Isc 6.58 A, which ARE correct for the real SKU, and this is an Enphase micro system so the fake Vmp/Imp do not drive string sizing on other sheets. Hence P1 not P0: fabricated datasheet values under a real SKU name on a permit sheet are an AHJ-rejection/credibility defect (the 388 W internal inconsistency is reviewer-catchable), but not an unsafe-design defect.

_Reported by 4 agent(s)._

### P1-13 [APP-A vs VAL-1 + PV-3] Module physical dims contradict layout dims used by the planset (79.9" vs 66")

**Description:** APP-A states module Length 79.9" (2029mm) × Width 40.9" / 44 lbs, but VAL-1's 'Layout Dimensions Verified' PASS row states the layout was drawn with 'Panel: 40" × 66"' and PV-3's attachment callout says 'PV MODULE — 66" × 40" @ 45 LBS'. That is a 14-inch (~21% area) discrepancy in the long dimension between the equipment spec and the roof layout/attachment sheets in the same package. The real Maxeon 3 400W is ~66.3" × 40-41", so the layout is right and the spec sheet is wrong — but as submitted the sheets disagree on the physical size of all 53 modules, invalidating area, dead-load, and setback math on its face, and the validation gate stamped PASS on one of the conflicting values.

**Evidence:** APP-A.png Length 79.9" (2029mm) (HTML 2891-2892) vs VAL-1.png 'L=47.9 ft × H=11.3 ft | Panel: 40" × 66" | Row Spc: 5.58 ft' (HTML 3778) and PV-3.png callout '66" × 40" @ 45 LBS'.

**Root cause:** C:/Users/Ray/Solarpro Claude/repo/lib/permit/sections/compliancePages.ts:363-365 — `project.panelLengthIn || 79.9`, `panelWidthIn || 40.9`, `panelWeightLbs || 44`. When the project record has no panel dims (this job), every sheet falls back to its OWN default: APP-A uses 79.9"/40.9"/44lbs, while PV-3's attach callout (lib/drafting/templates/roof.ts:918-920) uses `|| 66` / `|| 45`, VAL-1 + layout use 66 (lib/permit/sections/sitePlan.ts:63, lib/cad/roof/roofCAD.ts:36 DEFAULT_PANEL_LENGTH_IN=66), plus two more divergent strays: generatePermit.ts:321 `|| 65` and lib/permit/utils/canonical.ts:315 `|| 66.9`.

**Fix sketch:** Single-source the module footprint: add one resolver (e.g., in lib/permit/utils/canonical.ts next to buildLayoutDimensions) that returns {lengthIn, widthIn, weightLbs} from project fields, else from an equipment lookup keyed by panelModel, else ONE shared default (66 × 40 × 45 — matching the CAD footprint). Replace the divergent literals at compliancePages.ts:363-365, roof.ts:918-920, sitePlan.ts:63, generatePermit.ts:321, canonical.ts:315 with calls to it. Ideally seed real Maxeon 3 400W dims (66.5" × 41.2" / 1690×1046mm, 41.9 lbs) in the equipment table so APP-A matches the stated model.

**Verifier notes:** Verified in the generated HTML: APP-A line 2891-2892 "Length 79.9" (2029mm)" / "Width 40.9"" / "44 lbs" for a SunPower Maxeon 3 400W; PV-3 line 1819 "PV MODULE — 66" × 40" @ 45 LBS" (plus "MODULE (66" × 40")" in the detail legend); VAL-1 line 3778 "Panel: 40" × 66"" stamped PASS. The finder's facts are accurate — the package internally contradicts itself on module size (79.9 vs 66, 21% area) and weight (44 vs 45 lbs), and the real Maxeon 3 is ~66.5" long, so APP-A is the wrong sheet. APP-A's derived Module Efficiency (19.0%) is also corrupted by the wrong area (real Maxeon 3 400W is ~22.6%). Severity re-assessed P0→P1: the layout, setback, and dead-load sheets all use the CORRECT ~66" footprint, so the governing engineering math is not actually invalidated — the defect is a self-contradicting reference appendix that risks a plan-check correction letter, not wrong structural numbers. The finder's claimed suspect (generatePermit.ts spec resolver pulling a generic record) is close but not exact: there is no spec DB pull for physical dims at all — just five uncoordinated hard-coded fallbacks that only agree when project.panelLengthIn happens to be set.

_Reported by 3 agent(s)._

### P1-14 [APP-A vs PE-1 + PV-3 + PV-4C] Lag bolt 5/16" contradicts the 3/8" minimum required on PE-1, PV-3, and PV-4C

**Description:** APP-A's racking table specifies 'LAG BOLT 5/16" × 3" Stainless Steel', while PE-1 requires 'LAG DIAMETER 3/8" min.', PV-3's attachment specs require 'LAG BOLT: 3/8" DIA × 3" MIN SS', and PV-4C's detail note reads 'Lag bolt: 3/8" diameter minimum stainless steel, 2.5" minimum embedment'. The package specifies purchasing/installing an attachment smaller than its own stated structural minimum — the 500 lb withdrawal capacity and 2.38 safety factor on PE-1/PV-4C are computed for a diameter APP-A contradicts, so installing the 5/16" hardware would void the PE structural analysis. Also note a 3" lag through rail foot + flashing + 0.5" sheathing cannot reliably achieve 2.5" rafter embedment.

**Evidence:** APP-A.png 'LAG BOLT 5/16" × 3"' (HTML line 2945) vs PE-1.png 'LAG DIAMETER 3/8" min.' (HTML line 3170), PV-3.png 'LAG BOLT 3/8" DIA × 3" MIN SS', and HTML line 2485 (PV-4C detail '3/8" diameter minimum').

**Root cause:** lib/permit/sections/compliancePages.ts:451 (APP-A hardcodes 5/16") vs lib/permit/sections/certPages.ts:458 (PE-1 hardcodes 3/8" min), lib/permit/sections/structuralPages.ts:775 & 792 (PV-3/PV-4C hardcode 3/8"), lib/permit/buildPermitCoverSheet.ts:763 and lib/permit/utils/titleBlock.ts:129 (general notes hardcode 3/8" min) — six independent hardcoded literals, no single source.

**Fix sketch:** Single-source the lag diameter: pass the racking system's fastenerDiameterIn (already in lib/mounting-hardware-db.ts, 0.3125 for the L-foot/FlashFoot family) into the permit render context, format it as a fraction (0.3125 → 5/16"), and use that one value in all six locations (compliancePages 451, certPages 458, structuralPages 775/792, buildPermitCoverSheet 763, titleBlock 129). The 3/8"-min boilerplate should change to the actual analyzed diameter, since structural-calc.ts:585 computes capacity with the 5/16" NDS value (246 lb/in).

**Verifier notes:** Contradiction verified directly in the raw HTML (line 2945 "5/16\" × 3\" Stainless Steel" vs 3170 "3/8\" min.", 2468 "3/8\" DIA.", 2485 "3/8\" diameter minimum"). However, the finder's severity rationale is inverted: the structural engine's 984-lb-class withdrawal math is computed for a 5/16" lag (structural-calc.ts:567-588, NDS Table 12.2A tabulated 246 lb/in), so APP-A's 5/16" hardware matches the analysis and installing it would not void the PE calc — the 3/8"-min text on PE-1/PV-3/PV-4C is the erroneous boilerplate. Real-world impact is plan-review rejection / document credibility, not a structural safety gap, hence P1 not P0. The embedment aside in the finding is not a defect: the engine explicitly assumes 3" lag − 0.5" sheathing = 2.5" rafter embedment, the industry-standard model.

_Reported by 4 agent(s)._

### P1-15 [CERT] Wrong IFC citation: 'IFC 2021 Chapter 6 §605' — solar PV moved to Chapter 12 (§1204/1205) in IFC 2018+

**Description:** The certification's applicable-codes list cites 'International Fire Code (IFC) 2021 — Chapter 6 §605 Solar Photovoltaic Systems'. In the 2021 IFC, photovoltaic systems live in Chapter 12 (Section 1204/1205); §605 was the PV location only through the 2015 edition (the Wyssling reference correctly pairs 605.11 with its 2012 IFC). A certification a PE signs that cites a nonexistent section of the claimed code edition is factually wrong, undermines the fire-pathway compliance story on the roof-plan sheets, and gets flagged at intake. The same stale §605 citation appears on at least 5 other sheets.

**Evidence:** CERT.png codes bullet list, 5th bullet; HTML line 3021: '<li>International Fire Code (IFC) 2021 — Chapter 6 §605 Solar Photovoltaic Systems</li>'; same §605 string at HTML lines 1065, 1168, 2629, 2655, 2689. ref-text.txt line 194 uses '2012 INTERNATIONAL FIRE CODE' with 605.11.3.1(1) — correct for that edition.

**Root cause:** repo/lib/permit/sections/certPages.ts:43 (CERT bullet 'Chapter 6 §605'), repo/lib/permit/sections/coverSheet.ts:261 ('CH. 6 §605'), repo/lib/permit/sections/compliancePages.ts:106, 291, 305 (bare 'IFC §605.11')

**Fix sketch:** Add an edition-aware IFC PV-section helper in lib/permit/constants.ts (pre-2018 → '§605.11'; 2018/2021 → '§1204'; 2024 → '§1205') and use it at certPages.ts:43, coverSheet.ts:261, and compliancePages.ts:106/291/305, matching the already-migrated wording in arrayPages.ts:426-428 and titleBlock.ts:116. For this package (IFC 2021) the CERT bullet becomes 'International Fire Code (IFC) 2021 — Chapter 12 §1204 Solar Photovoltaic Power Systems' and the compliance rows cite 'IFC §1204.2'.

**Verifier notes:** Verified in the generated HTML: line 3021 (CERT) and line 1065 (cover codes table) pair 'IFC 2021' with Chapter 6 §605, and compliance/inspection rows at lines 2629/2655/2670/2689 cite §605.11 under an IFC 2021 title block. In IFC 2018+ PV moved to Chapter 12 §1204 (2024: §1205), so the citation does not exist in the claimed edition — and certPages.ts even emits 'IFC 2024 — Chapter 6 §605' for NEC-2023 jobs, doubly wrong. The package is internally inconsistent: PV-2's fire-setback block (HTML 1754-1756, from arrayPages.ts) and the titleBlock note (HTML 1168) already cite §1204 correctly, which makes the stale CERT/cover citations stand out at intake. Finder over-counted slightly: HTML line 1168 is a correct '(§605.11 in pre-2018 editions)' parenthetical, not a defect. Downgraded P0→P1: it is a citation error under a PE seal risking a plan-check correction, but the actual setback design cites and applies §1204 correctly, so no safety/design impact.

_Reported by 2 agent(s)._

### P1-16 [CERT] Certification asserts full compliance while PE-1 certifies non-compliance

**Description:** CERT's statement reads 'I hereby certify that the solar photovoltaic system design ... complies with the following applicable codes and standards', with ASCE 7-22 and IBC/IRC in the list. Thirteen pages later, PE-1 states the modeled framing 'does not satisfy all code checks' and must not be issued. The package asks an engineer to sign two mutually exclusive certifications — an explicit rejection trigger.

**Evidence:** CERT.png certification paragraph vs PE-1.png certification statement + DO NOT ISSUE box (HTML 3209-3212).

**Root cause:** repo/lib/permit/sections/certPages.ts:34-45 (pageEngineerCert) — the "complies with ASCE 7-22 / IBC / IRC" certification is unconditional static template text that never reads compliance.structural, while pagePELetterRoof in the same file computes _allPass (line 401) and conditionally emits the "STRUCTURAL REVIEW REQUIRED — DO NOT ISSUE" box (lines 486-514).

**Fix sketch:** Extract the _bendPass/_deflPass/_lagPass/_allPass computation (certPages.ts:391-401) into a shared helper, e.g. structuralAllPass(input.compliance.structural), and call it in pageEngineerCert. When it returns false, qualify the CERT statement ("...complies with the codes listed below, EXCEPT as noted on Sheet PE-1 — structural review required prior to issuance") and add a red banner mirroring PE-1's DO NOT ISSUE; also swap the Rev A description/title-block "ISSUED FOR PERMIT" to "NOT FOR ISSUE — STRUCTURAL REVIEW PENDING" so the package is consistently gated.

**Verifier notes:** Both statements verified on CERT.png and PE-1.png and in the raw HTML (line 3013 vs 3202/3209). PE-1 numbers check out: deflection 0.868 in vs 0.600 in allowable = 145% (fail), bending 89% (pass), lag SF 2.38 (pass), so _allPass=false and the failure branch fired correctly on PE-1 — only CERT ignores it. Downgraded P0→P1: the CERT is an unsigned template and PE-1's DO NOT ISSUE already blocks issuance, so this is an internal-consistency defect (still an AHJ rejection trigger and a false-certification trap) rather than a silent failure. Note the code comment at certPages.ts:387-390 shows the conditional-certification principle was deliberately applied to PE-1 but never propagated to the CERT sheet.

_Reported by 2 agent(s)._

### P1-17 [CERT / PE-1 / PV-0] No PE seal or signature anywhere; package states a stamp is required

**Description:** Every seal provision is an empty placeholder: PV-0 'AFFIX PE STAMP HERE', CERT 'AFFIX SEAL HERE' wet-stamp box with 7 blank signature lines, PE-1 'AFFIX SEAL HERE' with blank name/license/date/signature, and every title block has an empty 'PE SEAL' box. PV-0 General Note 4 says 'PE STAMP REQUIRED FOR PERMIT SUBMISSION PER AHJ' and CERT's footer says 'requires engineer review and wet stamp before AHJ submission' — the package documents its own incompleteness. The reference package contains an actually signed PE letter (Scott E. Wyssling, PE11900440, 'Signed 8/14/2025'). As shipped, the AHJ receives an uncertified structural analysis that additionally says DO-NOT-ISSUE.

**Evidence:** PV-0.png Engineer of Record box 'AFFIX PE STAMP HERE / LICENSE NO. ___ / STATE: ___'; CERT.png all fields blank except license-expiration; PE-1.png blank PE block; ref-text.txt p2 lines 143-161 signed PE with license number and date.

**Root cause:** lib/permit/sections/coverSheet.ts:484 (AFFIX PE STAMP placeholder; also :393 wet-stamp disclaimer, :512 Note 4), lib/permit/sections/certPages.ts:64/107/140-142 (CERT seal box + footer), lib/permit/utils/peLetter.ts:24-26 (PE-1 seal block), lib/permit/utils/titleBlock.ts:79 (empty PE SEAL box on every sheet) — all are hardcoded placeholders with no engineer-of-record data path.

**Fix sketch:** Add an optional engineerOfRecord field (name, license no., state, seal-image data URI, signed date) to the permit input model and thread it through generatePermit into coverSheet.ts, certPages.ts, peLetter.ts, and titleBlock.ts: render the actual seal/signature when present, placeholder otherwise. When unsealed, change the revision-block status from 'ISSUED FOR PERMIT' to 'ISSUED FOR PE REVIEW' so the package no longer contradicts its own disclaimers.

**Verifier notes:** Every factual claim verified: PV-0.png and HTML lines 1079/1277/1323 show the empty stamp box, Note 4, and the wet-stamp disclaimer; CERT/PE-1 blank seal blocks at HTML 3042/3085/3235; the reference package contains an actually signed PE letter (Scott E. Wyssling, PE11900440, Signed 8/14/2025, ref-text.txt). The internal contradiction is real: the revision block says 'ISSUED FOR PERMIT' while three separate notes say the document may not be submitted without a wet stamp. Downgraded P0→P1 because the blank seal is not erroneous generator output — software cannot legally affix a PE seal, and the reference is a post-review deliverable, so any freshly generated package will necessarily be unsealed. The actionable defects are the missing EOR/seal injection capability and the ISSUED-FOR-PERMIT vs do-not-submit labeling contradiction; treated as a product-stage gap plus a fixable labeling bug rather than a fatal code error. If judged strictly as 'can the AHJ accept this file today,' the impact is P0, but that reading indicts the product stage, not the sheet content.

_Reported by 1 agent(s)._

### P1-18 [PE-1] Allowable bending moment 1246 lb-ft impossible for stated 2x6 at F'b 1521 psi — true utilization ~116% (a second failing check)

**Description:** With the sheet's own inputs — 2x6 (S = 1.5 × 5.5²/6 = 7.5625 in³) and adjusted stress F'b = 1521 psi — allowable moment = 1521 × 7.5625 = 11,502 in-lb = 958.5 lb-ft, not the printed 1246.0 lb-ft (which implies S ≈ 9.83 in³, matching no standard 2x6). At the printed demand (1114.1 lb-ft, which does check out: 61.9 lb/ft × 12² / 8), bending utilization is ~116%, i.e. FAILING, not the printed 89%. The deflection math IS consistent with a true 2x6 (Δ = 5wL⁴/384EI with E = 1.6e6, I = 20.8 in⁴ gives exactly 0.868 in), so the bending allowable is the outlier — either an extra ~1.30 factor was applied after F'b was already adjusted, or the wrong section modulus was used. An engineer reviewing this letter will catch the arithmetic in one line.

**Evidence:** PE-1.png Rafter Bending & Deflection Analysis: F'B (ADJUSTED) 1521 psi, LINE LOAD 61.9 lb/ft, RAFTER SPAN 12.0 ft, Bending Moment '1114.1 / 1246.0 lb-ft', Bending Utilization 89% (HTML 3185-3188). Recomputed by hand.

**Root cause:** lib/permit/generatePermit.ts:357-359 (hardcoded Fb_prime = 1150*1.15*1.15 = 1521, disconnected from engine) + lib/structural-engine-v4.ts:281-286 and 482-486 (NDS_FB table stores size-specific Fb values, e.g. DF-L 2x6 = 1150, yet CF_b = 1.3 is multiplied in again — size factor double-applied, inflating Fb' to 1977 psi and capacity to 1246 ft-lb)

**Fix sketch:** In structural-engine-v4.ts, use true NDS base values (DF-L No.2 Fb = 900 for all dimension sizes) with CF_b applied once — or keep the size-specific table and set CF_b = 1.0. Expose Fb_prime (and the factor breakdown) on RafterAnalysis, then in generatePermit.ts:357-359 populate Fb_base/Cd/Cr/Fb_prime from the engine result instead of hardcoded literals so the printed F'b, allowable moment, and utilization are arithmetically consistent.

**Verifier notes:** All math verified by hand. Printed demand 1114.1 ft-lb is correct (61.9 x 12^2/8). Printed allowable 1246.0 ft-lb is exactly 1.30x the value implied by the sheet's own F'b (1521 x 7.5625 in^3 = 958.5 ft-lb); 1.30 = CF for 2x6, confirming the double-applied size factor. True bending utilization at stated F'b is 116% (failing), and even the most generous legitimate NDS reading (No.1 grade, CF once) gives 103% — 89% is unreachable. Deflection math independently confirms a real 2x6 (0.868 in exact), isolating bending allowable as the outlier. Severity note: this sheet already says DO NOT ISSUE (deflection 145%), so the overall verdict is not flipped here, but on projects where deflection passes and true bending is 90-115% this prints a passing PE letter for a failing rafter; the internal inconsistency is also one-line-catchable by any plan reviewer. P1 confirmed.

_Reported by 2 agent(s)._

### P1-19 [PV-0 / PV-2 / PE-1] Roof pitch stated three ways across the set: 4:12, 3.6:12, and '4/12 (20.0°)'

**Description:** PV-0 Design Criteria: 'ROOF PITCH 4:12'. PV-2 headline and system data: '3.6:12' (planes 3.6/4/3.8/4.1:12, tilts 17-19°). PE-1: '4/12 (20.0°)' — matching neither of the other sheets (and internally wrong, see the conversion finding). The structural sheet analyzed a single pitch while the roof has four planes at 17-19°; pitch feeds the snow slope-reduction (13.2 psf roof snow on PV-4C) and wind calcs, so the structural inputs are ambiguous across the set.

**Evidence:** PV-0.png 'ROOF PITCH 4:12'; PV-2.png 'PITCH 3.6:12' + plane labels 3.6/4/3.8/4.1:12; PE-1.png 'Roof Pitch 4/12 (20.0°)'. HTML contains all three strings.

**Root cause:** lib/permit/sections/coverSheet.ts:52-53 and lib/permit/sections/certPages.ts:420 both derive pitch from project.roofPitch (20°) with integer rounding ("4:12" / "4/12 (20.0°)"), while lib/drafting/sheetComposition.ts:365-372,385 derives PV-2's headline from cad.roof.planes[0].pitch (~16.7°) with one-decimal rounding ("3.6:12"). certPages.ts:420 additionally pairs the rounded ratio with the unrounded degree value (4/12 = 18.4°, not 20.0°).

**Fix sketch:** Compute one canonical pitch in generatePermit.ts using the existing fallback pattern at generatePermit.ts:289 (cad.roof.planes[0].pitch ?? project.roofPitch ?? 20), format it once as a single helper returning e.g. "3.6:12 (16.7°)", and pass that string to coverSheet.ts, certPages.ts, and sheetComposition.ts instead of each sheet re-deriving/re-rounding. Keep PV-2's per-plane labels, but the PV-0/PE-1/PV-2-headline value must be the same dominant-plane figure, and never print a rounded ratio beside unrounded degrees.

**Verifier notes:** All three strings verified in the generated HTML (lines 1038, 1616/1690, 3168). Recomputed: 4/12 = 18.43°, so PE-1's "4/12 (20.0°)" is self-contradictory; drawn planes are 3.6-4.1:12 = 16.7-18.9°, matching neither PV-0's 4:12-from-20° nor PE-1's 20.0°. Structural impact is likely nil at these slopes (snow Cs=1.0 below 30° for shingle; wind GCp slope bin mostly unchanged, though 20.0° sits exactly on the 7-20°/20-27° ASCE 7 boundary), so severity is plan-check correction risk rather than wrong engineering — P1 for a product sold on planset quality. Reference ref-text.txt contains no pitch text to compare, but this is a factual cross-sheet inconsistency, not a stylistic judgment.

_Reported by 2 agent(s)._

### P1-20 [VAL-1] '53 panels | 1 string(s)' — wrong topology for a 53-microinverter / 6-AC-branch system

**Description:** The 'Electrical Data Verified' PASS row resolves to '53 panels | 1 string(s) | Inverter: Enphase IQ8A (0.35 kW AC)'. A 53-microinverter system has no DC strings — it has AC branch circuits — and the package itself documents '53 MODULES / 6 BRANCHES' (PV-2B) and 6 branches on PV-4B. Describing the system inverter as '0.35 kW AC' (one micro's rating, vs the 18.50 kW AC system) further misstates the design in a row labeled 'Verified', damaging the credibility of the whole gate. (This is the known micro-'1 string' bug from the 2026-06-28 audit, still unfixed.)

**Evidence:** VAL-1.png Electrical Data Verified row; HTML 3766 exact text vs HTML 1688 ('CIRCUIT LAYOUT — 53 MODULES / 6 BRANCHES') and 1760.

**Root cause:** repo/lib/permit/utils/canonical.ts:218-220 (strings: inv0?.strings?.length || 1; inverterKw: inv0?.acOutputKw || 0 — topology-blind, so a micro system yields the '1' fallback and one micro's 0.35 kW), rendered verbatim at repo/lib/permit/sections/validationPage.ts:127.

**Fix sketch:** In validationPage.ts branch the 'Electrical Data Verified' value on topology (getInverterTopology is already imported there and used at line 387 for the header): for MICRO render `${totalPanels} panels | ${nBranches} AC branch circuit(s) | Micro: ${model} ×${totalPanels} (${systemAcKw.toFixed(2)} kW AC)`, sourcing nBranches from utils/branching.ts computeBranchCircuits (same source as PV-2B's 6 branches) and systemAcKw as panels × per-micro acOutputKw (mirror sldAdapter.ts:38/64-65). Alternatively add topology-aware fields (branches, systemAcKw) to CanonicalElectrical in canonical.ts so all consumers get it.

**Verifier notes:** Verified on VAL-1.png and HTML line 3766: 'Electrical Data Verified — 53 panels | 1 string(s) | Inverter: Enphase IQ8A (0.35 kW AC)' with green PASS, directly contradicting the same sheet's header (Topology: MICROINVERTER, 18.50 kW AC), PV-2B (HTML 1688: '53 MODULES / 6 BRANCHES'), and HTML 1760 ('6 AC branches'). Math checks: 18.50/53 ≈ 0.349, so 0.35 kW is one micro, not the system. Finder's suspected file (generatePermit.ts) was slightly off — the fallback lives in utils/canonical.ts — but the finding is otherwise accurate, and it is the known micro-'1 string' bug from the 2026-06-28 audit, still unfixed. P1 stands: a 'Verified' PASS row on the validation gate misstates topology and inverter capacity ~50x.

_Reported by 2 agent(s)._

### P1-21 [VAL-1] Sheet publishes that NOTHING is traceable to field evidence — no survey attached to this permit run

**Description:** The 'Survey Evidence Audit' prints, in a submitted permit document: 'No survey evidence attached to this permit run', 'No survey photos available', 'Missing Required Evidence: main_service_panel, meter, roof_plane, overview', and Physical Data/Roof Geometry/Electrical/Structural all 'Not traceable to survey evidence', plus 8 consecutive empty-provenance rows ('No engineering requirement registry evaluation was provided', 'No canonical evidence provenance records were provided', etc.). Handing the AHJ a written statement that the design was produced with zero field verification invites a site-survey demand and rejection of the structural assumptions (2x6@24", span 12 ft) that PE-1 already shows failing; it also contradicts PE-1's 'FIELD VERIFICATION REQUIRED' framing. The PE-sealed reference contains nothing of this kind.

**Evidence:** VAL-1.png Survey Evidence Audit section: rows 1-7, N/A status chips, orange 'Missing Required Evidence' row listing main_service_panel, meter, roof_plane, overview; empty-registry rows under Physical Data through Document Section Provenance.

**Root cause:** lib/permit/sections/validationPage.ts:196,202,207,343-347 (plus empty-registry fallback rows at 223,229,235,243,249,255,267,274) — negative provenance text emitted whenever surveyEvidence is null; included unconditionally in the issued package at lib/permit/generatePermit.ts:789

**Fix sketch:** When surveyEvidence is absent, suppress the entire "Survey Evidence Audit" section (and the empty registry/provenance subsections) from the issued-for-permit render — gate the section on surveyEvidence/traceability existing — and replace it with one neutral note such as "Design derived from canonical aerial/CAD geometry and jurisdiction data; field verification per PE-1 general notes." Keep the full negative audit only in an internal/QA render mode, never in the AHJ deliverable.

**Verifier notes:** Verified on VAL-1.png and in the raw HTML (lines 3804-3835): "No survey evidence attached to this permit run", "No survey photos available", "Missing Required Evidence: main_service_panel, meter, roof_plane, overview" (a hardcoded list, validationPage.ts:207), "Not traceable to survey evidence" for Physical/Roof/Electrical/Structural, and 8 consecutive "No ... was provided" registry rows. The sheet is "ISSUED FOR PERMIT — SHEET 15 OF 16", i.e. part of the AHJ submission. It also self-contradicts: banner says ALL CHECKS PASSED 11/11 and the canonical table shows rafter 2x6 @ 24" O.C. as verified PASS while the evidence audit says structural data is not traceable to any field evidence — handing a plan reviewer a written basis to demand a site survey or reject the structural assumptions. The PE-sealed reference set (PV0-PV5) publishes no such internal QA/provenance sheet, so the reference demonstrably does this better. Not P0 (no wrong engineering values are introduced), but a genuine submission-rejection risk, so P1 stands.

_Reported by 2 agent(s)._

### P1-22 [VAL-1] Entire sheet is internal QA/debug telemetry (hashes, rule IDs, dev-speak) unfit for an AHJ submission

**Description:** The sheet prints raw engineering-pipeline internals: 'PLANSET ENGINE v47379', decision hashes ('hash decision-b821eff4', 'state-generation-a6eb092d'), 'renderContexts: renderContext:primary', rule-ID strings ('DOC-PROVENANCE-V1, NEC-110.3, NEC-250...'), 'Cross-Contamination Check — PASS — PV-4C and PE-1 dispatch to isolated family functions — no shared narrative blocks', 'decision_lineage_required: PASS', 'fallback_assumptions_documented: PASS', and dev-speak decision records ('decision:bom_derivation | selected: 0 BOM row(s)', 'MOUNT_SYSTEM_MAP'). None of this is intelligible or useful to an AHJ; it reads as leaked developer QA output accidentally bound into the set. If a validation summary is kept at all, it should speak in permit terms (codes, loads, equipment), not pipeline internals — otherwise VAL-1 should be an internal artifact, not sheet 15 of the customer package.

**Evidence:** VAL-1.png full lower half: Engineering Decision Provenance / Decision Records tables with monospace hashes and rule IDs; Cross-Contamination Check row; Decision Provenance Bundle and Decision Dependencies rows; header 'PLANSET ENGINE v47379'.

**Root cause:** lib/permit/sections/validationPage.ts — pageValidationSummary(): telemetry row builders at lines 208-347 (decisionProvenanceRows L275-284 with deterministicHash/renderContextIds, decisionRecordRows L285-290 printing raw decision:* records, engineeringStateRows L304-319 with generation/provenance/dependency hashes, requirement/traceability rows L215-274), 'Cross-Contamination Check' dev-speak row L146-150, 'PLANSET ENGINE v...' banner L360; bound into the AHJ-facing set as sheet 15 at lib/permit/generatePermit.ts:789.

**Fix sketch:** Split VAL-1: keep only the top 'Canonical Object Audit' table rewritten in permit language (drop pipeline notes like 'throws if missing', 'MOUNT_SYSTEM_MAP', Cross-Contamination row, engine-version banner), and move all provenance/decision/state-registry/hash sections into a separate internal QA artifact gated behind an internalDebug flag (default off) so they are never concatenated into the submission HTML at generatePermit.ts:789. Also suppress the self-incriminating 'No survey evidence attached / Not traceable' rows from the AHJ-facing sheet.

**Verifier notes:** Every quoted string verified on VAL-1.png and 19 matches in the generated HTML (Cross-Contamination Check, decision:bom_derivation, renderContext:primary). Reference PE-sealed set (ref-text.txt + 8 ref sheets) contains no validation/QA/hash/provenance sheet at all, so the reference demonstrably does it better by omission. Aggravating factor beyond the finder's report: multiple rows openly state 'No survey evidence attached to this permit run' and 'Roof Geometry not traceable to survey evidence' — an admission sheet handed to the AHJ. Top canonical-checks table itself is salvageable; the defect is the telemetry dump and dev phrasing, so P1 (professionalism/rejection risk), not P0 (no engineering values are wrong because of it).

_Reported by 3 agent(s)._

### P1-23 [VAL-1] 100A interconnection breaker selected by 'deterministic fallback' with 'evidence: none'

**Description:** Decision record: 'breaker_sizing | selected: 100 | reason: Existing breaker value selected from project input or documented deterministic fallback | ... evidence: none | rules: NEC 690.8, NEC 690.9, NEC 705.12'. The sheet cites 705.12 but admits no evidence of bus or main rating. On a typical 200A residential service, a 100A backfed PV breaker fails the 120% rule (200×1.2−200 = 40A max) — an 18.5 kW AC system generally needs a supply-side connection or bus/main derate, none of which is evidenced. The gate marks this PASS anyway.

**Evidence:** VAL-1.png decision:breaker_sizing row (HTML text includes 'evidence: none'); system is 18.50 kW AC → 77A × 1.25 = 96.4A → 100A OCPD.

**Root cause:** lib/permit/sections/electricalPages.ts:342-399 (load-side 120% check hardcoded; renders 'FAIL — UPGRADE PANEL' at :380/:398 ignoring interconnection method) + lib/permit/sections/coverSheet.ts:207 (always labels OCPD 'BACKFEED BREAKER (NEC 705.12(B))')

**Fix sketch:** Thread the resolved interconnection method (elec.interconnection.method / project.interconnectionMethod, already computed correctly in lib/electrical-calc.ts:735-743) into the load-calc block in electricalPages.ts: when SUPPLY_SIDE_TAP, replace row 9 and the interpretation/formula blocks with 'N/A — Supply-Side Tap per NEC 705.11 (120% busbar rule not applicable)' and drop the FAIL badge; in coverSheet.ts:207 label the device 'FUSED AC DISCONNECT — SUPPLY-SIDE TAP (NEC 705.11)' for that method. Additionally, add bus/main-rating evidence requirements to the breaker_sizing decision (lib/engineeringDecisionProvenance/evaluator.ts:227) and an interconnection-consistency check to the VAL-1 gate so 'evidence: none' + cross-sheet contradiction cannot pass 11/11.

**Verifier notes:** Finding is confirmed in substance but its central compliance claim needed correction. TRUE: the VAL-1 breaker_sizing decision text ('selected: 100 ... evidence: none ... NEC 705.12') is verbatim in the HTML (line 3919); the math (77.1A x 1.25 = 96.4 -> 100A) is right; the VAL-1 gate shows ALL CHECKS PASSED 11/11 with no interconnection check. FALSE: 'supply-side connection ... none of which is evidenced' — the package documents a supply-side tap in 4 places (design criteria line 1240, compliance table lines 1919-1922 'N/A (Supply-Side Tap) PASS', SLD box lines 3417-3426 'Backfed Breaker: N/A — Tap Connection', sld_metadata decision line 3963), and under a supply-side tap the 120% rule legitimately does not apply, so 100A is not itself a violation. HOWEVER the verified defect is an internal contradiction that is permit-rejection grade: PV-0 equipment list says '(N) 100A BACKFEED BREAKER (NEC 705.12(B))' (line 1019), the load-calc sheet prints '120% Busbar Rule ... FAIL — UPGRADE PANEL' with a red FAIL badge and prose asserting a load-side backfeed design (lines 2238-2256), and design criteria says 'NEC 705.12(B): REQUIRES REVIEW' (line 1243) — all contradicting the supply-side SLD, with the validation gate passing regardless. Severity stays P1: an AHJ reviewer seeing a printed FAIL plus two mutually exclusive interconnection methods rejects the set.

_Reported by 1 agent(s)._

### P1-24 [VAL-1] Page overflow: decision/provenance content clipped at bottom sheet edge

**Description:** The printed sheet ends mid-table at 'decision:inverter_selection', but the HTML contains at least seven more decision rows (layout_orientation_assumption, mppt_assignment, ocpd_selection, placard_requirement, rapid_shutdown_placement, setback_assumption, sld_metadata, string_topology_selection) plus two entire subsequent sections ('Engineering State Invalidation' and 'Canonical Object — Field Resolution Transparency') that never appear on the page. The sheet silently truncates its own content.

**Evidence:** VAL-1.png last visible row is decision:inverter_selection flush against the sheet border; HTML lines 3938-3966 (further decision rows) and 3968-4090 (two missing sections) all precede sheet 16's start at line 4167.

**Root cause:** repo/lib/permit/sections/validationPage.ts:504-539 + :559-620 — unbounded record rows (decisionRecordRows built at :285, engineeringStateRecordRows at :314, one row per decision/BOM item) plus two trailing sections are all rendered into a single fixed-height sheet; the fixed 17in×11in .page container with overflow:hidden (repo/lib/permit/generatePermit.ts:879 .page rule) silently clips everything past the page bottom.

**Fix sketch:** Give VAL-1 a deterministic row budget: count the rows each subsection will emit and either (a) split the audit table across VAL-1/VAL-1B continuation pages by emitting a second .page div when the cumulative row estimate exceeds the budget, or (b) collapse the unbounded dumps (per-BOM engineeringStateRecordRows, per-decision decisionRecordRows) into aggregate summary lines (e.g., "14 bom_row records, all status: current, depHash range …") so worst-case row count is bounded. Also move the Canonical Object dump above the unbounded table so fixed-size sections are never the ones clipped.

**Verifier notes:** Verified on PNG (last row decision:inverter_selection flush against border) and in HTML: lines 3938-3966 (8 more decision rows), 3970 (Engineering State Invalidation, ~14 bom_row records) and 4081 (Canonical Object — Field Resolution Transparency) all precede sheet 16 at line 4167 but never render. Clipping mechanism confirmed: .page is fixed 17in×11in with overflow:hidden (HTML lines 79-88). Clipped content is provenance/traceability metadata rather than code-required data, but the truncation is silent, visually obvious (table cut mid-row at the sheet edge), and scales with BOM/decision count so it affects every real job — P1 upheld.

_Reported by 1 agent(s)._

### P1-25 [CERT] Certification/issue dates fabricated: certified 2026-06-03 on a document 'Generated 7/3/2026', unsigned; license-expiration prefilled with the issue date

**Description:** 'Date of Certification' is pre-filled 2026-06-03 and Document ID/Issue Date/Rev A ('ISSUED FOR PERMIT — 2026-06-03') all say 2026-06-03, but the footer states 'SolarPro Engineering Platform v60.5 · Generated 7/3/2026' — the document claims it was certified a full month before it was generated, no engineer has signed, and the liability clause keys off that date ('expires 180 days from the date of certification'), so the fabricated date has legal effect. Separately, the blank PE 'LICENSE EXPIRATION DATE' field is pre-filled with 2026-06-03 (the issue date), implying the engineer's license expires the day the permit was issued, while all other PE fields are blank underscores. Date formats also mix ISO (2026-06-03) and US (7/3/2026).

**Evidence:** CERT.png Date of Certification 2026-06-03 (HTML 3033); Document Control ISSUE DATE 2026-06-03; footer 'Generated 7/3/2026' (HTML 3084); left column 'LICENSE EXPIRATION DATE / 2026-06-03' amid otherwise-blank PE fields.

**Root cause:** lib/permit/sections/certPages.ts:55,74,89-90 stamp stale project.date (set once at config creation, app/engineering/page.tsx:480) as cert/rev/document-ID/issue date, while certPages.ts:106 footer stamps runtime new Date().toLocaleDateString() in US format

**Fix sketch:** In generatePermit.ts compute a single issueDate = new Date().toISOString().split('T')[0] at generation time, thread it through PermitInput, and use it for Rev A, Document ID, Issue Date, and the footer (drop toLocaleDateString) so one ISO date appears everywhere. Leave "Date of Certification" as blank underscores for the PE to fill at signing, since the 180-day liability clause keys off it. Also swap .cf-lbl above .cf-val (or tighten spacing) in the cert-field CSS so filled values can't be visually attributed to the label above them.

**Verifier notes:** Core finding verified against the HTML: cert/issue/rev/doc-ID all say 2026-06-03 while the footer says Generated 7/3/2026, formats mix ISO and US, no signature, and the liability clause expires 180 days from the (stale, prefilled) certification date. One sub-claim corrected: the License Expiration field is NOT bound to the date in the DOM (HTML line 3032 is underscores; 2026-06-03 is the Date of Certification value at line 3033) — the value-above-label CSS layout merely makes it read that way on the rendered sheet, which is itself worth fixing. Severity stays P1: fabricated-looking dates on the legal certification sheet undermine AHJ credibility, though no engineering calc is affected.

_Reported by 3 agent(s)._

### P1-26 [CERT] 'Designer / Engineer of Record' pre-filled with the software company name

**Description:** The field labeled 'Designer / Engineer of Record' is pre-filled with 'SolarPro Engineering' (the platform brand, not a licensed PE), directly above an 'I hereby certify ... prepared under my direct supervision' statement, with PE License Number, State of Licensure, and Signature all blank. Naming an unlicensed corporate entity as EOR on a certification page misrepresents responsible charge; the field should be blank for the sealing engineer.

**Evidence:** CERT.png Prepared By block; HTML 3028: cf-val 'SolarPro Engineering', cf-lbl 'Designer / Engineer of Record'.

**Root cause:** app/api/engineering/permit/route.ts:1019-1021 (server fallback sets project.designer='SolarPro Engineering' when empty) feeding lib/permit/sections/certPages.ts:50, which renders project.designer into the 'Designer / Engineer of Record' field on the EOR certification sheet.

**Fix sketch:** In certPages.ts:50, stop using project.designer for the EOR line: render a distinct project.engineerOfRecord field if present, else the blank underscores, matching the other PE fields. Keep the route.ts 'SolarPro Engineering' fallback for title blocks and the Revision History 'By' column (certPages.ts:74), where a drafter credit is appropriate; alternatively relabel the CERT line 'Prepared By (Drafter)' and add a separate blank 'Engineer of Record' line.

**Verifier notes:** Verified on all three sources: HTML line 3028 has cf-val 'SolarPro Engineering' under cf-lbl 'Designer / Engineer of Record'; CERT.png shows it as the only pre-filled identity in the PREPARED BY block, directly under the 'I hereby certify ... prepared under my direct supervision' statement, with Firm, PE License Number, State of Licensure, Signature all blank; the Wyssling reference certification instead names a real PE with 'Indiana License No. PE11900440' (ref-text.txt:144). Mitigations (blank seal box, footer 'requires engineer review and wet stamp') keep this at P1 rather than P0, but pre-filling an unlicensed software brand into the responsible-charge line risks AHJ rejection and misrepresents responsible charge. The finding's suspectedCode (generatePermit.ts) was slightly off; actual code is certPages.ts + the permit route fallback.

_Reported by 1 agent(s)._

### P1-27 [CERT] SLD reference leaks app UI navigation text and falsely says the SLD is generated separately (E-1 is in the package)

**Description:** The SLD REFERENCE row reads: 'Sheet E-1 | Single-Line Electrical Diagram — generated separately via Engineering → Diagram tab → Download SLD PDF'. Internal product-UI navigation instructions do not belong in a legal submission document — meaningless to an AHJ — and the statement is false for this package: E-1 'SINGLE LINE DIAGRAM — PHOTOVOLTAIC SYSTEM' IS included as sheet 14 of 16 (it appears in the PV-0 sheet index and has its own title block). A checker reading CERT would conclude the SLD is missing.

**Evidence:** CERT.png SLD REFERENCE row (HTML 3059) vs sheet index entry for E-1 (HTML 1130) and E-1 title block (HTML 3290/3299, 'sheet 14 of 16'); footer 'SolarPro Engineering Platform v60.5'.

**Root cause:** repo/lib/permit/sections/certPages.ts:81 — hardcoded stale SLD REFERENCE row; generatePermit.ts:788 always embeds E-1 via pageSingleLineDiagram(input, cad, 14, TOTAL, storedSldSvg), so the "generated separately" claim is unconditionally false.

**Fix sketch:** In certPages.ts:81, replace the row value with package-internal cross-reference copy, e.g. "Single-Line Electrical Diagram — included in this package as Sheet E-1 (see sheet index)", dropping all app UI navigation text. Optionally pass the E-1 sheet number/TOTAL into the cert page builder so it can render "Sheet 14 of 16" dynamically instead of hardcoding.

**Verifier notes:** Verified on CERT.png (SLD REFERENCE block, right column) and raw HTML line 3059. E-1 presence verified three ways: PV-0 sheet index entry (HTML 1130, "E-1 | SINGLE-LINE DIAGRAM — ELECTRICAL SCHEMATIC"), E-1 title block "SHEET 14 OF 16" (HTML 3287), and sheet title "SINGLE LINE DIAGRAM — PHOTOVOLTAIC SYSTEM" (HTML 3299). generatePermit.ts includes the SLD page unconditionally, so this text is wrong for every generated package, not just this one. Same stale copy also present in repo/public/audit_pages/page_12_PAGE-12.html:910 (static snapshot). Severity kept at P1: false statement + internal product UI leak on the engineer certification sheet of a legal submission; a checker cross-referencing CERT against the index sees a contradiction. Not P0 because the SLD is actually present and no engineering data is affected.

_Reported by 3 agent(s)._

### P1-28 [APP-CAD] Internal debug artifact shipped as a permit sheet — UUIDs, hashes, pipeline warnings, and five self-disclaimers

**Description:** Sheet 16 prints raw pipeline telemetry: 'roofCAD: 3 Nearmap AI obstruction(s) on-roof (51 neighbor/off-roof dropped)' and '[OBSTRUCTION] plane aa72d4e3-2a53-48de-9fde-88acb2a8e01a: removed 1 panel(s) blocked by obstructions' (internal UUIDs and AI-vendor names), export hashes (9473f8a2, 07084c35, d7c566ce), 'units=meters_local_xy', and boundary notes in pure dev-speak ('additive deterministic DTO sheet', 'consumes CADModelExportBundle and CADSvgArtifactPreview', 'mutate NEC, mutate BOM, mutate routing'). It also declares 'CAD PREVIEW ONLY · NON-AUTHORITATIVE · NOT PERMIT AUTHORITY · NOT ENGINEERING AUTHORITY · NOT CONSTRUCTION DRAWING' in red in FIVE places (header banner, metadata panel, red warning box, fine-print caption, footer strip). A sheet that exists mainly to disclaim itself — while duplicating PV-2 content at lower quality and inviting the checker to ask whether the 53-module count reflects the 'removed 1 panel' — creates ambiguity about which drawings govern. It has no analog in the PE-sealed reference and should be dropped from the export or stripped of debug output.

**Evidence:** APP-CAD.png: WARNINGS block with UUID string (verbatim at HTML line 4175); CAD PREVIEW METADATA hash rows; red banner and footer strip; 'units=meters_local_xy' under the preview title; boundary-note dev-speak.

**Root cause:** lib/permit/generatePermit.ts:794 (pageCADAppendixPreview unconditionally pushed as sheet 16); warnings text originates in lib/cad/roof/roofCAD.ts:214 and :377 and is passed verbatim via lib/permit/sections/cadAppendixPreviewPage.ts:27 (renderingWarnings: cad.warnings) into lib/drafting/cadAppendixPreviewSheet.ts:129-130, which renders each warning string raw; dev-speak boundary notes hardcoded at cadAppendixPreviewSheet.ts:116; fifth disclaimer strip hardcoded at cadAppendixPreviewPage.ts:41-42.

**Fix sketch:** Remove line 794 in generatePermit.ts (or gate it behind an internal/debug flag not set for customer permit exports) so APP-CAD never ships in the submitted package. If the sheet is kept for internal QA, stop passing cad.warnings verbatim (map to human-readable plain-English notes without UUIDs/vendor/pipeline prefixes), drop the hash/units metadata rows, and collapse the five disclaimers into one footer line.

**Verifier notes:** Every claim checks out against the artifacts. APP-CAD.png shows verbatim: WARNINGS block 'roofCAD: 3 Nearmap AI obstruction(s) on-roof (51 neighbor/off-roof dropped)' and '[OBSTRUCTION] plane aa72d4e3-2a53-48de-9fde-88acb2a8e01a: removed 1 panel(s) blocked by obstructions'; metadata rows EXPORT HASH 9473f8a2 / ARTIFACT HASH 07084c35 / SHEET HASH d7c566ce; 'units=meters_local_xy' under the preview title; boundary notes with 'additive deterministic DTO sheet', 'consumes CADModelExportBundle and CADSvgArtifactPreview', and the 'mutate NEC, mutate BOM, mutate routing' sentence. The five disclaimer locations are all present (header sub-banner, metadata panel 'NO PERMIT / ENGINEERING / CONSTRUCTION AUTHORITY', red warning box, fine-print caption under the SVG, bottom footer strip); HTML line 4180 confirms the footer text. The reference set (ref-p3 through ref-p10, PV0-PV5 only) contains no debug/preview appendix sheet, so the 'no analog in the PE-sealed reference' claim holds. Severity P1 is appropriate: no engineering data is wrong (not P0), but shipping raw pipeline telemetry, UUIDs, AI-vendor names, and a sheet that repeatedly declares itself non-authoritative inside a submitted permit package is unprofessional, invites plan-checker questions (e.g., whether the 53-module count reflects the 'removed 1 panel' warning), and creates ambiguity about governing drawings — it should be pulled before submission.

_Reported by 5 agent(s)._

### P1-29 [APP-A / package-wide] No actual manufacturer datasheets — 'see manufacturer website' stub with the bottom half of APP-A blank (reference includes ~16 pages of cut sheets)

**Description:** The sheet indexed as 'SPECIFICATION REFERENCE — EQUIPMENT DATA SHEETS' provides only 'Module: SunPower — see manufacturer website / Inverter: Enphase — see manufacturer website' and 'Copies available upon AHJ request', with roughly the bottom 55% of the sheet empty white space. The reference package dedicates pages 11-26 to real manufacturer spec sheets (module, microinverter, battery, combiner, Unirac racking datasheets confirmed on p26) and its index explicitly lists 'SPEC SHEETS'. Many AHJs and most utility interconnection reviewers require the actual UL-listed manufacturer cut sheets in the submission; 'available upon request' invites a correction cycle, and blank half-sheets read as an unfinished document side-by-side with the reference.

**Evidence:** APP-A.png Manufacturer Data Sheets note block ('Module: SunPower — see manufacturer website', 'Inverter: Enphase — see manufacturer website'); no content below ~y 500 of 1030. ref-text.txt p3 index line 'SPEC SHEETS'; p26 full Unirac STRONGHOLD BUTYL / NXT UMOUNT datasheet text.

**Root cause:** C:\Users\Ray\Solarpro Claude\repo\lib\permit\sections\compliancePages.ts:373-471 — pageSpecSheetReference() renders only summary tables plus a 'see manufacturer website / Copies available upon AHJ request' note (lines 457-466); no datasheet ingestion/appendix mechanism exists anywhere in lib/permit (invoked from generatePermit.ts:785).

**Fix sketch:** Bundle static manufacturer cut-sheet assets (PDF-rendered PNGs or embedded pages) for the supported equipment catalog (SunPower/Maxeon modules, Enphase IQ8 micros, IronRidge/Unirac racking, Tesla Powerwall), keyed by manufacturer+model, and append them as full-page appendix sheets (APP-B, APP-C, ...) after APP-A with standard title blocks — mirroring the reference's dedicated SPEC SHEETS section. Also replace the synthesized module values (Vmp=Voc*0.83, Imp=Isc*0.94, hardcoded temp coefficients and 5400/2400 Pa loads at compliancePages.ts:360-401) with real per-model data from that same catalog, and use the racking datasheet content to fill the currently blank lower half of APP-A.

**Verifier notes:** Core claims verified: (1) generated APP-A PNG content ends ~y490/1030, bottom half blank; HTML lines 2953-2956 contain the 'see manufacturer website' + 'Copies available upon AHJ request' stub; no manufacturer cut sheet exists anywhere in the 16-sheet set. (2) Reference index lists 'SPEC SHEETS' (ref-text.txt:214) and ref-text.txt lines 1418-1500 are verbatim Unirac STRONGHOLD BUTYL / NXT UMOUNT datasheet text (SALES@UNIRAC.COM, 'WHY STRONGHOLD BUTYL?'), confirming real cut sheets are physically included in the reference; the exact '~16 pages' count could not be verified from text (other datasheet pages likely image-based) but at least the racking datasheet is confirmed. Caveat: the finding overstates 'provides only' — APP-A does carry substantive spec tables (module electrical/physical, NEC 690.8 calcs, inverter, racking). However those values are partly synthesized in code (Vmp=Voc*0.83, Imp=Isc*0.94, hardcoded coefficients/loads), i.e. estimates rather than actual manufacturer data, which strengthens rather than weakens the finding. P1 retained: utility interconnection reviewers and many AHJs require in-submission UL cut sheets; not P0 since key ratings are still present on APP-A.

_Reported by 4 agent(s)._

### P1-30 [PV-2 + PV-2B vs PV-5] Fire setback contradiction: 18" ridge/hip setback on the roof plan vs '3 ft from ridge' (stale IFC §605.11) in PV-5 install notes

**Description:** PV-2 and PV-2B design the layout to an 18" (1.5') ridge/hip setback with 36" access pathways ('18" ridge/hip setback per AHJ amendment — IFC 2021 §1204.2.1.1'), and the drawing hatches 1'-6" setbacks. PV-5's Structural/Installation general note 5 instructs 'Maintain IFC §605.11 fire code setbacks: 3 ft from ridge, 18" from edges and valleys' — a different requirement under a different (pre-2018) code section than the IFC 2021 §1204.2 cited everywhere else. The layout as drawn violates PV-5's own stated rule.

**Evidence:** PV-2.png fire setback legend/hatching '1'-6" FIRE SETBACK', callout '1.5' ridge/hip/rake · 3' access pathway — IFC §1204.2'; PV-2B.png fire setback notes; HTML PV-5: 'Maintain IFC §605.11 fire code setbacks: 3 ft from ridge, 18" from edges and valleys.'

**Root cause:** C:\Users\Ray\Solarpro Claude\repo\lib\permit\sections\compliancePages.ts:291 (hardcoded PV-5 note "Maintain IFC §605.11 fire code setbacks: 3 ft from ridge, 18\" from edges and valleys"); also :305 (hold-point "IFC 605.11") and :106 (necRef "IFC §605.11 / NEC 690.56(A)"). Contradicts the PV-2 setback panel generated at lib/permit/sections/arrayPages.ts:428, which cites IFC 2021 §1204.2.1.1 with the 18" ridge/hip value the layout is actually drawn to.

**Fix sketch:** In compliancePages.ts:291, replace the hardcoded legacy text with the same AHJ-driven values/citation used by arrayPages.ts (e.g. "Maintain fire code setbacks per IFC 2021 §1204.2 as shown on sheet PV-2: 18\" ridge/hip setback per AHJ amendment, 36\" access pathways"), ideally interpolated from the single setback-engine/AHJ source rather than a literal. Update :305 and :106 citations from "IFC 605.11" to "IFC 2021 §1204" (mirror titleBlock.ts:116, which already does this correctly).

**Verifier notes:** Verified in the generated HTML: PV-5 sheet (sheet id line 2612) note 5 at line 2655 says 3 ft from ridge per IFC §605.11, while PV-2's FIRE SETBACKS panel (lines 1754-1756) says 18" ridge/hip per IFC 2021 §1204.2.1.1 and the layout is drawn/hatched at 1'-6". The package thus instructs the installer to maintain a setback the approved layout violates, citing a code section that ceased to exist in 2018+ IFC editions the set claims compliance with (cover note cites "2021 IFC"). This is exactly the kind of internal inconsistency plan checkers flag — P1 stands (not P0: it's a notes contradiction, not an actual code violation of the governing 18"-amendment requirement). The constants.ts:120 comment shows a §605.11→§1204.2 citation sweep was done but missed the compliancePages.ts PV-5 template.

_Reported by 1 agent(s)._

### P1-31 [PV-1 vs E-1 + PV-5 + SCHED] Battery/ESS equipment shown on the site plan for a system with 'Battery Storage: NONE'

**Description:** PV-1's equipment legend includes '(N) ENPHASE SYSTEM CONTROLLER' (item 3), '(N) BACKUP LOAD PANEL — CRITICAL LOADS SUB-PANEL' (item 4), and the AC-disconnect location note says 'EXTERIOR WALL (ESS)'. But E-1's equipment schedule explicitly says 'Battery Storage: NONE', SCHED's BOM contains no battery/system-controller/sub-panel items, PV-0's system summary lists neither, and PV-5 greys out all battery labels (L-7/L-8 'N/A'). The site plan depicts scope no other sheet, the BOM, or the SLD includes — it looks copied from a battery/ESS job (the reference project has exactly this equipment) and an inspector will ask where the ESS is.

**Evidence:** PV-1.png legend items 3-4 and 'LOCATION OF AC DISCONNECT: EXTERIOR WALL (ESS)'; e1_tables.png 'Battery Storage NONE'; PV-5.png label schedule L-7/L-8 greyed 'N/A'; SCHED.png BOM has no ESS rows.

**Root cause:** repo/lib/permit/sections/sitePlan.ts:50-51 (unconditional '(N) SYSTEM CONTROLLER' and '(N) BACKUP LOAD PANEL' in equipItems despite hasBatt gate existing at line 43) and sitePlan.ts:538 (hardcoded 'EXTERIOR WALL (ESS)' in the AC-disconnect location box)

**Fix sketch:** Move the '(N) ${invMfr} SYSTEM CONTROLLER' and '(N) BACKUP LOAD PANEL' entries (lines 50-51) inside the existing `...(hasBatt ? [...] : [])` spread at lines 54-57 so they only appear on battery jobs. At line 538, make the location text conditional: `${hasBatt ? 'EXTERIOR WALL (ESS)' : 'EXTERIOR WALL — ADJACENT TO UTILITY METER'}`.

**Verifier notes:** Verified on PV-1.png (legend items 3-4 and the ESS location note are visibly present) and in the raw HTML (lines 1452, 1463, 1492). E-1's table explicitly renders 'Battery Storage / NONE' (HTML lines 3465-3466), and the project data confirms batteryCount=0 since the hasBatt-gated battery rows are absent from the legend (items stop at 6, no '(N) BATTERY'). The finder's suspected cause was nearly right — the gate exists but is applied only to the battery unit + second disconnect rows, not to the controller/sub-panel rows or the ESS location text. Both a system controller and a critical-loads sub-panel are ESS/backup-only equipment for an Enphase micro system, so their presence contradicts E-1/SCHED/PV-5 and reads as copied from a battery job. P1 stands: internal scope contradiction likely to draw a plan-check correction, not a safety/calculation error.

_Reported by 2 agent(s)._

### P1-32 [PV-5 vs E-1] NEC 690.54 point-of-interconnection labels state 8.2A max circuit current; actual AC output is 77A

**Description:** Labels L-1 ('SOLAR ELECTRIC SYSTEM CONNECTED — MAXIMUM CIRCUIT CURRENT: 8.2A', NEC 690.54) and L-6 ('PHOTOVOLTAIC SYSTEM DISCONNECT — MAXIMUM CIRCUIT CURRENT: 8.2A') use 8.2A, which is the single-module DC Isc × 1.25 — not the system AC output. E-1 computes 'AC Output Amps: 77 A' and PV-4B sizes the feeder at 100A OCPD. NEC 690.54 requires the rated AC output current at the point of interconnection (77A here); the printed placard values are off by ~10x and inconsistent with the SLD.

**Evidence:** PV-5.png L-1/L-6 red labels 'MAXIMUM CIRCUIT CURRENT: 8.2A'; HTML label locations 'On combiner box and at DC disconnect' / 'On AC disconnect'; e1_tables.png 'AC Output Amps 77 A'.

**Root cause:** C:/Users/Ray/Solarpro Claude/repo/lib/permit/sections/compliancePages.ts:30 — `const maxCircuitCurrent = (panelIsc * 1.25).toFixed(1)` (single-module DC Isc×1.25) is injected into L-1's NEC 690.54 placard (line 52) and L-6's 690.53 placard (line 128) with no aggregation over modules and no AC-output basis.

**Fix sketch:** In pageWarningLabels, compute the system AC output current the same way E-1 does (electricalPages.ts:791 / generatePermit.ts:175): `const acOutputAmps = (input.system.totalAcKw * 1000) / 240` and use `${acOutputAmps.toFixed(0)}A` for L-1 (690.54 requires rated AC output current + nominal operating AC voltage, so also label the 240V line as 'RATED AC OUTPUT CURRENT' / 'NOMINAL OPERATING AC VOLTAGE'). For L-6 on micro topology, use the same AC output amps at 240V AC; keep panelVoc×1.25 / string Isc-based values only for string-DC topologies.

**Verifier notes:** Verified on PV-5.png: L-1 reads 'MAXIMUM CIRCUIT CURRENT: 8.2A' under NEC 690.54, L-6 reads 'MAXIMUM INPUT VOLTAGE: 240V AC / MAXIMUM CIRCUIT CURRENT: 8.2A'. Math confirmed: 6.58A Isc × 1.25 = 8.22 → 8.2A; 53 × IQ8A (349VA) = 18.5 kW AC → 77A, matching E-1's 'AC Output Amps 77 A' and PV-4B's 100A OCPD (77×1.25=96.3→100A). Placard value is ~9.4x low and contradicts the SLD in the same package. Same maxSystemVoltage/maxCircuitCurrent pair also feeds L-6 where mixing 240V AC with a DC per-module current is internally incoherent for a microinverter system.

_Reported by 1 agent(s)._

## P2 — Polish (4)

### P2-1 [SET-WIDE (PV-0, PV-1, PV-2, PV-3, E-1)] Contradictory scale statements on nearly every sheet (AS NOTED vs NTS vs stated ratios that are false)

**Description:** Each sheet declares its scale two or three incompatible ways. PV-0: title block 'AS NOTED' vs sheet ID 'NOT TO SCALE'. PV-1: footer 'SCALE: NTS' vs graphic bar '1" ≈ 14'' (a non-standard approximate ratio) vs title block 'AS NOTED' — and the drawing actually IS to scale, so the NTS note is the false one. PV-2: 'AS NOTED' pointing at nothing (no ratio stated anywhere). PV-3: header claims '1"=1'-0"' while the drawing note says NTS and nothing is drawn to that ratio. E-1: inner title block 'NOT TO SCALE' vs outer 'AS NOTED'. Reviewers scale off site plans to check setbacks; contradictory scale declarations are a standard checklist correction. The reference states one conventional scale per sheet (1/16"=1'-0", 3/32"=1'-0").

**Evidence:** HTML 1386 vs 1311 (PV-0); HTML 1561, 1546, 1592 (PV-1); PV-2.png 'AS NOTED' + 'GRAPHIC SCALE — SEE BAR'; PV-3 SVG 'SCALE: 1"=1'-0"' + '— NTS' (HTML ~1819, 1824); E-1.png dual blocks.

**Root cause:** lib/permit/sections/sitePlan.ts:554 (hardcoded 'SCALE: NTS' footer vs the computed scale bar emitted at sitePlan.ts:395-403 on the same sheet) and lib/drafting/templates/roof.ts:936 (hardcoded SCALE: 1"=1'-0" title bar vs the '— NTS' schematic note at roof.ts:1165); set-wide 'AS NOTED' comes from lib/permit/utils/titleBlock.ts:72

**Fix sketch:** In sitePlan.ts:554 replace the hardcoded 'SCALE: NTS' with the same computed ratio used by the scale bar (pass scaleFeetPerInch out of the SVG builder and print `SCALE: 1" ≈ N' — SEE GRAPHIC BAR`), or better, snap the crop to a standard engineering scale (1"=10'/1"=20') so a true ratio can be stated. In roof.ts:936 change the title-bar scale string to 'SCALE: NTS' to match the schematic note at line 1165 (the detail is not drawn at 1"=1'-0"). Leave titleBlock.ts 'AS NOTED' as-is once each sheet carries exactly one truthful scale note.

**Verifier notes:** Core finding verified in both HTML and PNGs, but scope trimmed. Genuine defects: (1) PV-1 — footer 'SCALE: NTS' (HTML 1561) contradicts the graphic scale bar + computed '1" ≈ 14'' (HTML 1546) on a georeferenced aerial that IS to scale; NTS is the false statement, and site plans are what reviewers scale for setbacks. (2) PV-3 — title bar 'SCALE: 1"=1'-0"' (HTML 1818) directly contradicts the same detail's 'CROSS-SECTION SCHEMATIC ... — NTS' note (HTML 1824), and the schematic is not at that ratio. Reference package states one conventional scale per sheet (1/16"=1'-0" site, 3/32"=1'-0" roof, NTS details; ref-text.txt lines 398/548/227) with no conflicts and never uses 'AS NOTED'. Overreach in the finding: title-block 'AS NOTED' paired with a per-drawing NTS note (PV-0 cover, E-1 SLD) is standard drafting convention, not a contradiction — those instances are not defects. Severity re-assessed P1→P2: annotation-consistency issue likely to draw a plan-check correction, but no engineering data is wrong and the fix is two template strings.

_Reported by 11 agent(s)._

### P2-2 [PE-1] Roof pitch conversion wrong: '4/12 (20.0°)' — 4:12 is 18.4°

**Description:** arctan(4/12) = 18.43°, not 20.0° (20.0° = ~4.4:12). Either the pitch ratio or the degree value is wrong, and the degree value feeds ASCE 7 roof-zone classification (7°-20° vs 20°-27° C&C slope bins) and the snow slope factor — so a wrong conversion on the structural letter is a data error, not cosmetic, and invites a plan-check question about which value the loads were computed with.

**Evidence:** PE-1.png Existing Roof Construction 'ROOF PITCH 4/12 (20.0°)'; HTML line 3168 confirms '4/12 (20.0°)'. atan(4/12) = 18.43°.

**Root cause:** repo/lib/permit/sections/certPages.ts:420 — ratio is derived from degrees with Math.round(Math.tan(deg°)*12), so 20° → round(4.37)=4 → prints the contradictory pair "4/12 (20.0°)". Same pattern in legacy lib/permit_gen.mjs:222 and :445.

**Fix sketch:** In certPages.ts:420, stop rounding the derived rise to an integer: format as `${(Math.tan(deg*Math.PI/180)*12).toFixed(1)}/12 (${deg.toFixed(1)}°)` → "4.4/12 (20.0°)", or if the survey captured a true ratio (e.g. 4/12), carry that ratio through and derive degrees via atan so both values agree (4/12 (18.4°)). Apply the same fix to lib/permit_gen.mjs:222/445.

**Verifier notes:** Verified: HTML line 3168 prints "4/12 (20.0°)"; atan(4/12)=18.43° and 20°=4.37:12, so the pair is self-contradictory. Root cause: project.roofPitch is stored in DEGREES (20 — likely the survey pitch-enum default standard→20° in lib/siteSurvey/fromPhysicalData.ts:196-197); certPages.ts derives the ratio from it and rounds the rise to the nearest integer. Finder's severity was overstated: structural calcs consume the degree value (shown correctly), so ASCE bins/snow slope factor are NOT computed from the wrong number — the defect is an inconsistent displayed label. Still a real plan-check risk since 20° sits exactly on the 7-20/20-27 C&C bin boundary and a reviewer recomputing atan(4/12) gets a different bin. Downgraded P1→P2. Finder's suspected file (generatePermit.ts) was wrong; actual emitter is certPages.ts.

_Reported by 2 agent(s)._

### P2-3 [PE-1] LRFD load combo (0.9D + 1.0W, §2.3) paired with ASD capacities

**Description:** The Governing Load Combination block cites 'ASCE 7-22 §2.3' (strength design/LRFD) with combo '0.9D + 1.0W (Uplift)', yet every capacity on the sheet is allowable-stress based (adjusted F'b in psi, lag 'safety factor 2.38 vs min 2.0 req.'). Mixing LRFD wind combos with ASD wood/withdrawal capacities is methodologically inconsistent (ASD would be 0.6D + 0.6W per §2.4). Also, the sheet's own governing check is gravity deflection, not uplift, so the displayed 'governing combo' doesn't match the displayed 'governing check'.

**Evidence:** PE-1.png 'GOVERNING LOAD COMBINATION (ASCE 7-22 §2.3): 0.9D + 1.0W (Uplift)' alongside 'F'B (ADJUSTED) 1521 psi', 'SAFETY FACTOR 2.38 (min. 2.0 req.)' and 'GOVERNING CHECK deflection — 145%'.

**Root cause:** repo/lib/permit/sections/certPages.ts:476-477 (hardcoded 'Governing Load Combination (ASCE 7-22 §2.3)' / '0.9D + 1.0W (Uplift)' row); same boilerplate at certPages.ts:246-247, 340-341 and structuralPages.ts:330-339, 549-558, 804-816

**Fix sketch:** Since every displayed capacity is ASD (adjusted F'b, lag safety factor ≥2.0, service deflection), relabel the block to ASCE 7-22 §2.4 ASD combos — '0.6D + 0.6W' for uplift and 'D + S' for gravity — across certPages.ts and structuralPages.ts. Additionally, derive the 'Governing Combo' cell from the already-computed _governs variable (certPages.ts:475) instead of the hardcoded uplift string, e.g. deflection/bending → 'D + S (gravity)', lag withdrawal → '0.6D + 0.6W (uplift)'.

**Verifier notes:** Verified on PE-1.png and HTML (Downloads/PermitPackage...(9).html lines 2505-2506, 3193): §2.3 (LRFD) combo '0.9D + 1.0W (Uplift)' is displayed next to ASD capacities (F'b adjusted 1521 psi, lag SF 2.38 vs min 2.0, service deflection), and the sheet's own Governing Check cell says 'deflection — 145%', contradicting the uplift combo one row below. Both halves of the finding hold. Downgraded P1→P2 because the combo string is display-only boilerplate — it drives no calculation, so no computed value is wrong; impact is PE-letter credibility / plan-check red-line risk. Defect is set-wide (also PV-4C and ground/fence structural pages), not PE-1 only.

_Reported by 1 agent(s)._

### P2-4 [VAL-1] Decision record 'mppt_assignment: selected 2' contradicts APP-A 'MPPT Channels: 1'

**Description:** VAL-1's Engineering Decision Records include 'mppt_assignment | selected: 2 | reason: Existing MPPT channel metadata selected, with explicit topology default when missing.' APP-A specifies the IQ8A with 'MPPT CHANNELS 1' (correct — each IQ8A has a single MPPT per module). The provenance sheet and the spec sheet disagree about the same device parameter, citing NEC 110.3(B) as the governing rule while getting the listing data wrong.

**Evidence:** HTML 3943 (mppt_assignment selected: 2) vs HTML 2932 / APP-A.png 'MPPT CHANNELS 1'.

**Root cause:** repo/lib/engineeringDecisionProvenance/evaluator.ts:236 (mppt_assignment case)

**Fix sketch:** In evaluator.ts:236, the micro detection `String(system?.topology ?? '').toLowerCase() === 'micro'` is a strict-equality check that matches neither 'MICROINVERTER' nor the 'STRING' value this permit's system.topology actually carries, so it falls to the hard-coded string-inverter default of 2. Replace with a substring/model-based check (topology.toLowerCase().includes('micro') || /iq\d|micro/i.test(firstInverter?.model ?? '')) and select 1 (per-unit MPPT) for micro topologies — or better, derive from the same source APP-A uses (inv.strings?.length || 1, compliancePages.ts:438) so the two sheets cannot diverge. Separately investigate why string_topology_selection records 'STRING' for a 53x IQ8A micro system (HTML line 3967) — the same stale system.topology feeds both decisions.

**Verifier notes:** Verified in the raw HTML: line 3943 VAL-1 decision record 'mppt_assignment | selected: 2 ... confidence: explicit_default_policy_v1' vs line 2932 APP-A 'MPPT Channels 1'. The system is unambiguously microinverter — 53x Enphase IQ8A, VAL-1's own header says 'Topology: MICROINVERTER', and the E-1 SLD branch table shows 'MICROINVERTER / 53 units'. Each IQ8A has 1 MPPT per module, so APP-A's 1 is correct (compliancePages.ts:438 computes it independently as inv.strings?.length || 1) and the VAL-1 record's 2 is a wrong default. Code path confirmed: firstInverter.mpptChannels was null, and the micro branch in evaluator.ts:236 requires topology to lowercase to exactly 'micro'; this permit's system.topology is 'STRING' (see string_topology_selection 'selected: STRING' at HTML 3967 — itself contradicting the MICROINVERTER labels, a related upstream defect), so the evaluator emitted the string-inverter fallback of 2. Severity re-assessed P1→P2: the wrong value is confined to VAL-1's provenance metadata table — the SLD, APP-A, and all electrical calcs use correct micro values — but it is a reviewer-visible self-contradiction on a sheet that cites NEC 110.3(B) listing compliance, so it should be fixed.

_Reported by 1 agent(s)._


## LOST TO RATE LIMIT (23 findings deduped but never verified — re-verify by resuming workflow wf_0526f4cd-53c after limit reset; finder evidence in scratchpad/teardown-results.json is NOT included here)

- PV-1+E-1: AC disconnect 100A non-fused mismatch
- SCHED: BOM table clipped mid-row
- PV-4A vs PV-4C: attachment uplift disagreement (37x…)
- PV-0: cover ships unresolved compliance flag (dup of P0-2)
- APP-A: FlashFoot description vs racking
- APP-A: NEC voltage correction mislabeled Voc×factor
- APP-A: UL 1741-SA vs claimed compliance
- VAL-1: snow-load PASS justified by leaked fallback
- CERT: IBC/IRC bullet cites no edition year
- CERT: Document ID truncates client name
- APP-CAD: lacks legend/scale/north arrow
- SCHED vs APP-A: micro AC rating drift 0.35/0.366 kW
- PV-3/APP-A: four sheets 40-60% empty white space
- PE-1: awkward text wraps in Project Information
- PV-0+PV-5: 705.12(A) vs 705.11 citation drift
- package: no dwelling service load calculation
- E-1+SCHED: AC feeder conduit three different sizes
- PV-4B+E-1: EGC #14 vs #12 contradiction
- PV-4B: raw float 110.39999999999999
- PV-0: sheet index lists 9 of 16
- PV-4B vs E-1: branch ampacity/OCPD impossible
- PV-2/PV-2B: module count 53 vs 52 (dup of P0-24)
- PV-0 vs PE-1: seismic design category B mismatch