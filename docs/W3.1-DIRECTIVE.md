# W3.1 Directive — Corrective Pass Before W4

Status: ACTIVE 2026-07-21. W3 (`b1506bd6`) is directionally accepted but NOT closed.
W4 must not begin until W3.1 is reviewed. Ray's binding corrections, verbatim contract.

## 1. Freeze the campaign acceptance fixture

The original audited Braidon design must become an immutable repository fixture
independent of the live database:

- 31 × Q CELLS Q.PEAK DUO 400W
- 31 × Enphase IQ8A
- 12.40 kW DC
- 3 legal microinverter branches at 11/10/10
- roof-mounted system
- original audited address/project identity

Name it clearly as a test fixture, e.g. `braidon-original-audit-fixture`. Do not read
mutable production rows when generating its acceptance evidence.

Produce two separate evidence artifacts:

A. `braidon-original-w3.planset-evidence.json` — immutable campaign fixture, directly
   comparable with W1 and W2 evidence, proves the originally audited failure classes
   were corrected.
B. `braidon-live-w3.planset-evidence.json` — current database design; may reflect the
   newer 88-module hybrid/string configuration; must not replace the immutable
   acceptance fixture.

The production REC/Qcells equipment-identity conflict remains unresolved and blocking.
A deterministic test fixture may explicitly select Qcells, but that must not reconcile
or alter production data.

## 2. Unify structural and drawing coordinates

Count, spacing and ID parity are insufficient. Create an explicit canonical coordinate
authority for: module polygons; rail start/end points; splice points; attachment
points; roof-plane geometry; setbacks/pathways.

Every object must carry: coordinate-system ID; units; source frame; transform ID;
transform revision; transform provenance.

The renderer must place all physical objects by applying the snapshot-carried
transform. It may not independently geo-register or reposition them.

Add blocking invariants proving:
- every rendered attachment coordinate equals the transformed canonical attachment
  coordinate within a documented tolerance
- every rendered rail endpoint equals the transformed canonical rail endpoint
- every rendered module polygon equals the transformed canonical module polygon
- no rendered physical object exists without a canonical object ID
- no canonical physical object is omitted from the drawing

Include the transform matrix/parameters and transform digest in planset evidence.

## 3. Contain the parallel plan-set generator

The reachable `lib/plan-set/*` path and `/api/engineering/plan-set` route may not
declare `structuralStatus = PASS`, `permitReady = true`, `approved`, or `compliant`
unless they consume and validate the canonical PermitDesignSnapshot.

For W3.1, choose one immediate containment method:
- route it through PermitDesignSnapshot, or
- disable permit-ready generation on that path, or
- return an explicit LEGACY PATH — NOT FOR PERMIT / PENDING CANONICAL MIGRATION state.

It must fail closed. Add a test proving the parallel route cannot emit a structural
PASS or permit-ready artifact without a valid snapshot ID, digest and completed
validators. The permanent convert-or-delete decision may remain in W4, but the bypass
must be neutralized now.

## 4. Strengthen racking-capacity provenance

The RT-MINI 600 lb ASD authority record must include: exact source document; source
revision/date; document hash; issuing engineer/entity; jurisdiction or applicability
boundary; exact mount model; exact fastener pattern; substrate/installation condition;
adjustment factors; whether the value is ASD allowable or ultimate; proof that it
applies to the selected mixed assembly.

The 900 lb ultimate records must remain explicitly excluded from allowable-capacity
calculations. If the 600 lb source does not cover the exact selected assembly and
installation condition, preserve a blocking structural-authority gap rather than
applying it generically.

## 5. Acceptance evidence

Deliver: immutable original Braidon W3 evidence; separate current live Braidon
evidence; exact coordinate-transform report; module/rail/attachment coordinate parity
tests; proof that renderers cannot independently reposition physical objects; proof
that the parallel generator cannot emit PASS or permit-ready output; racking-capacity
provenance record; activated blocking validators; full focused test results; full
repository test results with no new failures; tsc result; production build result.

## Boundaries

- Commit W3.1 separately on dev.
- Do not modify MFA. Do not modify migration governance. Do not patch rendered HTML.
- Do not begin W4 until W3.1 is reviewed.
